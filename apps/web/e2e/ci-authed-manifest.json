{
  "ci": ["outcome-parity-panel.spec.ts"],
  "localOnly": {
    "authed-canon-p1.spec.ts": "[BL-802] CI 에서 **실측으로** 떨어졌다 (2026-08-18 run 32051177105): /dashboard·/backtests 는 통과하는데 /trading 은 「등록된 거래소 계정이 없다」, /backtests/:id/trades 는 「완료된 백테스트 상세 링크를 찾지 못했다」 — 둘 다 CI 결정론 시더가 있어야 푼다. 인프라가 아니라 데이터 전제다.",
    "api-request-dedup.spec.ts": "[BL-789] 첫 CI 검증 라운드의 대상이 아니다. 이 spec 은 아직 CI 실행 증거가 없어 다음 후보 확장 전까지 명시적으로 보류한다.",
    "authed-canon-remaining.spec.ts": "[BL-789] 첫 CI 검증 라운드의 대상이 아니다. 이 spec 은 아직 CI 실행 증거가 없어 다음 후보 확장 전까지 명시적으로 보류한다.",
    "authed-functional-parity.spec.ts": "[BL-802] 하드코딩 UUID를 사용한다. CI 결정론 시더가 없어 현재 auth DB에서 대상 엔티티를 보장할 수 없다.",
    "authed-row-locator-guard.spec.ts": "[BL-802] 하드코딩 UUID를 사용한다. CI 결정론 시더가 없어 현재 auth DB에서 대상 엔티티를 보장할 수 없다.",
    "authed-settings-save.spec.ts": "[BL-802] 하드코딩 UUID를 사용한다. CI 결정론 시더가 없어 현재 auth DB에서 대상 엔티티를 보장할 수 없다.",
    "authed-tier-c-cockpit.spec.ts": "[BL-789] 첫 CI 검증 라운드의 대상이 아니다. 이 spec 은 아직 CI 실행 증거가 없어 다음 후보 확장 전까지 명시적으로 보류한다.",
    "backtest-live-mirror.spec.ts": "[BL-802] 하드코딩 UUID를 사용한다. CI 결정론 시더가 없어 현재 auth DB에서 대상 엔티티를 보장할 수 없다.",
    "close-resting-entries.spec.ts": "[BL-802] 하드코딩 UUID를 사용한다. CI 결정론 시더가 없어 현재 auth DB에서 대상 엔티티를 보장할 수 없다.",
    "dogfood-flow.spec.ts": "[BL-789] 첫 CI 검증 라운드의 대상이 아니다. 이 spec 은 아직 CI 실행 증거가 없어 다음 후보 확장 전까지 명시적으로 보류한다.",
    "invite-token-page.spec.ts": "[BL-789] 첫 CI 검증 라운드의 대상이 아니다. 이 spec 은 아직 CI 실행 증거가 없어 다음 후보 확장 전까지 명시적으로 보류한다.",
    "live-session-deeplink.spec.ts": "[BL-802] 하드코딩 UUID를 사용한다. CI 결정론 시더가 없어 현재 auth DB에서 대상 엔티티를 보장할 수 없다.",
    "live-session-flow.spec.ts": "[BL-802] 하드코딩 UUID를 사용한다. CI 결정론 시더가 없어 현재 auth DB에서 대상 엔티티를 보장할 수 없다.",
    "report-section-anchors.spec.ts": "[BL-789] 첫 CI 검증 라운드의 대상이 아니다. 이 spec 은 아직 CI 실행 증거가 없어 다음 후보 확장 전까지 명시적으로 보류한다.",
    "sprint46-tier1-critical.spec.ts": "[BL-802] 하드코딩 UUID를 사용한다. CI 결정론 시더가 없어 현재 auth DB에서 대상 엔티티를 보장할 수 없다.",
    "sprint46-tier2-high.spec.ts": "[BL-802] 하드코딩 UUID를 사용한다. CI 결정론 시더가 없어 현재 auth DB에서 대상 엔티티를 보장할 수 없다.",
    "sprint46-tier3-nth.spec.ts": "[BL-802] 하드코딩 UUID를 사용한다. CI 결정론 시더가 없어 현재 auth DB에서 대상 엔티티를 보장할 수 없다.",
    "sprint55-optimizer-bayesian.spec.ts": "[BL-802] 하드코딩 UUID를 사용한다. CI 결정론 시더가 없어 현재 auth DB에서 대상 엔티티를 보장할 수 없다.",
    "trading-ui.spec.ts": "[BL-802] 하드코딩 UUID를 사용한다. CI 결정론 시더가 없어 현재 auth DB에서 대상 엔티티를 보장할 수 없다."
  }
}
