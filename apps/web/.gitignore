node_modules
.next
.next*/
out
dist
coverage
.env
.env.local
.env*.local
*.tsbuildinfo
next-env.d.ts
.DS_Store

# clerk configuration (can include secrets)
/.clerk/

# Sprint 25: Playwright e2e auth state (dev credentials, never commit)
e2e/.auth/

# Playwright artifacts
test-results/
playwright-report/
playwright/.cache/

# [BL-797] 화면 증거 팩의 회차 산출 (리포트·측정 JSON). baseline 과 스냅샷은 커밋 대상이라
# 여기 걸리지 않는다 — 그 둘이 PR 의 before/after 이므로 레포에 있어야 링크가 성립한다.
.screen-evidence/
