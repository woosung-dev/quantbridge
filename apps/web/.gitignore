node_modules
.next
.next*/
out
dist
coverage
.env
.env.local
.env*.local
# [BL-757] `.env.production` 은 Next.js 표준 이름이라 여기서 특히 쉽게 생긴다.
.env.prod*
.env.production*
.env.staging*
# ★부정 패턴은 **뒤에 와야** 이긴다 — 추적 중인 `.env.example` 을 되살린다.
!.env*.example
*.tsbuildinfo
next-env.d.ts
.DS_Store

# clerk configuration (can include secrets)
/.clerk/

# Sprint 25: Playwright e2e auth state (dev credentials, never commit)
e2e/.auth/

# Playwright artifacts
test-results/
playwright-report/
playwright/.cache/
