.venv
__pycache__
*.pyc
*.pyo
.pytest_cache
.mypy_cache
.ruff_cache
.coverage
htmlcov
*.egg-info
.env.local
.env
.git
.gitignore
.python-version
node_modules
dist
build

# 프로메테우스 multiproc 디렉터리 — 볼륨으로 마운트되는 런타임 산출물이라
# 이미지에 들어갈 이유가 없다. 2026-08-30 실측 6,638 entries.
.metrics

# 테스트·문서는 런타임 이미지에 들어갈 이유가 없다 — `COPY . .` 가 통째로 굽는다.
# 2026-09-10 실측: tests 21M(src 5.8M 의 3.6배). 런북 §3.3 ①-b 의 「pytest 가 없어야 한다」 판정과 같은 결.
tests
docs
*.md
.env.prod.example
