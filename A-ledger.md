# 원장 초안 — 레인 α ([BL-784])

오케스트레이터가 `docs/backlog.md` 에 반영할 초안이다. 이 레인은 `backlog.md`·`status.md`·
`lessons.md`·`dev-log/` 를 직접 건드리지 않았다.

---

## 1. [BL-784] 상태줄 교체

**교체 대상** (`docs/backlog.md`, `### BL-784` 절 끝):

```
**상태:** 🟡 부분 해결 — 2026-08-17 에 제출 동기화 1곳 보강. **기전 미확정**(가설 2건 반증) · 관측 증거(trace) 부재
```

**교체 후:**

```
**상태:** 🟡 부분 해결 — ★**기전 확정 (2026-08-17 야간, 레인 α · `stage/bl784-trace`).** 지연이 아니라 **거부**다: BE 전역 레이트리밋 `default_limits=["100/minute"]`(`apps/api/src/common/rate_limit.py:122`, 신원 단위)이 authed e2e 를 429 로 끊는다. 실패 시점 trace 를 4회 확보했고 **4회 전부** 대상 목록 요청이 **5~31ms 만에 429**(`x-ratelimit-limit: 100` · `remaining: 0` · `retry-after: 1~7`)였다. 화면에는 `API 429 /api/v1/backtests` 가 그대로 떠 있었다 — UI 는 옳게 동작했고 테스트가 기다린 행이 없었을 뿐이다. authed 스위트는 **한 신원으로 90 테스트**를 돌며 페이지마다 BE 요청 4~8건을 내 60초 창을 상시 소진한다(한 밤 BE 로그 **429 616건**). 대부분은 단언 안 하는 내비 배지 프로브라 조용히 지나가고 **하필 단언 대상이 걸린 회차만 red** 라 실패 지점이 실행마다 갈린다. ★★**전제 2건이 실측으로 뒤집혔다** — ⑴ 「단독 실행은 항상 green」이 **거짓**(단독 3회 중 1회 red). 「게이트에서만」이라는 축 자체가 틀렸고 게이트는 그 레그를 돌린 유일한 것이었을 뿐이다 ⑵ 반증됐던 「pytest 가 시드를 지운다」 가설의 출처가 **테스트의 오도 문구**였다 — `:108` 은 429 를 「데이터 시딩 필요」라고 보고한다. ★재현 **15회 중 4회 red**(부하 없음 1/9 · 합성 부하 2 조건 3/6) — 부하는 원인이 아니라 확률을 올리는 요인이다. ★계측 도입 — `PW_ARTIFACT_RUN` 관측 모드(`playwright.config.ts`) + 재현 하네스(`tools/scripts/e2e-authed-repro.sh`, shape 3종). ★★**증거가 없던 이유가 확인 행위였다** — playwright 는 매 실행 setup 에 `outputDir` 을 통째로 지우고 project 7종이 `test-results/` 하나를 공유해서, 게이트 red 뒤 「단독으로도 실패하나」를 돌리는 순간 그 trace 가 파괴된다([LESSON 후보]). **수리는 미착수** — 처방은 아래.
**트리거 판정:** 도래 — 기전이 확정됐고 수리 대상이 특정됐다
```

★**권장 접근 ⑵ 는 번지수가 틀렸다** — 「고정 대기를 조건 대기로」는 요청이 **거부**된 이 기전을
못 고친다. 본문의 권장 접근을 아래로 교체할 것을 제안한다:

```
**권장 접근:** ⑴ ★**한도를 e2e 신원에서만 풀거나 넓힌다.** `default_limits` 는 프로덕션 방어물이니
지우지 말고 개발/e2e 프로필에서만 키우거나 면제 신원을 둔다. ★음성 대조 필수 — 면제가 프로덕션
경로로 새지 않는지 확인해라 ⑵ **FE 가 `Retry-After` 를 존중하게 한다** — 429 에 `retry-after: 7` 이
왔는데 React Query 는 1.15초 뒤 한 번 치고 포기했다. e2e 와 무관하게 실사용자에게도 옳다
⑶ **중복 요청을 줄인다** — trace 에서 목록·배지 요청이 **전부 쌍으로** 나갔다(같은 URL 2건). 창을
두 배로 먹는다. 어디서 두 번 나가는지가 별도 조사거리다 ⑷ ★**timeout 예산을 늘리지 마라**
⑸ ★**수리 뒤 판별력 증명** — 한도를 인위적으로 낮춰(예: `5/minute`) 그 테스트가 red 가 되는지 확인해라
```

---

## 2. 새 항목 제안 — 목록·배지 요청이 쌍으로 나간다

기존 BL 에 해당 항목이 없으면 신규 등재를 제안한다(번호는 오케스트레이터가 배정 —
`origin/main` 뿐 아니라 **열린 PR 의 diff** 도 봐야 충돌하지 않는다).

```
**Title:** authed 화면이 같은 BE 요청을 **쌍으로** 낸다 — 레이트리밋 창을 두 배로 먹는다
**Category:** Frontend / 데이터 페칭
**Priority:** P2
**Trigger:** 도래 — 2026-08-17 trace 로 실측
**Est:** S (원인 특정이 본체. 중복 지점 1~2곳으로 추정)
**출처:** 2026-08-17 레인 α [BL-784] 관측

**원인 / 영향:** `/backtests` 진입 trace 에서 578ms 에 나간 BE 요청이 **전부 2건씩**이었다 —
`orders?state=…&limit=1` ×2 · `backtests?limit=1` ×2 · `strategies?limit=1` ×2 ·
`strategies?limit=100` ×2 · `backtests?limit=20&order_by=created_at` ×2. React Query 는 같은
queryKey 를 dedupe 하므로, 쌍이 나간다는 것은 **키가 다르거나 QueryClient 가 둘**이라는 뜻이다.
[BL-784] 의 429 는 이 중복이 없었으면 창을 절반만 먹었다.

**Risk:** 🟡 (기능 영향 없음. 한도·부하만 두 배)
**상태:** ⬜ Open — 2026-08-17 trace 실측으로 확정. 미착수
```

---

## 3. `docs/lessons.md` 반증 카드 후보

```
### [LESSON-###] 실패를 확인하려고 다시 돌리는 행위가 그 실패의 증거를 지운다 (2026-08-17)

playwright 는 매 실행의 setup 단계에서 `outputDir` 을 통째로 지운다
(`runner/tasks.js` `createRemoveOutputDirsTask`). 이 레포의 project 7종은 기본 `test-results/`
하나를 공유하므로 **어떤 `--project` 로 돌리든 직전 회차 산출물이 사라진다.**

[BL-784] 는 넉 달 가까이 「실패 시점 trace 가 없다」로 막혀 있었는데, 설정
(`trace: "retain-on-failure"`)은 처음부터 정상이었다. 사라진 것은 기록이 아니라 **기록의 수명**이고,
그것을 끊은 것은 게이트 red 직후 사람이 하는 가장 자연스러운 행동 — 「단독으로도 실패하나」 —
바로 그것이었다.

★**이 회차가 그 함정을 문서화한 직후 자신이 다시 밟았다.** 변이 M1(관측 모드 무력화)을 심고
돌리자 그때까지 모은 9회분 `results.json`·trace 가 같은 기전으로 전부 삭제됐다.
**계측기를 끄는 변이는 계측 결과가 있는 자리에서 돌리지 마라.**

일반화: **관측 대상과 관측 산출물이 같은 저장소를 공유하면, 재관측이 원관측을 지운다.**
```

---

## 4. 이 브랜치의 커밋

`stage/bl784-trace` — push·PR 없음(야간 계약).

| 파일                                           | 무엇                                                                                |
| ---------------------------------------------- | ----------------------------------------------------------------------------------- |
| `apps/web/playwright.config.ts`                | `PW_ARTIFACT_RUN` 관측 모드 (project 별 `outputDir` · `trace:"on"` · json reporter) |
| `tools/scripts/e2e-authed-repro.sh`            | 신규 — 게이트 조건 재현 하네스 (shape 3종 + 유계 합성 부하)                         |
| `docs/reference/operations/gates-and-traps.md` | 관측 방법 + 429 기전 절 신설                                                        |

산출 증거: `.claude/bl784-evidence/`(gitignore 대상) · 보고서 `A-REPORT.md`.
