# 원장 초안 — 레인 γ ([BL-786])

수치의 정본은 `C-REPORT.md` 다. 여기서는 원장에 옮길 문장만 낸다.

## `docs/backlog.md` — BL-786 상태줄 교체

```
**상태:** ✅ RESOLVED — 2026-08-17 `stage/bl786-dupreq`. 원인은 StrictMode 가 **아니다**(프로덕션
standalone 빌드에서 동일 재현). React Query 키의 첫 인자 `uid` 가 한 로드 안에서 `"anon"` → 진짜
id 로 바뀌어 모든 목록·배지 쿼리가 두 키로 각각 나갔다. SSR 이 아는 `userId` 를 첫 렌더에 넘겨
키 churn 을 없앴고(`ServerIdentityProvider` + `useAuthCtx`), 덤으로 `/backtests` 의 SSR prefetch
키가 클라이언트 키와 어긋나 **SSR 요청이 통째로 버려지던** 것도 공용 생성자(`features/backtest/
list-query.ts`)로 정렬했다. 검사면 신설 = `e2e/api-request-dedup.spec.ts`(수리 전 red 확인, 변이 2/2 red).
측정·판정 전문 = `C-REPORT.md`
```

## `docs/backlog.md` — [BL-784] 본문에 덧붙일 한 줄

[BL-784] 의 처방 ⑶(「목록·배지 요청이 전부 쌍으로 나간다 → [BL-786]」)이 종결됐고, **그 결과가
[BL-784] 의 산술을 바꾼다** — 다만 레인 파일이 예상한 방향은 아니다.

```
⑶ 종결(2026-08-17, [BL-786]). ★중복은 **프로덕션에도 그대로 있었다** — 개발/e2e 한정이 아니다.
   따라서 「e2e 만 창을 두 배로 먹는다」가 아니라 실사용자도 같은 배수였고, 수리는 양쪽에 똑같이
   듣는다. authed e2e 가 한 신원으로 소비하던 100/minute 창은 화면당 브라우저 요청이
   `/backtests` 10→4 · `/dashboard` 15→8 로 줄어든 만큼 늦게 찬다(수치 정본 = `C-REPORT.md`).
   ★한도 자체는 이 레인이 건드리지 않았다 — 레인 α 몫이다.
```

## `docs/lessons.md` 후보 (승격은 오케스트레이터 판단)

**「동시에 두 번」은 「한 번 마운트되고 두 번 요청」이 아니라 「두 키가 각각 한 번」일 수 있다.**
증상은 같은 ms 에 같은 URL 두 건이라 컴포넌트 이중 마운트나 StrictMode 로 읽히기 쉽다. 실제로는
queryKey 의 신원 성분이 로드 중에 한 번 바뀌었고, 두 요청이 같은 ms 에 나간 것은 둘 다 토큰 발급을
기다렸다가 함께 풀렸기 때문이다. **증상의 동시성이 원인의 동시성을 뜻하지 않는다.**

가른 방법 셋: ⒜ 프로덕션 빌드에서 재현 여부(StrictMode 배제) ⒝ DOM 에서 셸 개수 세기(이중 마운트
배제) ⒞ 키 성분의 전이를 타임라인으로 찍기(확정). 앞의 둘은 **가설을 죽이는 데만** 쓸 수 있고,
확정은 ⒞ 가 했다.

**곁가지 교훈 — SSR prefetch 는 키가 한 글자만 어긋나도 조용히 전량 낭비된다.** `/backtests` 는
넉 달간 SSR 이 목록을 가져와 버리고 브라우저가 다시 가져오고 있었다. 어떤 테스트도 이것을 안 봤고,
증상은 「요청이 두 번」으로만 보였다. 이 레포에는 이미 답(`features/strategy/sort.ts` 의 서버·클라이언트
공용 순수 모듈)이 있었는데 한쪽 도메인만 그것을 갖고 있었다.
