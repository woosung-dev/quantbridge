# CODEOWNERS — 리뷰 라우팅 + 구조 문서화 ([BL-718])
#
# ★★**이 파일의 강제력은 0 이다.** 2026-08-13 실측·2026-08-18 재확인:
#     gh api repos/woosung-dev/quantbridge/branches/main/protection → 404
#     gh api repos/woosung-dev/quantbridge/rulesets               → []
#   브랜치 보호도 ruleset 도 없으므로 GitHub 은 이 파일을 근거로 **아무것도 막지 않는다.**
#   지금 얻는 것은 둘뿐이다 — ⑴ PR 에 리뷰어가 자동 배정된다 ⑵ 어느 구획이 누구 소유인지
#   저장소가 스스로 답한다. **「CODEOWNERS 가 있으니 리뷰 없이는 못 머지한다」고 읽지 마라.**
#   강제가 필요해지면 ruleset(required review) 도입을 **별도 결정**으로 올려라 — 이 파일을
#   고치는 것으로는 강제가 생기지 않는다.
#
# 구획은 [ADR-029] 표준 레이아웃의 최상위 4종을 따른다. 경로 4종 실재 확인 완료(2026-08-18).
# 규칙은 **아래로 갈수록 이긴다**(마지막에 일치한 줄이 소유자다) — 순서를 바꾸지 마라.

# ── 기본 소유 ────────────────────────────────────────────────────────────
*                   @woosung-dev

# ── 애플리케이션 코드 ────────────────────────────────────────────────────
# 규칙 본문은 apps/api/AGENTS.md · apps/web/AGENTS.md 가 정본이다.
/apps/              @woosung-dev

# ── 인프라 ───────────────────────────────────────────────────────────────
/infra/             @woosung-dev

# ── 운영 셸 ──────────────────────────────────────────────────────────────
# ledger-vitals · soak-gate 등. 구 감사기 층(final-gates·bl-audit·docs-audit 등)은
# ADR-037 제로베이스로 철거 — 원문 = git show harness-v1:tools/scripts/.
# ★여기가 바뀌면 「무엇이 통과인가」의 정의가 바뀐다 — 리뷰 우선순위가 가장 높은 구획이다.
/tools/             @woosung-dev

# ── 결정 기록 ────────────────────────────────────────────────────────────
# ADR 은 폐기가 삭제가 아니라 `Superseded` 표기다([ADR-026]). 변경 전 필독 대상.
/docs/adr/    @woosung-dev

# ── CI 배선 ──────────────────────────────────────────────────────────────
# ★이 디렉터리가 「CI 초록이 무엇을 뜻하는가」를 정한다. authed e2e 가 CI 에서 0회 돌던 것을
#   [BL-789] 가 적발한 자리다 — 여기 변경은 게이트 변경과 같은 무게로 본다.
/.github/           @woosung-dev
