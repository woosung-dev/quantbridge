# Sprint X1+X3 — Final Report

> **Date:** 2026-04-23 | **Duration:** ~2시간 30분 (사용자 개입 1회 "ok") | **Stage branch:** `stage/x1-x3-indicator-ui` @ `da366b9`

---

## 1. Sprint 개요

L2 Deep Parallel + 3-way Evaluator (codex CLI + Opus 빈컨텍스트 + Sonnet 빈컨텍스트) 로 5 워커 병렬 실행. dogfood 18 매트릭스의 5개 공백 해소.

| W   | 영역                               | Branch                          | Commit    | 결과 |
| --- | ---------------------------------- | ------------------------------- | --------- | ---- |
| W1  | alert heuristic loose mode         | `worktree-agent-a3edae41`       | `07a7f41` | ✅   |
| W2  | `ta.sar` Parabolic SAR stdlib      | `worktree-agent-a2493f6f`       | `73bc311` | ✅   |
| W3  | EquityChart width(-1) fix          | `feat/x1x3-w3-equity-chart-fix` | `acfa9b1` | ✅   |
| W4  | Trade Analysis direction breakdown | `w4/trade-analysis-breakdown`   | `cb44a49` | ✅   |
| W5  | Re-run button                      | `feat/x1x3-w5-rerun-button`     | `5e963da` | ✅   |

---

## 2. 3-way Evaluator 종합 판정

| W   | Codex | Opus   | Sonnet  | 평균 | 판정                                           |
| --- | ----- | ------ | ------- | ---- | ---------------------------------------------- |
| W1  | 8 GWF | 8 GO   | 8 GWF   | 8.0  | 1G+2GWF → fixes applied                        |
| W2  | 7 GWF | 8 GO ✓ | 8 GO ✓  | 7.7  | 2G+1GWF → clean                                |
| W3  | 8 GWF | 8.5 GO | 8 GWF   | 8.2  | 1G+2GWF → accept (modern browser RO universal) |
| W4  | 8 GWF | 9 GO ✓ | 8 GWF   | 8.3  | 1G+2GWF → fix applied (boundary test)          |
| W5  | 7 GWF | 9 GO   | 8.5 GWF | 8.2  | 1G+2GWF → accept (cosmetic)                    |

✓ = Opus/Sonnet 가 worktree 에서 실제 테스트 재실행 + git merge-tree 충돌 검증

**평균 8.1/10. 5/5 워커 모두 squash merge 가능 판정.**

---

## 3. Orchestrator Follow-up Fixes (`f483598`)

3 평가자가 발견한 finding 중 **must-fix** 적용:

1. **W1 Golden Rule 위반** — `PINE_ALERT_HEURISTIC_MODE` 환경변수가 코드에서 참조되지만 `.env.example` 에 없음
   - `backend/.env.example` + 루트 `.env.example` + `docs/05_env/env-vars.md` 에 추가
2. **W4 disclosure boundary test** — codex+Sonnet 동시 finding (Sonnet 가 fix 코드 제공)
   - `trades.length === num_trades` (disclosure 미표시) + `< num_trades` (표시) 회귀 테스트 2건 추가

추가 ruff fix (`da366b9`): `test_stdlib_sar.py` 의 `for h, l in zip(...)` → `for h, lo in zip(...)` (E741 Ambiguous variable name).

---

## 4. Stage 합류 검증 결과

### 정량 (모두 GREEN)

| 검증                        | 결과                                            |
| --------------------------- | ----------------------------------------------- |
| Backend pytest              | **946 passed**, 1 skipped (legacy golden, 무관) |
| Frontend vitest             | **28 files / 167 tests passed**                 |
| FE `tsc --noEmit`           | exit 0                                          |
| FE `lint`                   | 0 errors / 0 warnings                           |
| Backend `ruff check`        | All checks passed                               |
| i2_luxalgo E2E (loose mode) | 3/3 passed                                      |
| i3_drfx E2E + supertrend    | 3/3 passed                                      |

### Baseline 비교

- Backend: 922 (pre-sprint) → 946 (+24 = W1×14 + W2×10 + W4 disclosure×2 - 일부 인덱스 변동)
- Frontend: 140 (pre-sprint) → 167 (+27 = W3×3 + W4×11 + W5×14 - 일부 인덱스)

---

## 5. 18 매트릭스 dogfood 공백 해소

| 공백 (이전)                         | 해결                                                                                                                              |
| ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| `i2_luxalgo` 0 trades × 2           | W1 + `PINE_ALERT_HEURISTIC_MODE=loose` 설정 시 LONG_ENTRY 분류. E2E 3/3 PASS (loose)                                              |
| `i3_drfx` FAILED × 2                | W2 ta.sar 구현. strict=False 유지 + supertrend tuple unpack PASS (strict=True 는 ta.rma 미구현으로 H2 deferred — out of W2 scope) |
| FE EquityChart `width(-1) warning`  | W3 ResizeObserver mount gate. vitest 3건 PASS (mount + width=0 placeholder + width≥1 chart)                                       |
| Trade Analysis long/short breakdown | W4 `computeDirectionBreakdown` + 방향별 성과 section. pagination disclosure 포함                                                  |
| Backtest "재실행" 버튼              | W5 RerunButton in detail header. terminal 상태에서만 활성화 + sonner toast                                                        |

---

## 6. Follow-up Items (별도 PR — non-blocking)

3 평가자가 발견한 minor finding 중 **defer to next sprint**:

1. **W1**: `_KEYWORD_RULES_LOOSE` 의 EXIT 우선순위 명시 테스트 (`"close long"` → LONG_EXIT). Sonnet finding — 잠재 금전 위험이지만 코드는 올바름, 회귀 방지 테스트만 추가하면 됨
2. **W1**: JSON except 변경 (`AttributeError` → `ValueError`) 의도성 검증 — Opus finding
3. **W2**: Wilder reference 12-bar golden vector 추가 (codex self 마크다운에 표는 있지만 코드화 안 됨) — codex finding
4. **W3**: ResizeObserver 미지원 + initial width=0 fallback. modern browser 100% 지원이라 영향 0이나 방어적 추가 권고 — codex finding
5. **W3**: Playwright `/backtests/<id>` 첫 페인트 console warning 0건 검증 — Phase 4 deferred (이 세션은 Clerk 세션 미설정으로 사용자 측 검증 권고)
6. **W4**: 서버 측 long/short breakdown 컴퓨테이션 (BE schema 확장 — H2) — Opus finding
7. **W5**: double-click race guard (useRef + 즉시 disable) — Sonnet finding
8. **W5**: `initial_capital` NaN/Infinity 단위 테스트 — codex finding

---

## 7. Stage Commits (8개)

```
da366b9 fix(pine_v2): rename loop var l → lo (ruff E741) in test_stdlib_sar
f483598 fix(sprint-x1+x3): orchestrator follow-up — env doc + W4 disclosure boundary test
dd06e45 feat(backtest): re-run button in detail header (W5, sprint X1+X3)
05a2231 feat(backtest): Trade Analysis direction breakdown (W4, sprint X1+X3)
f613733 fix(backtest): EquityChart width(-1) warning via ResizeObserver mount gate (W3)
b692812 feat(pine_v2): ta.sar Parabolic SAR stdlib (W2, sprint X1+X3)
a8b4e0b feat(pine_v2): alert heuristic loose mode (W1, sprint X1+X3)
b24f385 docs(reviews): Sprint X1+X3 3-way Evaluator reports (W1-W5)
840f9a9 docs(plans): Sprint X1+X3 5 worker plans + review/report dirs
```

---

## 8. Acceptance Criteria 종합

### 정량 (sprint plan §Acceptance Criteria 정량)

- [x] 18 매트릭스 PASS ≥ 17/18 — `i2_luxalgo` 3/3 (loose) + `i3_drfx` 3/3 (strict=False)
- [x] backend pytest 녹색 — 946 passed
- [x] FE typecheck / lint clean — tsc 0, lint 0/0
- [x] 각 워커 PR 에서 3-way Evaluator 3/3 GO 또는 2/3 GO + fix-applied — 5/5 충족
- [ ] EquityChart 콘솔 warning 0건 (Playwright) — vitest 검증 ✅, live Playwright 는 Clerk 세션 필요 (사용자 측 deferred)
- [x] Trade Analysis 에 long/short count + direction breakdown 표시 — W4 컴포넌트 + 11 tests
- [x] Backtest detail/list 에 "재실행" 버튼 1-click 동작 — W5 + 14 tests

### 정성

- [x] 새 코드가 기존 테스트 semantic drift 없음 — 946 baseline 유지 (skipped 1 legacy 무관)
- [x] AC 에 spurious pass 허점 없음 — 3-way evaluator 가 cross-check
- [x] memory 규칙 준수 (LESSON-004 effect-loop / Option C / Decimal-first / Golden Rule env)
- [x] dogfood UX 체감 개선 — RerunButton + 방향별 breakdown + EquityChart warning 제거
- [x] edge case 커버 — 평가자 5종 × 5 워커 검증

---

## 9. 사용자 개입 통계

- **시작 "ok"**: 1회 (Phase 1)
- **최종 merge 승인**: 대기 중 (Phase 5 — 본 리포트 후)
- **총 인터럽트**: 1회 (목표 2회 ✅)

---

## 10. 다음 단계 — 사용자 승인 필요

stage 브랜치는 `da366b9` 에서 origin 동기화 완료. 사용자가 **stage → main merge 수동 진행** (Option C — main 직접 touch 금지).

```bash
# 권장 명령:
git checkout main
git merge --ff-only stage/x1-x3-indicator-ui
git push origin main
git branch -d stage/x1-x3-indicator-ui
git push origin --delete stage/x1-x3-indicator-ui
```

Or via GitHub UI: PR 생성 → 자동 merge.

또는 `git diff main..stage/x1-x3-indicator-ui` 로 최종 검토 후 fast-forward.

---

## 11. Cleanup TODO (Phase 5 후)

- [ ] 5 워커 worktree 제거: `git worktree remove --force .claude/worktrees/agent-{a3edae41,a2493f6f,a3916c29,a5bc9ad0,a0d2ef2b}`
- [ ] 5 워커 branch 삭제: `git branch -D worktree-agent-a3edae41 worktree-agent-a2493f6f feat/x1x3-w3-equity-chart-fix w4/trade-analysis-breakdown feat/x1x3-w5-rerun-button`
- [ ] `/tmp/w{1..5}-diff.txt` + `/tmp/w{1..5}-eval-prompt.txt` 정리 (선택)
- [ ] beat container `trading_encryption_keys` 누락 해소 (별건 이슈 — Sprint 6 follow-up)
