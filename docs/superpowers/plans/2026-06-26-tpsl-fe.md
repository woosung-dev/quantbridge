# Wave 2 FE — manual TP/SL + risk% + liquidation display + cockpit polish 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans 로 task-by-task 실행. 체크박스(`- [ ]`)로 추적.

**Goal:** 라이브 손익보호 프리미티브(Wave 1 BE 필드)를 FE 에서 입력·표시하고, 청산가/risk% 크로스도메인 계약을 graceful 하게 소비하며, 콕핏을 모바일 반응형으로 다듬는다.

**Architecture:** 기존 trading feature 모듈(`features/trading/`)에 surgical 추가. 신규 BE 엔드포인트 0 — Wave1 `OrderRequest`/`OrderResponse` 의 optional 필드를 기존 webhook/order 경로에 전달. 청산가·risk% 는 미머지 계약(W-A/W-B)이라 graceful-empty 처리, 최종 wire-up 은 Orchestrator Phase 3.

**Tech Stack:** Next.js 16 (React 19) · zod v4 · react-hook-form · @tanstack/react-query · vitest · Tailwind v4.

## 컴포넌트별 TDD 모드
- `schemas.ts` (OrderSchema, LiquidationInfoResponseSchema): 정석 TDD (공유 zod 스키마).
- `query-keys.ts` (liquidation key): 정석 TDD (factory).
- `hooks.ts` (useLiquidationInfo): 정석 TDD (React Query hook).
- `api.ts` (getLiquidationInfo): 정석 TDD (graceful 계약 소비).
- `test-order-dialog.tsx` (TP/SL/risk% 입력 + payload): test+impl 동시 (pure UI + 폼 draft state).
- `orders-panel.tsx` (TP/SL/청산가 컬럼): test+impl 동시 (pure UI 렌더).
- 콕핏 반응형 polish: 시각 변경 — 단위테스트 회귀 0 + dev smoke 검증.

## Global Constraints
- TS strict, `any` 금지 (`unknown` + type guard).
- LESSON-004: `react-hooks/*` eslint-disable 금지.
- LESSON-005: queryKey factory 첫 인자 = userId.
- LESSON-006: render body 에 `ref.current=` 금지.
- 신규 파일 첫줄 한국어 역할주석. 커밋 영어 + 🤖 trailer.
- frontend/ 전용. backend 미편집. 신규 BE 엔드포인트 0.
- 청산가/risk% = 미머지 계약 → graceful-empty, 콘솔에러 0.
- 터치타겟 ≥ 44pt.

---

## Task 1: OrderSchema Wave1 필드 1:1 (정석 TDD)

**Files:**
- Modify: `frontend/src/features/trading/schemas.ts`
- Test: `frontend/src/features/trading/__tests__/order-schema.test.ts` (신규)

**Interfaces:**
- Produces: `OrderSchema` 에 `reduce_only: boolean`, `trigger_price: string|null`, `trigger_by: string|null`, `take_profit: string|null`, `stop_loss: string|null`. BE `OrderResponse`(schemas.py:67-94) 와 1:1. Decimal→string 직렬화 규약 준수. 누락 fixture 회귀 방지 위해 `.default()`.

- [ ] Step 1: 신규 + 누락 fixture 둘 다 parse 성공하는 테스트 RED
- [ ] Step 2: OrderSchema 에 5필드 추가 (GREEN)
- [ ] Step 3: 전체 trading 테스트 회귀 0 확인
- [ ] Step 4: commit

## Task 2: 청산가 계약 — schema + query-key + api + hook (정석 TDD, graceful)

**Files:**
- Modify: `frontend/src/features/trading/schemas.ts` (LiquidationInfoResponseSchema)
- Modify: `frontend/src/features/trading/query-keys.ts` (liquidation key, userId 첫인자)
- Modify: `frontend/src/features/trading/api.ts` (getLiquidationInfo, graceful)
- Modify: `frontend/src/features/trading/hooks.ts` (useLiquidationInfo)
- Test: `frontend/src/features/trading/__tests__/liquidation.test.ts` (신규)

**Interfaces:**
- W-B 계약: `LiquidationInfoResponse`(symbol, entry_price, side, leverage, liquidation_price, maintenance_margin_rate, distance_pct). Decimal→string.
- `getLiquidationInfo(params, token)` → 404/연결거부 시 throw(React Query error→graceful-empty 렌더).
- `useLiquidationInfo(params)` → enabled gate(파라미터 완비 시만), userId 첫인자, retry false (premature 콘솔노이즈 회피).

- [ ] Step 1: schema parse + queryKey 모양 테스트 RED
- [ ] Step 2: 구현 GREEN
- [ ] Step 3: commit

## Task 3: OrdersPanel — TP/SL + 청산가 컬럼 (test+impl 동시, graceful)

**Files:**
- Modify: `frontend/src/features/trading/components/orders-panel.tsx`
- Test: `frontend/src/features/trading/__tests__/OrdersPanel.test.tsx` (보강)

**Interfaces:**
- Consumes: OrderSchema Wave1 필드. 청산가는 W-B 미머지 → graceful-empty "—" (Phase 3 wire-up).

- [ ] Step 1: TP/SL 값 렌더 + 청산가 컬럼 "—" 테스트
- [ ] Step 2: 컬럼 추가 (모바일 가로스크롤 보존)
- [ ] Step 3: 회귀 0 + commit

## Task 4: test-order-dialog — 수동 TP/SL + reduce_only + risk% 택일 (test+impl 동시)

**Files:**
- Modify: `frontend/src/features/trading/components/test-order-dialog.tsx`
- Test: `frontend/src/features/trading/components/__tests__/test-order-dialog.test.tsx` (보강)

**Interfaces:**
- Consumes: 기존 webhook 경로. W-A `risk_percent`(Decimal,%) 계약.
- payload: 기존 5필드(symbol/side/type/quantity/exchange_account_id) 순서 보존 — optional 필드는 **값이 있을 때만** append (기존 HMAC golden vector + body 정확매칭 테스트 보존).
- sizing_mode 택일: "quantity"(기본) | "risk_percent". superRefine 로 모드별 필수 검증. 기존 에러 메시지("수량을 입력하세요." 등) 보존.

- [ ] Step 1: TP/SL/reduce_only 입력 시 payload append 테스트 + risk% 모드 테스트
- [ ] Step 2: TEST_ORDER_FORM_SCHEMA 확장 + 입력 필드 + payload 조립
- [ ] Step 3: 기존 8 테스트 회귀 0 + commit

## Task 5: 콕핏 반응형 polish (positions/PnL/KS, 터치타겟 ≥44pt)

**Files:**
- Modify: `frontend/src/app/(dashboard)/trading/_components/trading-tabs.tsx` 또는 관련 콕핏 컴포넌트 (신규 API 0).

- [ ] Step 1: 터치타겟 + 반응형 grid 점검·수정
- [ ] Step 2: dev smoke (3100) + 회귀 0 + commit

---

## Self-Review
- 스펙 5개 scope 항목 → Task 1~5 매핑 완료.
- 청산가/risk% graceful + Phase 3 명시.
- 타입 일관성: LiquidationInfoResponse 필드명 W-B 계약과 일치.
