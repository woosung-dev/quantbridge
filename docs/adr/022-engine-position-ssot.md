# ADR-022: 엔진 포지션 SSOT — 원장을 진실로, 거래소는 거부권만 (veto + 관망)

> **상태:** 확정 (Accepted)
> **일자:** 2026-08-04
> **출처:** 2026-08-04 engine-position-ssot ([BL-591] §설계 축 4개 질문의 사용자 확정)
> **관련:** [`tasks/live_signal.py:2528`](../../apps/api/src/tasks/live_signal.py) (`run_live` 재생) · `live_signal.py:2248` (동어반복 금지 제약) · `live_signal.py:494-516` (발산 분류기) · [`pine_v2/event_loop.py:192`](../../apps/api/src/strategy/pine_v2/event_loop.py) (주입점) · [`pine_v2/strategy_state.py:331-357`](../../apps/api/src/strategy/pine_v2/strategy_state.py) (`seed_positions_from_ledger`)

---

## 배경

**엔진 포지션에는 SSOT 가 없다.** `run_live` 는 매 tick 봉을 처음부터 재생해 포지션을 **도출**하며
(`live_signal.py:2528`), 정상 운행 중엔 현실로 **보정되지 않는다.** `last_strategy_state_report` 는
발산 스트라이크 플래그와 보고용일 뿐 엔진 상태를 이어받지 않는다. ⇒ **엔진에 「쓸」 자리가 없다.**

그래서 시스템이 어긋남에 대해 할 수 있는 일은 **죽이는 것**뿐이고, 실제로 `direction`(양쪽이 정반대)
갈래만 fail-closed 종료시킨다. 이 축에서 증상 BL 을 16건 Resolved 했으나 소크는 **스스로 죽을 때
늘 2시간 안에 죽는다**(가드 머지 후 자동 종료 6/6). 기전은 매번 달랐고 뿌리는 하나다.

## 결정

**엔진 포지션 SSOT = 원장(`trading.orders`). 거래소는 값을 주지 않고 거부권만 행사한다.**

```
매 tick:
  ① 원장에서 포지션 유도            (값의 유일한 출처)
  ② 거래소 스냅샷 조회              (이미 매 tick 한다 — 게이트 비용 0)
  ③ ①≠② → veto                     (거래소는 "넣지 마"만 말한다)
       veto 아님 → 엔진이 flat 이면 주입
       veto      → 주입 ✖ + 발주 ✖ (관망) + 버팀 카운터 +1
                   상한 초과 시 오늘처럼 fail-closed 종료
  ④ 주입/veto/버팀 내역을 tick 상태(jsonb)에 영속
  ⑤ 주입 후 남는 발산은 현행대로 `direction` 만 킬
```

**상한은 `interval` 비례**로 둔다(`max(절대 하한, interval × N)` 류 혼합형).
**계수는 계측 실측으로 정하고, 그 전에는 켜지 않는다.**

## 근거

**1. 거래소를 값의 출처로 쓰면 안전망이 사라진다** — `live_signal.py:2248` 이 이미 적고 있다:
「seed 를 거래소에서 가져오면 아래 대조가 **동어반복**이 되어 가드가 통째로 사라진다」.
veto 는 거래소가 **막을 수만 있고 값을 주지 못하므로** 이 제약에 걸리지 않는다. 사후
「엔진 vs 거래소」 대조는 값의 출처가 원장이라 **여전히 독립**이다.

**2. 원장 구멍은 실재하나 앱의 결함이 아니다** — `trading.exchange_exits` 실측(2026-08-04):
청산 103건 중 원장 밖(`external_manual`) **12건 = 11.7%**, 6일에 걸쳐 상시. **그러나 12건 중 10건이
「활성 세션 없음」 구간**이고, `src/` 에는 원장을 건너뛰는 청산 경로가 **없다**
(`ClosePositionService` → `OrderService.execute` → `Order` 행). 범인은 운영자 스크립트다([BL-593]).
⇒ 구멍을 먼저 메우는 선행 슬라이스보다 **veto 게이트가 정확한 대응**이다.

**3. 게이트 비용이 0 이다** — `_detect_position_divergence`(`live_signal.py:537`)가 매 tick
`fetch_open_positions`(:570)를 이미 호출한다. 거래소 스냅샷은 이미 손에 있다.

**4. 관망이 지연을 흡수한다** — 원장이 거래소 청산을 아는 지연 실측(n=90):
p50 **7초** · p90 **266초** · max **1925초**. **82.2% 가 1 tick 안**에 해소되지만 **15.6% 가 2 tick 을
넘긴다.** 현행 유예는 1회(=2 tick)뿐이라 그 15.6% 를 못 덮는다. 관망 중엔 발주가 없으므로
**돈이 새지 않고**, 잃는 것은 기회뿐이다.

**5. 상한이 절대 시간이면 안 된다** — `list_active_due` 가 `last_evaluated_bar_time + interval <= now`
로 due 를 정하므로 **tick 주기 = interval** 이다(1m/5m/15m/1h → 60/300/900/3600초).
⇒ 「2 tick 유예」는 1분봉이면 2분, 1시간봉이면 2시간이다. ★**현행 `direction_transient` 1회 유예도
이미 interval 비례**이며 아무도 명시한 적이 없었다. 단 **순수 비례도 안 된다** — 지연의 원인은
절대 시간(WebSocket 왕복 · 폴링 300초)이고 관망의 비용은 봉 개수(interval 비례)라 **비대칭**이다.
그래서 혼합형이고, 계수는 추정이 아니라 실측으로 정한다.

## 대안 (거부됨)

- **B. 거래소 SSOT** — 매 tick 거래소 포지션을 엔진에 복사. **가드 소멸**(근거 1). 게다가 포지션
  크기만으로는 진입가·`trade_id`·pending order 를 복원하지 못한다.
- **D. 조정 연산 정의** — 엔진에 쓸 자리가 없으므로 결국 같은 주입점을 쓴다. **C 의 부분집합.**
- **구멍부터 메우는 선행 슬라이스** — 수집기가 `external_manual` 청산에 합성 `Order` 행을 쓴다.
  머니-패스 원장에 **「발주한 적 없는 주문 행」**이 섞이고, 수집이 300초 주기라 최대 5 tick 구멍이
  어차피 남는다. veto 가 그 구간을 이미 덮는다.
- **게이트 없이 주입** — [BL-591] 이 경고한 「C 가 지금보다 나쁘다」가 정확히 이 경우다.
  오주입 → 반대 주문 발주 → **그 뒤에야** 가드 발화. **돈이 먼저 나간다.**
- **주입만 건너뛰고 발주는 그대로** — C 의 효과가 82% tick 에만 미치고, 지연이 긴 15.6% 구간은
  오늘과 똑같이 발산·사망한다.
- **veto 면 즉시 킬** — 체결 직후 tick 의 17.8% 에서 즉사. 지금 1회 유예로 살아나던 사례가 전부
  사망으로 바뀌어 **P0([BL-003])의 달력 시계를 오히려 말린다.**
- **원장으로 전면 덮어쓰기**(주입 범위 확대) — `engine_only` 314 · `size` 28 까지 사정권에 들지만
  **멱등성이 깨진다**(재생이 스스로 만든 포지션과 원장이 섞여 이중 계상). 재생 포지션을 지우는
  로직이 새로 필요하고, 원장 구멍 11.7% 에 훨씬 크게 노출된다.
- **킬 대상을 `engine_only`·`size` 로 확대** — 314+28 규모로 사망해 소크가 사실상 못 돈다.
  분류기 주석이 이미 「양쪽을 죽이면 이 스프린트가 고치려는 상태에서 세션이 상시 사망」이라 경고한다.

## 결과

**기대 효과를 정확히 적는다 — C 는 교정이 아니라 예방이다.**

`seed_positions_from_ledger` 는 `if not legs or self.open_trades: return ()` 로 **엔진이 완전히
비었을 때만** 주입한다(`strategy_state.py:357`). 분류기와 대조하면 주입이 닿는 것은 엔진 flat 인
**`exchange_only` 21건뿐**이고, `engine_only` 314 · `size` 28 · `direction` 3 · `transient` 23 은
`open_trades` 가 차 있어 **주입 자체가 일어나지 않는다.**

★★★**주입과 `direction` 킬은 같은 tick 에 공존할 수 없다** — `direction` 은 양쪽 non-flat,
주입은 엔진 flat 이 조건이다. ⇒ **C 는 소크를 죽이는 그 발산을 고치지 못한다.** 진입을 놓친
상태를 미리 메워 **나중에 `direction` 으로 발전하는 것을 막을 뿐**이다. 이 구분을 지우면 다음
사람이 「314건짜리 수리」로 읽고 효과를 과대평가한다.

**작업량도 정확히 적는다 — 유도 로직은 신설이다.**

「순수 함수(`_ledger_gap_seed`)가 이미 있으니 발화 조건만 넓히면 된다」는 **틀렸다.** 그 함수의
채택 조건은 「창 안 체결이 전부 같은 방향 + reduce-only 하나도 없음」(`live_signal.py:405-407`)이라
상시 주입에서는 `since` 를 어떻게 잡아도 legs 가 빈다(세션 시작 → 진입+청산이 섞여
`inadmissible` / 직전 tick → 체결이 없어 `no_basis`). **Pine `trade_id` 단위로 진입/청산을 상쇄해
열린 trade 만 남기는 유도 함수를 새로 써야 한다.** 순수 함수라 단위 테스트로 전량 검증 가능하다.

**켜는 순서 — 계측이 먼저다.**

슬라이스 1 은 판정을 **계산만** 하고 기록한다(판정·발주 경로 무변경). ★단 **거래소 조회가
tick 당 1회 늘어 2회가 된다** — `_detect_position_divergence` 는 `engine_position`(= `run_live`
**결과**)이 필요해 뒤로 갈 수밖에 없고 veto 는 주입 **직전**에 판정해야 하므로, 두 조회는
**구조적으로 합칠 수 없다.** 「동작 변경 0」이라고 적으면 다음 사람이 이 비용을 못 본다.
거기서 ① 주입 가능 tick 수
② veto 발동률 ③ **veto 해소까지 tick 분포**(상한 계수의 직접 근거) ④ **`exchange_only` →
`direction` 발전율**(= 본 결정의 기대 효과 그 자체) ⑤ 유도 함수 `None` 비율을 잰 뒤 슬라이스 2 를
착수한다. ★**④가 0 이면 본 결정의 근거가 무너지므로 슬라이스 2 를 착수하지 않는다.**

**재현성 — 라이브는 더 이상 「전략+OHLCV」만으로 재현되지 않는다.**

백테스트 골든은 안 깨진다(`ledger_seed_legs` 를 채우는 곳은 `live_signal.py:2512` 하나뿐 —
★줄번호만 2026-08-04 에 갱신했다, 주장은 손대지 않았다. 검증 결과는 §슬라이스 1 후속 실측 참조).
다만 그 격리가 **관례로만** 유지되므로 회귀 테스트를 신설한다. 라이브 쪽은 주입 후 그 시점
원장이 있어야 재현되므로, `live_signal_states.last_strategy_state_report`(기존 jsonb,
**마이그레이션 0**)에 `ledger_seed{applied, vetoed, hold_ticks}` 를 영속한다.

**본 ADR 은 B 안(거래소 SSOT) 재제안을 차단한다** — 거부 근거는 취향이 아니라 코드에 적힌
동어반복 제약이다.

---

## 슬라이스 1 실측 — ④ 반증 · 슬라이스 2 미착수 (2026-08-04)

> ★위 §결과·§근거는 **고치지 않았다.** 예고와 실측을 나란히 둬야 다음 사람이 「예고가 어디까지
> 맞았고 어디서 틀렸는지」를 볼 수 있다. 본 절이 그 실측이다. 결정 자체는 **Accepted 유지** —
> 무너진 것은 결정이 아니라 **슬라이스 2 의 착수 근거**다.

### 판정 — 사전등록 V1 발동 (④ = 0 ⇒ 슬라이스 2 를 착수하지 않는다)

**1. 연역 상계 — 주입이 값을 넣는 tick ⊆ `exchange_only` tick**

- 주입 조건은 `strategy_state.py:357` 의 `if not legs or self.open_trades: return ()` ⇒ **엔진 flat 필수**
- 주입이 무의미하지 않으려면 원장 non-flat이고, `agree` 이므로 **거래소도 non-flat**
- `_classify_position_divergence`: 엔진 flat + 거래소 non-flat = **`exchange_only`**

**2. 그 모집단의 크기** (`.soak/snap-*.txt` 28벌 차분, 08-03 09:53Z → 08-04 02:56Z = **17.06h**.
★절대값 비교 금지 — counter 출생일은 `backend/.metrics` 최고참 파일 = 2026-07-28 12:19)

| counter                                                         | 시작 | 끝  | 차분   |
| --------------------------------------------------------------- | ---- | --- | ------ |
| `position_divergence{exchange_only}`                            | 20   | 21  | **+1** |
| `position_divergence{direction_transient}`                      | 15   | 23  | +8     |
| `signal_divergence{category=direction,stage=position}`(하드 킬) | 1    | 3   | **+2** |

★**결과 사건(하드 킬)이 이 창에서 2회 발화했다.** 「무사고라 못 쟀다」가 아니라 **발화한 사건의
상류를 읽을 수 있는** 창이다 — 판별력이 있다.

**3. 부검 — 하드 킬의 상류에 `exchange_only` 가 없다** (워커 로그 38h 전량 census:
`live_signal_position_divergence` 는 tick 마다 무조건 찍힌다. `direction` 10줄 · `exchange_only` 1줄)

| 세션       | 수명  | 사인                | 상류 `exchange_only` |
| ---------- | ----- | ------------------- | -------------------- |
| `04097fdc` | 65분  | position_divergence | **0건**              |
| `a201a47b` | 105분 | position_divergence | **0건**              |
| `4bf679af` | 163분 | user_stopped        | 0건 (킬 없음)        |
| `aa0c76b3` | 1분   | user_stopped        | 1건 ← 유일한 관측    |

그 유일한 1건(`08-03 17:59:47`)은 **세션 첫 tick** 이고 `engine=0.0 / exchange=-0.001`(먼지 잔여)다.
첫 tick 이라 **원장이 비어 넣을 것이 없고**, 원장 flat vs 거래소 non-flat 이므로 설계상 `disagree`
→ **veto(관망)** 로 떨어진다. **주입 대상이 아니다.**

**4. 최악 상계** — 로그 창 밖(07-28 12:19 ~ 08-03 09:53)에 `exchange_only` 20건 · 하드 킬 1건이
있다. 그 1건이 전부 `exchange_only` 상류를 가졌다고 최대로 가정해도 ④ ≤ **1/21 = 4.8%**.

**5. 왜 0 인가** — 사망 경로는 **반전**이다. 엔진이 롱→숏으로 뒤집는데 거래소가 못 따라오면 양쪽이
정반대가 된다(실측 `engine +0.0304 / exchange -0.03`). **반전은 tick 경계에서 flat 을 거치지 않는다.**
⇒ C 의 전제(엔진 flat)는 사망 경로에서 **구조적으로 밟히지 않는다.** §결과가 예고한 「주입과
`direction` 킬은 같은 tick 에 공존할 수 없다」의 한 단계 강한 버전이다 — 같은 tick 이 아니라
**같은 에피소드 어디에도** 없다. 보조 지표: `engine_only` 314 vs `exchange_only` 21 — 엔진은
거래소보다 **앞서는**(유령을 든다) 쪽으로 15배 치우쳐 있고, C 가 메우는 것은 그 반대 방향이다.

### ★더 큰 발견 — 유도 함수의 전제가 반전 전략에서 성립하지 않는다

과거 **29세션 전량**에 `derive_open_position` 을 프로덕션과 같은 호출로 재생했다(읽기 전용).

| outcome          | 세션 |                    |
| ---------------- | ---- | ------------------ |
| `open`           | 10   |                    |
| `duplicate_open` | 7    | ★**판정 불가**     |
| `no_fills`       | 6    |                    |
| `flat`           | 5    |                    |
| `foreign_fill`   | 1    | ★**판정 불가**     |
| **판정 불가 합** | 8/29 | **27.6%** (⑤ 실측) |

**뿌리 둘.** ① **`trade_id` 는 trade 인스턴스가 아니라 Pine 진입 규칙 이름이다** — 실측
`PivRevSE` **56 체결 / 19 세션**, `PivRevLE` 39/16. 한 세션 안에서 같은 규칙이 여러 번 발화하면
같은 id 가 반복된다. ② **반전은 `:close:` 키를 만들지 않는다** — 배(倍) 수량 진입 주문 하나로
나간다(실측: 숏 `0.03` 보유 중 롱 `0.06` 진입 = 청산 0.03 + 신규 0.03). ⇒ 「어느 진입을 어느
청산이 닫았는지 키만으로 이어붙일 수 있다」는 전제가 깨진다.

★★★**net 은 맞고 legs 는 틀리다 — 계측이 재는 것과 슬라이스 2 가 쓰는 것이 다르다.**
구현과 **독립된 오라클**(워커 로그가 그 시각 거래소에서 직접 읽은 `exchange_position`) 11건 대조:

| 판정       | 건수  |                                                                                                                                                        |
| ---------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **빗나감** | **0** | 유도 함수는 **틀린 답을 낸 적이 없다**                                                                                                                 |
| 적중       | 4     | ★**3건이 `legs=2`** 인데 거래소는 **단일 포지션**. ★나머지 1건은 **반전이 없던 세션**(체결 1건)이라 `legs=1` 로 정확했다 — **깨지는 것은 반전일 때다** |
| 판정 불가  | 7     | 전부 `duplicate_open`                                                                                                                                  |

예: 숏 0.03 + 롱 0.06 = net **+0.03** ✓(거래소와 일치) 이지만 실제는 **롱 0.03 하나**다.
슬라이스 1 계측은 **net** 으로 `agree` 를 판정하고 슬라이스 2 는 **legs** 를 주입하므로,
**계측이 초록이어도 주입될 값은 틀렸다.** 잘못된 legs 를 주입하면 진입가·청산 대상·`liq_price`·
pyramiding 캡이 전부 허구가 되고, 없는 leg 를 닫으려는 reduce-only 주문이 거래소에서 거절된다
(이 레포가 이미 기록한 `110017` 형태). ⇒ **④가 0 이 아니었더라도 현행 유도 함수로는 슬라이스 2 를
켤 수 없다.**

★**회고가 아니라 지금 발화 중이다** (`2026-08-04T03:34Z`, 소크 `bbea6da4` T0+40분 실측).
원장은 `03:04:11 sell 0.029 …:PivRevSE` · `03:27:12 buy 0.058 …:PivRevLE`(= 청산 0.029 + 신규 0.029)
두 줄뿐이고, 그 시각 tick 상태는:

```json
{
  "legs": 2,
  "outcome": "open",
  "decision": "agree",
  "ledger_net": "0.029",
  "exchange_net": "0.029"
}
```

**`agree` 이고 net 도 맞다. 그런데 거래소가 든 것은 롱 0.029 하나이고 유도는 leg 를 둘로 본다.**
그리고 다음 `PivRevSE` 진입이 나가는 순간 이 세션은 `duplicate_open` 으로 **판정 불가가 된다.**

★**설계 미덕 하나는 실증됐다** — 「모른다」(`legs=None`)와 「비었다」(`legs=()`)를 접지 않은
fail-closed 가 실제로 작동해 **오답 0/11** 이다. 문제는 정확성이 아니라 **판정 불가율**과
**legs 분해**다.

### 슬라이스 2 재개 조건 (셋 다 충족해야 한다)

1. **④가 0 이 아님을 다시 보인다** — `exchange_only` 가 하드 킬로 발전한 사례를 **최소 1건**
   구현과 독립된 증거로 제시한다(로그 부검 또는 재생 오라클).
2. **legs 분해가 반전을 견딘다** — `trade_id` 재사용과 배수량 반전 주문을 상쇄할 수 있는 유도
   규칙으로 교체하고, 위 오라클(거래소 실측 대조)에서 **net 뿐 아니라 legs 도** 맞힌다.
3. **판정 불가율이 실용 범위로 내려온다** — 현재 27.6%(세션) / 63.6%(발산 사건).

### 그리고 이번 실측이 답하지 **못한** 것

- ①②③(주입 가능 tick 수 · veto 발동률 · veto 해소 tick 분포)은 소크 창이 **19 tick** 뿐이라
  판별력이 없다(②는 1/19, ③은 `bucket="1"` 단 1건). **상한 계수는 여전히 근거가 없다.**
- ★**①의 라벨은 「주입 가능」을 세지 못한다** — `veto_total{decision="agree",engine_flat="true"}`
  는 원장도 거래소도 flat 인 무의미 tick 을 포함한다(실측 9건 전부 `no_fills` 구간). 실제로 값이
  들어가는 tick 은 **원장 non-flat + 엔진 flat + agree** 인데 그 교차는 현재 counter 로 못 센다.
- 회고 재생은 **같은 함수**를 쓰므로 함수의 정확성을 검증하지 않는다. 정확성을 잰 것은 위
  **오라클 11건**(외부 진실)뿐이다. 두 층을 합쳐 말하지 마라.

---

## 슬라이스 1 후속 실측 — veto 절반까지 꺼진다 (2026-08-04, 같은 날 후속 회차)

> 위 §슬라이스 1 실측과 마찬가지로 **§결과·§근거의 예고 문장은 고치지 않았다**(줄번호만 갱신).
> 본 절이 그 뒤에 잰 것이다. **결정은 여전히 Accepted** — 무너진 것은 C 의 **적용 범위**다.

### 사전등록 예측 — **적중**

「소크 `bbea6da4` 는 다음 `PivRevSE` 진입 체결에서 `duplicate_open` 으로 넘어간다」는 예측이
**그대로 맞았다.** 트리거는 **2번째 `PivRevSE` 체결**(`03:48:16`)이었고, 그 직후 tick 부터
`derive_total{duplicate_open}` 이 오르기 시작했다. ★첫 읽기(`03:49:29`)가 0 이었던 것은
**호스트 `/metrics` 지연**(상비 참조에 이미 기록된 함정)이고 `03:50:23` 에 1.0 으로 보였다.
⇒ **§슬라이스 1 실측의 기전 설명은 반증되지 않았다.**

### 신규 3건 — 판정 불가는 「비율」이 아니라 **흡수 상태**다

`_capture_ledger_shadow` 는 `since = sess.created_at`(`live_signal.py:471`)으로 **세션 전체**를
읽는다. ⇒ `trade_id` 가 한 번 반복되는 순간 그 뒤 **모든 tick 이 영구히 `legs=None`** 이다.

| 사실                                                           | 실측                                                                                                                       |
| -------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| `duplicate_open` = **흡수 상태**                               | 프로덕션 실증: `open` 44.0 에서 **완전 정지**, `duplicate_open` 36 연속                                                    |
| 라이브 시간 중 **어두운 비율**                                 | **19.0%** (601 / 3165 세션·분, n=29)                                                                                       |
| 실명까지 걸린 시간                                             | median **26.6분** (min 17.3 / max 60.5, n=8)                                                                               |
| ★**`position_divergence` 사망 2건이 모두 이미 어두운 뒤 사망** | `a201a47b` T0+17.3 dark → T0+104.9 사망 · `04097fdc` T0+26.1 dark → T0+65.0 사망                                           |
| 깨지는 조건 = **반전(`:close:` 키 없음)**                      | close 키 0 + 체결 ≥2 세션 **6/6 전건** dark. 916분 무실명 세션 `e1f6d84c` 는 `Long` 12 open / 12 close **짝 맞는 롱 전용** |

★재생 산식이 위 §⑤ 의 **27.6%(8/29)를 그대로 재현**했다 — 계측기 자체가 교차검증됐다.

### 판정 — ④=0 이 주입 절반을 막았다면, 이 3건은 **veto 절반까지** 막는다

§슬라이스 1 실측은 **주입**이 사망 경로에 닿지 않음을 보였다(④=0). 남은 절반인 **veto/관망**은
`net` 만 필요하고 net 은 오답 0/11 이라 **살아 있다고 볼 수 있었다.** 그러나:

1. **사망 시점에 유도 자체가 판정 불가였다** — 위 표. veto 를 계산할 입력이 없다.
2. **설령 계산됐어도 발화하지 않는다** — veto 는 「원장 vs 거래소」 대조인데 사망 경로는
   **원장 == 거래소이고 엔진만 거짓말**한다. `agree` 로 통과하고 `direction` 킬이 그대로 난다.
3. **방향이 반대다** — `engine_only` **314** vs `exchange_only` **21**. C 가 메우는 것은 21 쪽이다.

⇒ **C 는 예방 전용이며 사망 경로에 구조적으로 닿지 않는다.** 결정은 유지하되 **적용 범위를
그렇게 축소한다.** 사망 경로의 수리는 [ADR-023](./023-engine-state-ssot.md) 이 다룬다.

### 대안 D 기각이 순환이었다

> **D. 조정 연산 정의** — 엔진에 쓸 자리가 없으므로 결국 같은 주입점을 쓴다. **C 의 부분집합.**

「쓸 자리가 없다」는 **본 ADR §배경이 진단한 결함 그 자체**이지 넘을 수 없는 경계가 아니다.
같은 형태의 순환이 둘 더 있었다(백테스트 분기 우려 · 합성 주문 행 오염 우려) — 전문은
[ADR-023 §순환 기각 3건](./023-engine-state-ssot.md).

### §결과의 「회귀 테스트를 신설한다」는 **이행됐다**

`backend/tests/strategy/pine_v2/test_ledger_seed_isolation.py`(75줄, 테스트 3개). ★단서 둘:
**⑴ `origin/main` 에 없다**(PR #539 미머지 → main CI 는 아직 이 계약을 집행하지 않는다)
**⑵ `ledger_seed_legs` 단일 심볼만 지킨다**(`:31`) — `position_epoch`·`emit_from_bar_time` 은 무방비.
