# QuantBridge — Status

> **업데이트:** 2026-08-04
> **★[BL-591] 설계 확정 — 엔진 포지션 SSOT = 원장, 거래소는 거부권만.** 근거는
> [`ADR-022`](decisions/022-engine-position-ssot.md). 브랜치 `stage/engine-position-ssot`
> (`main` @ `70127fdd`) · 마이그레이션 **0** · FE **0**.
> ★★★**착수 전제가 5건 반증됐다** — [BL-591] 본문 숫자를 그대로 믿고 착수하면 안 된다.
> 특히 **「30건 Resolved」는 실제 16건**이고, **「발화 조건만 넓히면 된다」는 틀렸다**
> (`_ledger_gap_seed` 는 상시 주입에 **재사용 불가** — 유도 로직 신설이다).
> 정정 전문 = [BL-591] §실측 정정.
>
> ★**C 는 교정이 아니라 예방이다.** 주입은 「엔진이 완전히 비었을 때만」 작동하므로
> (`strategy_state.py:357`) 겨냥 대상은 `exchange_only` **21건**이지 `engine_only` **314건**이
> 아니다. **주입과 `direction` 킬은 같은 tick 에 공존할 수 없다** — 소크를 죽이는 그 발산을
> 고치는 게 아니라, 진입을 놓친 상태를 미리 메워 **거기까지 가지 않게** 하는 것이다.
>
> **최근 머지:** `stage/breach-rejection-recovery` → `main` (**PR #538** @ `70127fdd`, 2026-08-03).

---

## 다음 스프린트 — **engine-position-ssot** (설계 확정 → 계측 먼저)

> ★**이것이 다음 세션의 유일한 진입점이다.** 별도 킥오프 파일을 만들지 않는다.
> ★**`AGENTS.md` 는 읽지 마라 — 자동 로드된다.** `CONTEXT.md` 는 반대다(읽어야 들어온다).

### 확정된 설계 (사용자 결정 2026-08-04 · 근거 = ADR-022)

```
매 tick:
  ① 원장에서 포지션 유도            (값의 유일한 출처)
  ② 거래소 스냅샷 조회              (이미 매 tick 한다 — 게이트 비용 0)
  ③ ①≠② → veto                     (거래소는 "넣지 마"만 말한다)
       veto 아님 → 엔진이 flat 이면 주입
       veto      → 주입 ✖ + 발주 ✖ (관망) + 버팀 카운터 +1
                   상한 초과 시 오늘처럼 fail-closed 종료
  ④ 주입/veto/버팀 내역을 tick 상태(jsonb)에 영속
  ⑤ 주입 후 남는 발산은 현행대로 `direction` 만 킬
```

★**상한은 `interval` 비례**다(`max(절대 하한, interval × N)` 류). **tick 주기 = interval** 이라
「2 tick」이 1분봉이면 2분, 1시간봉이면 2시간이다 — **현행 `direction_transient` 1회 유예도 이미
interval 비례**였고 아무도 명시한 적이 없었다. ★**계수는 추정으로 찍지 않는다. 슬라이스 1 실측이 정한다.**

### 슬라이스 1 — 계측만 (판정·발주 경로 무변경) ★현재 작업

★**「동작 변경 0」은 정확하지 않다** — 거래소 조회가 tick 당 1회 늘어 **2회**가 된다.
`_detect_position_divergence` 는 `engine_position`(= `run_live` **결과**)이 필요해 뒤로 갈 수밖에
없고, veto 는 주입 **직전**에 판정해야 하므로 두 조회는 **구조적으로 합칠 수 없다.**
바뀐 것은 조회 횟수뿐이고 판정·발주 경로는 무변경이다.

- **1a 유도 함수 신설** — Pine `trade_id` 단위로 진입/청산을 상쇄해 열린 trade 만 남기는 순수 함수.
  `_ledger_gap_seed` 는 **재사용 불가**(같은 방향 + reduce-only 없음 조건 → 상시에선 항상 빔).
  ★**「빈 포지션」과 「모름」을 같은 값으로 접지 마라** — 접으면 flat 으로 오인해 주입이 폭주한다.
- **1b 계측 배선** — `run_live` **직전**(= 슬라이스 2 의 주입 판정 지점과 **같은 자리**)에서
  계산만 하고 기록. `_detect_position_divergence` 는 **무수정**.
  ★**계측 지점이 슬라이스 2 의 판정 지점과 다르면 여기서 잰 계수가 무의미해진다.**
- **재는 것** ① 주입 가능 tick 수 ② veto 발동률 ③ **veto 해소까지 tick 분포**(상한 계수의 직접
  근거) ④ ★**`exchange_only` → `direction` 발전율**(= C 의 기대 효과 그 자체) ⑤ 유도 함수 `None` 비율.

### 사전등록

- **V1** ★**④가 0 이면 C 의 근거가 무너지므로 슬라이스 2 를 착수하지 않는다.**
  「몇 시간 무사고」를 증거로 쓰지 않는다.
- **V2** 1a 가 **변조에 red** 임을 보인다(상쇄 로직 · 절단 감지 · `None` 대 flat 구분 각각).
  ★사전등록 변이가 **판별력 0 이었던 사례가 4회 연속** 있었다 — 실행 전에 「이 변조로 정말
  red 가 되나」부터 확인해라.
- **V3** 매 관측에서 `soak-observe.sh` exit 0. `UNKNOWN` 이 나오면 그것부터 닫는다.

### 실행 순서 — ★거래소 flat 정리는 도구를 만든 뒤에 한다

| #   | 단계                                     | `src` 편집 | 소크     |
| --- | ---------------------------------------- | ---------- | -------- |
| 1   | 문서 3종 + [BL-592]/[BL-593] 등재        | ✖          | 유지     |
| 2   | 소크 세션 종료(deactivate 만)            | ✖          | **종료** |
| 3   | [BL-593] 운영자 청산 도구 원장 기록      | ✔          | 꺼짐     |
| 4   | 슬라이스 1 (1a + 1b)                     | ✔          | 꺼짐     |
| 5   | 게이트 + **변이**(소크 꺼진 지금만 가능) | ✔          | 꺼짐     |
| 6   | 거래소 flat 정리 — ③의 도구로            | ✖          | 꺼짐     |
| 7   | 소크 재시작                              | ✖          | **시작** |

★★★**6번이 3번 뒤에 오는 것이 핵심이다.** provider 를 직접 불러 flat 을 만들면 그 순간
**원장 밖 청산 12건에 13번째를 손수 추가**하게 된다([BL-593]). ③을 먼저 하면 같은 정리가
**원장에 남고** 그 자리에서 실전 검증된다 — `external_manual` 이 **안 늘고** `ours` 가 늘면 성립.

★**재시작 뒤에는 `backend/src` 편집 금지가 복귀**한다(워커가 `watchfiles` 로 물어 중간 상태를
적재하면 세션이 죽는다). **변이 테스트도 그때부터 다시 금지**다.

### 비목표

슬라이스 2(실제 주입·관망) — **계수 근거가 없으므로 착수 금지.**
[BL-580] 잔여 96곳 · [BL-578] 취소 사유 영속화 · [BL-389]/[BL-466] 파산 모델 · [BL-462] · mainnet.
★**[BL-591] 이 열려 있는 동안 새 발산 증상 BL 은 거기에 링크**해라 — 증상 수리를 반복하지 마라.

---

## ⛔ 종료 — **breach-rejection-recovery** (완료 · 참고자료)

> ★**진입점은 여기가 아니라 위 블록이다.**

### [BL-590] ✅ **완료** — 소크를 105분에 끊은 거절

- **수리** — `trigger_breached` 거절의 CAS 승자가 commit 뒤 `live_signal.recover_breached_entry`
  를 예약한다. 복구는 계획 시점 전환과 **같은 순서**의 가드를 통과한다(interval · 이중 진입
  억제창 · **만료 1 interval** · 기준가 재조회 · 돌파 재확인 · 캡). ★**fail-closed 킬 경로는
  무수정** — 복구가 실패하면 오늘과 똑같이 안전하게 죽는다.
- **크기 괄호를 단일 숫자로 줄이지 마라** — PR #493 이후 거절 **4건 / 19.09 세션시간**.
  「거절 직후 bar 가 트리거를 찍었다」는 **4/4**, 치명(`direction`)은 거래소가 반대 포지션을
  들고 있을 때만이라 **2/4**, 오늘의 반전 아키텍처를 공유하는 것은 **1건뿐**이다.
- **남긴 것(의도적)** — ① commit↔enqueue 사이 크래시 시 복구 유실(실패 모드가 오늘과 같다)
  ② 서로 다른 `trade_id` 두 복구의 동시 실행 경쟁. 둘 다 dev-log §8 에 근거를 적었다.

### 직전 회차 — soak-divergence-root + trust-envelope (참고자료)

> ★**진입점은 여기가 아니라 위 블록이다.** 아래는 이번 회차가 무엇을 어떻게 닫았는지의 기록이다.

★**순서가 결과를 만들었다.** A(실주행 수리) 를 먼저, B(오프라인 계측) 를 창이 도는 동안 했다.
B 를 먼저 하면 계측 부채는 무한하고 오프라인 검증이 가능해서 항상 그쪽만 고르게 되고, 그래서
실주행 시계가 며칠씩 멈춰 있었다(2026-08-03 방향 재판정). **다음 회차도 같은 순서를 지켜라.**

★**작업 위치 = 메인 체크아웃.** celery 경유 검증은 워크트리에서 구조적으로 불가능하다
(워커가 메인 `src` 를 mount). ★워크트리를 쓴다면 python 을 **3.12 로 핀해라** — 이제
`backend/.python-version` 이 그걸 강제한다([BL-587]).

### A. [BL-589] ✅ **완료** — 소크를 65분에 끊은 발산

수리·검증·재가동 끝. 상세는 dev-log.

- **수리** — `conditional_entry_planner.py` 의 배제 술어를 `matching_actual`(대기 주문 존재)에서
  `resting_could_have_fired`(대기 주문 **자신의** 트리거가 같은 기준가로 뚫렸는가)로 좁혔다.
  부수로 `as_market` 을 등가 비교에 넣었다 — 굵은 눈금에서 시장가 의도가 대기 stop 과 「일치」로
  접혀 **아무것도 안 나가는** 구멍이 있었고, 이 구멍은 술어를 좁히기 전에는 도달 불가였다.
- **검증** — 사전등록 M1 이 수리 전 **red** · 후 green, 대조 M2(기존 테스트 무수정)·M3 은 전후
  불변 green. 재생 오라클(원장 + Bybit 1분봉)이 **취소 172건 중 「발화 불가 + 대체 없음」 29건**을
  집어냈고 그 안에 사망 유발 주문이 있다. 로그 남은 breach 2건은 **정반대 두 판정 2/2 적중**.
- **크기 괄호를 단일 숫자로 줄이지 마라** — 확정 **1건** · `breach_with_resting` **11건** ·
  드롭 추정 **29건**. 취소 사유가 원장에 없어 11건 전수 재판정은 **불가능**했다([BL-578] 계열).

**★A3 재가동 완료** — 거래소 `FLAT=YES` 확인 후 서비스 계층으로 등재(HTTP 는 Clerk `azp` 때문에
불가, 손 INSERT 는 `equity_baseline_usdt` 누락으로 첫 tick 사망). 세션 `a201a47b` ·
T0 `2026-08-03T14:09:38Z` · equity baseline `190419.28986309`. 앵커는 `.soak/session`.

**T0+1.03h 실측 (`15:11:23Z`) — 생존.** 주문 9건(체결 5 / 취소 3 / submitted 1) · 평가 정상 ·
H8 위반 0 · `/metrics` 12836([BL-581] Trigger 20000 미달).

| counter               | T0  | +1h    | 뜻                                                     |
| --------------------- | --- | ------ | ------------------------------------------------------ |
| `breach_with_resting` | 11  | **11** | ★**수리한 갈래는 프로덕션에서 아직 한 번도 안 밟혔다** |
| `market_converted`    | 10  | 11     | **기존**(대기 주문 없음) 경로로 1회 — 내 수리와 무관   |
| `direction_transient` | 17  | **19** | 2회 발생, **둘 다 자기해소**                           |
| hard `direction`      | —   | **0**  | 2회 연속 정책이 실제로 걸러냈다                        |

★★★**「65분을 넘겼다」를 증거로 쓰지 마라.** 사망 기저율이 25세션 중 1건이라 무사고는 기본값이고,
`breach_with_resting` 은 8일에 11건이라 **1시간 기대값 ≈0.06건**이다 — 안 밟히는 게 정상이다.
⇒ **이 수리를 지금 지지하는 것은 소크가 아니라 오프라인 사전등록 변이와 재생 오라클이다.**
프로덕션 확인은 `breach_with_resting` 이 **다음에 증가할 때** 그 시점의 `market_converted` 동시
증가로 한다. 그때까지는 「미검증」이라고 적어라.

★**자기해소 skew 가 사망 사례와 갈리는 지점을 그대로 보여줬다** — `14:20:34` engine `+0.03007` vs
exchange `−0.03`. 원장상 거래소가 `14:20:20` 에 뒤집힌 **14초 뒤**라 엔진이 아직 이전 봉 종가에
있었을 뿐이고, 다음 평가에서 해소됐다. **사망 사례는 시뮬이 잡은 진입이 거래소에 영영 없어서
2회차에도 안 풀렸다.**

### B. 정답지 envelope ✅ **완료** ([BL-587] · [BL-585] · [BL-588] 전부 Resolved)

- **B1+B2** `backend/.python-version` = 3.12 핀(원인 차단) **+** `test_envelope_*` 3건(탐지기).
  세 변조에 각각 **자기 assert 만** red 로 판별력 확인, 복원은 sha256 대조.
- **B3** 안 도는 스키마를 **지웠다.** 중복이 아니었던 유일한 검사(`corpora` 8벌)는 **키 집합**
  대조로 강화해 테스트로 이관.
- **B4** 5→7 이 아니라 **목록을 `_corpus.py` 하나로 합쳤다.** ★`_MUTATION_CORPORA` 의 5벌이
  **정확히 위험조정지표가 축퇴한 5벌**이라(sharpe 전부 0 · sortino/calmar 전부 null) 그 3지표의
  산술 회귀가 구조적으로 감지 불가였다. nightly **183s → 218s (+19%)**, 감지 결과 **불변** —
  ★기존 8변이는 `trades_digest` 로 이미 잡히므로 증가가 없는 게 정상이다. 7벌의 값은 감지율이
  아니라 **앞으로의 지표-산술 회귀를 위한 채널의 존재**다. 이 구분을 지우면 「+19% 주고 얻은 게
  없다」고 되돌리게 된다.

### 사전등록 (착수 전에 문턱을 적어라)

- **A-V1** ✅ **충족** — 출처는 `run_live` 시뮬(`live_signal.py:520-534` 인용). 되돌리는 경로는 없다.
- **A-V2** ✅ **충족** — M1 수리 전 red / 후 green, 대조 2건 전후 불변 green.
  ★**두 변경을 한 번에 넣지 않은 것이 결정적이었다** — 술어만 먼저 넣으니 M1b 만 red 로 남아
  눈금 붕괴 구멍이 **독립 증거**를 얻었다. 묶었으면 서로의 증거를 가렸다.
- **A-V3** ❌ **폐기** — 「65분 초과」는 기저율(25세션 중 1건)상 판별력 0. 대체 = 재생 오라클이
  0 이 아닌 모집단(**29건**, 사망 유발 주문 포함)을 냈다.
- **A-V4** ✅ **기록함** (판정이 아니라 관측 — 절대값 금지, 출생일이 다르다). T0→+1h 차분:
  `market_converted` **+1**(기존 무-resting 경로) · `breach_with_resting` **불변 11** ·
  `direction_transient` **+2**(둘 다 자기해소) · hard `direction` **0**.
  ⇒ **수리한 갈래는 프로덕션 미검증**이다(1시간 기대값 ≈0.06건이라 정상).
- **B-V1** ✅ **충족** — 세 변조에 각각 **자기 assert 만** red(매번 1 failed / 2 passed, 교차 오염 0).
  복원은 sha256 대조.
- **B-V2** ⚠️ **「유지」로 충족 — 「증가」는 아니다.** nightly 183s → 218s(+19%), 감지
  7 passed + 1 xpassed **불변**. 기존 8변이는 전부 `trades_digest` 로 이미 잡히므로 증가가 없는 게
  정상이다. ★**7벌의 값은 감지율이 아니라 채널의 존재**다 — `_MUTATION_CORPORA` 의 5벌이 정확히
  위험조정지표가 축퇴한 5벌이라(sharpe 전부 0 · sortino/calmar 전부 null) 그 3지표의 **산술
  회귀가 구조적으로 감지 불가**였다. 이 구분을 안 적으면 다음 사람이 「+19% 주고 얻은 게 없다」고
  되돌린다.

★사전등록 변이가 **판별력 0 이었던 사례가 네 회차 연속** 있었다(직전 회차 V3 포함 — 백로그가
지목한 표적이 **이미 커버돼 있었다**). **「테스트가 green」은 증거가 아니다. 「이 변조에 red 다」가
증거다.**

### 이번 회차 인계 (2026-08-03 soak-divergence-root)

- **소크가 돌고 있다** — 세션 `a201a47b` · T0 `2026-08-03T14:09:38Z`. 앵커는 `.soak/session`
  (이미 새 세션을 가리킨다). ★재가동 직전 거래소는 `FLAT=YES` 였다.
- 2026-08-03 오전에 이전 세션을 flat 으로 정리한 건은 ★**앱 원장을 거치지 않았다** —
  `ClosePositionService` 가 HTTP 경로에만 조립돼 있어 provider 를 직접 호출했다. 그 청산에 대응하는
  `trading.orders` 행이 **없다.** 과거 원장을 셀 때 이 구멍을 빼먹지 마라.
- 게이트 baseline (본 회차 실측): BE **3914 passed / 46 skipped** · ruff 0 · mypy 0 (214) ·
  FE **1242** (205 파일) · bl-audit active **151**.
  nightly `--run-mutations` **218s**(코퍼스 7벌 — 5벌이던 시절은 183s).
- ★**`ruff format --check` 는 게이트가 아니다** — 레포 695 파일 중 **393 파일이 미정렬**이고,
  집행자는 `lint-staged`(스테이징된 파일만)다. 커밋할 파일이 이미 미정렬이면 훅이 무관한 줄까지
  건드리니 **미리 적용해 diff 에 드러내라.**
- 재생 오라클 스크립트는 scratchpad 에만 있다(레포 미등재). 다시 필요하면 dev-log §4 의 방법을
  보라 — 원장 + Bybit 공개 1분봉만 쓰므로 인증이 필요 없다.
- ★**`ruff format --check` 는 게이트가 아니다** — 레포 695 파일 중 **393 파일이 미정렬**이고,
  집행자는 `lint-staged`(스테이징된 파일만)다. 커밋할 파일이 이미 미정렬이면 훅이 무관한 줄까지
  건드리니 **미리 적용해 diff 에 드러내라.**
- 재생 오라클 스크립트는 scratchpad 에만 있다(레포 미등재). 다시 필요하면 dev-log §4 의 방법을 보라 —
  원장 + Bybit 공개 1분봉만 쓰므로 인증이 필요 없다.

---

## 📌 소크 운영 상비 참조 (창이 도는 동안 계속 유효)

> 아래는 특정 회차가 아니라 **소크를 굴릴 때마다 다시 밟는 함정**들이다. 회차별 숫자는
> dev-log 로 갔다 — 여기에 낡은 T0/baseline 을 남겨두면 다음 사람이 죽은 세션을 현행으로 읽는다
> (2026-08-03 실측 사고: 이 절이 이미 죽은 세션의 창 종료 시각을 가리키고 있었다).
> ★**`AGENTS.md` 는 읽지 마라 — 자동 로드된다.** `CONTEXT.md`·`.ai/rules/*.md` 는 반대다(읽어야 들어온다).

### 판정 지표가 그 창에서 **발화 가능한지 먼저 계산해라**

두 회차 연속 같은 계산이 필요했다. 2026-08-03 오전 `close_position_flat` 은 회차마다 고쳐져
**07-28 15건 → 07-29 3건 → 07-30 1건 → 07-31 0건** 으로 감소 중이었고 30분 기대값이 ≈**0.02건**
이었다 — 기다려서는 못 잰다. 같은 날 오후 `position_divergence` 사망은 **전 이력 25세션 중 1건**
이라 「N분 무사고」가 아무 증거도 아니었다.

⇒ **발화 안 하면 창을 늘리는 게 아니라 (a) 발화 조건을 만들거나 (b) 구현과 독립된 오라클로 과거
원장을 재생한다.** 둘 다 실제로 통했다(전자 = H8 유도 주입, 후자 = BL-589 재생 오라클 29건).

### 증명한 것과 못 한 것을 **합쳐 말하지 마라**

프로덕션 유도는 분기의 **도달·종결**만 증명한다. 계측 실패 봉쇄 같은 것은 **오프라인 결정론
테스트**가 증명하며, 프로덕션에서 유도하려면 multiproc 디렉터리를 망가뜨려야 해서 소크 중 금지다.
층을 나눠 표로 적어라 — 한 줄로 합치면 다음 사람이 「검증 완료」로 읽는다.

### ★착수 전 반드시 읽을 것

1. ★★★**데스크 회차가 반증하는 것은 「내가 적은 산문」이고, 소크가 반증하는 것은 「코드가
   실제로 하는 일」이다.** 재가동 직전 5회차는 전자만 했다. 계측 부채는 무한(96곳)하고
   오프라인에서 검증 가능하지만 소크는 느리고 위험하다 — 그래서 **이 루프는 자기 지속된다.**
2. ★★**`roadmap.md` 가 2026-07-26 에 스스로 세운 규칙** — 「이후 스프린트는 **전부 실주행
   dogfood 를 포함**한다」.
3. ★★**소크 전후로 거래소를 flat 으로 맞춰라.** 세션 `DELETE` 204 는 **아무것도 flat 하지
   않는다**(0.03 포지션 + 조건부 1건 잔존 전례). T0 직전엔 `FLAT=YES` 확인했다.
4. ★★**호스트 `/metrics` 는 워커 증가를 몇 초 늦게 비춘다**(T0 실측 — 호스트 14, 같은 시각
   컨테이너 15). **이벤트 직후 읽기로 판정하지 마라.** 하루 1회 관측엔 영향 없다.
5. ★**`idle` 은 완료가 아니다** · **Clerk JWT 는 60초** · **`:3000` 은 다른 앱(Kairos)** ·
   API 는 `:8100`, DB 는 `:5433`(격리 스택).
6. ★★**게이트를 파이프에 넣지 마라** · **`cd backend && set -a; . ./.env.local` 금지**(아래 참조).
7. ★**세션 등재는 HTTP 로 헤드리스 불가**(Clerk 가 `azp` 를 요구). 서비스 계층 직접 호출이
   유일한 길이다(`backend/scripts/seed_dogfood.py:11-19` 선례). **손 INSERT 는 금지** —
   `equity_baseline_usdt` 를 건너뛰어 첫 tick 에 자동 비활성화된다.

### 현행 소크 눈금 (숫자는 「이번 회차 인계」에 있다)

★**counter 절대값을 비교하지 마라 — 출생일이 다르다.** 원본 스냅샷은 `.soak/snap-*.txt` 에 있고
**차분은 거기서 뜬다**(`soak-observe.sh` §4 가 자동으로 한다). 상시 확인 대상:
`qb_metrics_mutation_failed_total`([BL-580] Trigger, 아직 실측 0) ·
`/metrics` 파일 수([BL-581] Trigger 20000) ·
`qb_live_conditional_guard_total{breach_with_resting}` vs `{market_converted}`([BL-589] 수리 관측축) ·
`qb_live_conditional_guard_total{recovery_placed}`([BL-590] 수리 관측축 — 증가하면 그 시점
원장에 `condmkt` 주문이 짝으로 있는지 확인해라. `recovery_expired` 가 증가하면 **브로커 적체**다) ·
마이그레이션 head `20260801_0001`.

> ★★**`cd backend && set -a; . ./.env.local; set +a` 를 쓰지 마라.** 이미 `backend` 에 있으면
> `cd` 가 실패해 **`set -a` 만 건너뛰고** 나머지는 `;` 로 계속 실행된다 — env 가 export 되지
> 않은 채 pytest 가 5432 로 붙어 `InvalidPasswordError` **대량 거짓 red**.
> **`QB=…; set -a; . $QB/backend/.env.local; set +a; cd $QB/backend`** 로 써라.
> ★**브랜치 접두사는 `stage/`** · `QB_PRE_PUSH_BYPASS=1` 금지.
> ★**pre-commit 이 `ruff format`·`prettier --write` 를 돌린다** — **커밋 후 게이트를 다시 재라**.
> ★**표적 변이는 CONTROL 이 직접 집행**(`git checkout` 금지, sha256 복원 대조). 치환 문자열이
> 다른 함수와 겹치는지 **먼저 세라**.

### 보류 — [BL-580] 잔여 96곳 (P2, 재개 조건 명시)

방법은 검증됐다(주입 판정 **42/42 전건 유해**). 다만 **Trigger 가 실측 0** 이라 P0 보다 뒤다.
재개하면 다음 단위는 **`_reconcile_conditional_entries` 12곳** — 그 함수의 바깥 `except` 가
fail-open(예외를 `stage="reconcile"` 로 계상하고 정상과 똑같이 `None` 반환)이라 **H8 조건이
함수 전체 규모로** 있다. 그 외 잔여: `_evaluate_session_inner` 21 ·
`_async_sweep_conditional_entries` 4 · `_async_dispatch_event` **4(판정 보류 — 손대지 마라)** ·
`_async_evaluate_all` 2 · `_async_evaluate_session` 2 · `_async_dispatch_pending` 1.
★**판정 라벨은 누적 8종** — H1~H7 + **H8**(fail-open `except` 가 삼켜 거절이 집행으로 뒤집힌다).
★**도달 경로를 못 적으면 「판정 보류」다 — 하네스를 만들지 마라.**

## 완료 이력

- 직전 회차 — [`breach-rejection-recovery`](dev-log/2026-08-03-breach-rejection-recovery.md)
  ([BL-590] **Resolved**. ★★★**가드가 뚫린 게 아니라 거절 뒤 복구가 없었다** — 계획기는
  발주 시각에 옳았고(카운터 차분이 연역 증명) 거래소가 2.1초 뒤 자기 시각으로 거절했다.
  ★★**이 클래스는 `110093` 단독이 아니다** — 거울 코드 `110092` 가 원장 4건 중 2건.
  ★★**격리 실행이 거짓말했다** — 두 파일만 돌리면 24 passed 인데 전체 스위트는 8 failed
  (내 fixture 가 시각을 **모듈 import 시점**에 고정). ★★**두 안전한 것이 합쳐져 결함이 됐다**
  — codex 가 「flake 아님」으로 판정한 값이 내가 만료 가드를 넣으면서 load-bearing 이 됐다.
  ★변이 **8/8** · 유도 주입으로 프로덕션 발화 확인)
- 그 앞 — [`soak-divergence-root`](dev-log/2026-08-03-soak-divergence-root.md)
  ([BL-589] **Resolved**. ★★★**엔진은 취소를 못 본 게 아니라 주문을 아예 모른다** — 포지션 출처가
  `run_live` 시뮬이라 「되돌리는 경로」가 애초에 없다. 뿌리는 계획기가 「대기 주문이 있다」만으로
  시장가 전환을 껐다는 것이고 **그 주문은 발화 불가**였다. ★★**한 번에 둘을 고치면 서로의 증거를
  가린다** — 술어만 먼저 넣으니 눈금 붕괴 구멍이 독립 red 로 남았다. ★★**boolean 판정을 피한
  대체 술어도 검증 대상**(`deactivated_reason` 이 25건 중 12건에서 거짓말))
- 그 앞 — [`metric-guard-residual-sweep`](dev-log/2026-08-03-metric-guard-residual-sweep.md)
  (발주 outbox **12곳** 판정 — **수리함 8 · 판정 보류 4**, census 104→96.
  ★★★**같은 함수·같은 metric·전부 「commit 뒤」인데 한 자리만 fail-open `try` 안**이라 계측
  실패가 **거절을 집행으로 뒤집었다** — 거래소가 flat 이라 거부한 청산에 실주문이 나갔다(신규
  라벨 **H8**). ★변이 M4 가 코드가 아니라 **오라클 구멍**을 드러냄(1578건 판별력 0) → 5종으로
  확장. **BL-584 현재 코퍼스 도달 불가 확정**)
- 그 앞 — [`metric-guard-residual-close`](dev-log/2026-08-03-metric-guard-residual-close.md)
  (BL-580 잔여 **25곳** 판정 — **수리함 23 · 판정 보류 2**, census 129→104.
  ★**산문 2줄이 25곳을 잘못 뺐다** — 「blast radius 0」은 10/10 이 도메인 예외 대신 OSError 를
  탈출시켰고, 「already_synced 수렴」은 7곳 중 1곳만 성립. ★**반쪽 수리는 사이트 주입 29건을
  전부 통과**한다(변이 M5). 신규 **BL-584**)
- 그 앞 — [`gate-trustworthiness`](dev-log/2026-08-03-gate-trustworthiness.md)
  (「전부 통과」를 증거로 만든다. ★**순서는 랜덤이 아니었다** — `pytest-randomly` 미설치로
  `-p no:randomly` 는 no-op, 흔들린 것은 **수집 집합**이다. 뿌리 = 정의 모듈 패치 창의 첫 적재가
  가짜를 **모듈 전역으로 영구 복사**. 오염원 4곳(전역 8개) 처분 + 상시 가드. **BL-583 Resolved**)
- 그 앞 — [`metric-guard-residual`](dev-log/2026-08-03-metric-guard-residual.md)
  (「감쌀 필요 없다」의 근거를 고장 주입으로 재판정 — 명시 4곳 **전건 반증**, 12곳 수리 ·
  census 141→129. **BL-582 「7종 도달 불가」→5종**. 신규 **BL-583** = 스위트 순서 의존)
- 그 앞 — [`metric-guard-parity`](dev-log/2026-08-02-metric-guard-parity.md)
  (계측 실패가 성공한 발주를 실패로 기록하고 **주문을 하나 더 냈다**. 가드 18곳 · census 159→141)
- 그 앞 — [`context-budget-repair`](dev-log/2026-08-02-context-budget-repair.md)
  (문서·계측만. `INDEX.md` **−92.3%** · 자동 로드 고정비 **−42.2%** · 줄길이 게이트 신설.
  ★**착수 전제 3건 반증** — `CONTEXT.md`·`.ai/rules` 는 자동 로드가 아니다)
- 그 앞 — [`canonical-measurement-surface`](dev-log/2026-08-02-canonical-measurement-surface.md)
- 그 앞 — [`divergence-label-split`](dev-log/2026-08-02-divergence-label-split.md)
- 이번 주 완료 스프린트와 이전 회고 — [`dev-log/INDEX.md`](dev-log/INDEX.md)
- 2026-07-26 이전 status 원문 — [`archive/status-history.md`](archive/status-history.md)
- 열린 BL의 현재 상태 — [`backlog.md`](backlog.md) (`scripts/bl-audit.sh`가 정본)
