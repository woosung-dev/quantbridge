# QuantBridge — Status

> **업데이트:** 2026-08-01
> **활성 Sprint:** 없음. 다음 작업은 아래 「다음 스프린트」 블록만 읽는다.
> **준비 브랜치:** `stage/entry-completeness-rejudgement` — PR 준비 중
> **최근 머지:** `stage/silent-surface-honesty` → `main@f631f1c7` (PR #517, 2026-08-01)

---

## 🎯 다음 스프린트 — **divergence-label-split** (세기 전에 가르고, 판정식을 집행 가능하게 만든다)

> ★**이것이 다음 세션의 유일한 진입점이다.** 별도 킥오프 파일을 만들지 않는다.
> 시작 방법: **"다음 스프린트 진행해줘"**. `CONTEXT.md` + `AGENTS.md` + 본 파일을 읽고 시작한다.

**한 줄.** 직전 회차가 최대 리스크였던 P1 두 건을 재판정해 닫았다(BL-536 Resolved · BL-522 P1→P2).
**머니-패스 P1 은 0 이 됐다.** 그런데 그 과정에서 **같은 병으로 세 번 당했다는 것**이 드러났다 —
**크기 근거가 반증돼도 그 반증이 전파되지 않는다.** 그게 이번 회차의 대상이다.

### 왜 이것이 최대 리스크인가 (근거는 `roadmap.md:37` 블록)

| #   | 반증된 숫자                                               | 전파 실패                                                                                   |
| --- | --------------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| 1   | C2 `deferred_market_inflight` = 「청산 tick 수」(PR #511) | BL-536 섹션에만 기록 → **BL-522 본문은 3개월 가까이 「시간당 14회」 유지**                  |
| 2   | C3 = 청산측 `qty_step` 절삭 아티팩트 (PR #509)            | **PR #511 의 「축소」 철회가 07-29 표로 되돌리며 이 반증까지 되감았다**                     |
| 3   | 유실률 `2/(10+2)` 를 `2/12` 로 압축                       | 6행 위 `C2 … 12` 와 혼동 → **「두 근거가 모두 C2 의존」이라는 거짓 전제**가 3개 문서로 복제 |

### 첫 step

1. **[BL-576] 라벨을 가른다** — `live_conditional_reconcile_divergence` 한 이름이 **발화 8곳 ·
   payload 3종**을 덮는다(`live_signal.py:1086·1181·1196·1250·1455·1481·1511·1647`).
   `:1250` 은 `**divergence` 를 splat 해 **키 집합이 코드에 고정돼 있지도 않다.**
   ★**세기 전에 가르는 것이 순서다** — BL-560 도 BL-536 도 라벨을 가른 뒤에야 크기가 나왔다.
2. **사전등록 문턱을 집행 가능하게 만든다** (직전 회차 Evaluator 감사가 결함 3건을 확정했다):
   - **A2 가 공허하다** — 「C2 **제외** 유실률 0」인데 C2 는 애초에 유실률 분모에 없다 ⇒ **절대 발화 불가.**
     → **A2′ = 층위1 유실률 0 그리고 채널 합 0** 처럼 다시 써라.
   - **A5 에 숫자가 없다** — 「표본이 문턱 미달이면」이라 쓰고 문턱 미기재. A3 이 **N=1 에서 자동 100%**.
     → 제안: **판정 종결 시도 < 30 이거나 채널 합 < 5 이면 「판정 불가」.**
   - **A4 는 AND 인데 과거 기록 3곳이 OR** — [사전등록 감사](dev-log/2026-08-01-entry-completeness-rejudgement-prereg-audit.md)에서 대조해라.
3. **[BL-574]** `LIMIT 100` 이 세션 필터보다 앞서 걸린다 — **BL-566 의 20/9 분해를 직접 건드린다.**
   ★**아직 실측되지 않았다** — `SELECT count(*)` 한 줄로 분포부터 봐라.
4. **[BL-577]** 팬텀 가드 — (a) 짓기 / (b) 지우기가 **전략 선택이라 사람이 정한다.** 물어라.

### ★착수 전 반드시 읽을 것

1. ★★**「기록된 반증」을 신뢰 대상이 아니라 대조 대상으로 써라.** 직전 회차에 `metrics.py:637-650`
   이 「이 counter 를 진입 부분체결에 쓰지 마라」고 **이미** 적어 뒀는데도 BL-536 채널 표는
   그 counter 로 센 7 을 들고 있었다.
2. ★**판별력 0인 창에서 통과한 검증을 믿지 마라.** A4 의 AND/OR 드리프트가 07-29 에 안 걸린 이유는
   **그날 두 근거가 둘 다 참이라 AND 와 OR 이 같은 답을 냈기 때문**이다.
3. ★**[BL-565] 는 여전히 막혀 있다** — `bracket_attached` 0%. 전략을 바꾸지 않는 한 손대지 마라.
4. ★**[BL-578]** 은 크기를 재고 **일부러 보류**한 항목이다. 되살리려면 Trigger(하루 3건)를 먼저 확인해라.

### 사전등록 판정 문턱 (착수 전 확정할 것)

★**직전 회차가 이 표의 결함 3건을 확정했다. 그대로 베끼지 말고 위 첫 step #2 를 먼저 반영해라.**

| ID  | 판정        | 조건                                                                           |
| --- | ----------- | ------------------------------------------------------------------------------ |
| B1  | 계측기 선행 | 8 발화 지점 각각이 **어느 payload 모양**인지 코드 대조로 먼저 확정             |
| B2  | 가름 성공   | 새 라벨로 **같은 창을 다시 세었을 때** 사건별 합이 기존 총량과 일치            |
| B3  | 판별력      | 라벨을 되돌린 변이에서 회귀 테스트가 **반드시 실패**                           |
| B4  | 판정 불가   | 표본 문턱 미달이면 **「판정 불가」라고 쓴다** — ★문턱을 **숫자로** 먼저 박아라 |

### 하지 않는 것

**BL-565**(구조적 측정 불가) · **BL-553 PbR 재시도** · **BL-578 수리**(크기 근거로 보류 — Trigger 확인 전) ·
**C1 시장가 전환**(머니-패스 변경, 1건/2일에 그 위험을 지지 않는다).

### baseline (2026-08-01 실측 — `stage/entry-completeness-rejudgement` 착수 시점, 문서값과 **7/7 일치**)

**BE 3792 passed / 46 skipped**(착수 3749 대비 **+43**) · **FE 1237**(205 파일, **+0** — FE diff 0) ·
ruff clean · mypy **214** clean ·
마이그레이션 head **`20260801_0001`**(이번 회차 **+0**) ·
`scripts/bl-audit.sh` **exit 0**(active **150** / 전체 **235**) · `scripts/bl-audit-test.sh` **5/5**.

★**이 숫자도 대조 대상이다. 첫 step 에서 지금 HEAD 로 다시 재라.**

> ★★**계측기가 0 을 내면 판별력을 먼저 증명해라.** 직전 회차의 C4 판정기는 취소분 68건 전건에
> 0 을 냈는데, **체결분에 같은 판정기를 걸어 35/37 을 잡는 것**을 보이고 나서야 그 0 이 의미를 가졌다.
> 그리고 그 대조가 **내 판정 규칙 하나(엄격 규칙)를 폐기시켰다** — 체결분을 1/37 밖에 못 잡았다.
> ★**`psql -c` 에 세미콜론 여러 개 = 암묵적 단일 트랜잭션.** 문장마다 따로 쳐라.
> ★**게이트를 파이프에 넣지 마라** — exit code 가 `tail` 의 것이 된다.
> ★**`herdr agent prompt` 는 붙여넣기만 한다** — `send-keys enter` 필수.
> ★**리다이렉트 대상 디렉터리가 없으면 명령이 먼저 죽는다** — 직전 회차에 백그라운드 pytest 가
> 그렇게 죽었는데 태스크는 **「completed exit 0」으로 보고**했다. 산출물을 봐라.

## 완료 이력

- 직전 재판정 결과 — [`entry-completeness-rejudgement`](dev-log/2026-08-01-entry-completeness-rejudgement.md)
- 이번 주 완료 스프린트와 이전 회고 — [`dev-log/INDEX.md`](dev-log/INDEX.md)
- 2026-07-26 이전 status 원문 — [`archive/status-history.md`](archive/status-history.md)
- 열린 BL의 현재 상태 — [`backlog.md`](backlog.md) (`scripts/bl-audit.sh`가 정본)
