<!-- C 디자인 언어 프로토타입 17벌을 React 로 이식하기 위한 다음 세션 핸드오프 -->

# QuantBridge 핸드오프 — C 디자인 언어 React 이식

작성 2026-07-20 · 프로토타입 확정 세션에서 이어짐

## 0. 다음 세션이 할 일 (한 줄)

확정된 **C 디자인 언어 프로토타입 17벌**을 React 로 이식한다. 1차 슬라이스는 **셸 + 토큰 + P1 4화면**이고, **이식 계획 수립부터** 시작한다.

---

## 1. 확정된 결정 (사용자 인터뷰, 2026-07-20)

이 넷은 이미 정해졌다. 다시 묻지 마라.

| #   | 항목                 | 확정                                                                                           |
| --- | -------------------- | ---------------------------------------------------------------------------------------------- |
| 1   | **1차 슬라이스**     | 셸 + 토큰 + **P1 4화면** (`/trading` · `/dashboard` · `/backtests` · `/backtests/[id]/trades`) |
| 2   | **토큰 적용**        | `globals.css` **전면 교체** 방식 (병렬 추가 후 점진 전환 아님)                                 |
| 3   | **Track A 슬롭 9종** | **이식과 동시 처리** (어차피 같은 파일을 만진다)                                               |
| 4   | **거래소 표기**      | **Bybit 단일로 캐논 확정.** OKX 는 로드맵으로 내린다                                           |

---

## 2. ★측정이 뒤집은 전제 — 반드시 읽어라

이식 계획을 세우기 전에 **실측**했고, 사용자와 내가 인터뷰 시점에 공유하던 전제 하나가 틀렸다.

### 색은 이미 거의 다 맞다

"토큰 전면 교체"라는 말이 **색을 갈아끼우는 작업**을 뜻하는 것으로 읽혔지만, 실제로는

- 프로토타입 색상 토큰 **14개 중 13개가 hex 레벨에서 이미 완전 일치**한다.
- 어긋나는 건 **`--text-muted` 하나뿐**. 현재 `#7a828c`(`globals.css:360`), 프로토타입 `--ink-3 #8b939c`. 2줄 수정이면 끝난다.
- `@theme inline`(`globals.css:272-342`) 구조가 이미 올바르게 잡혀 있어, **hex 만 바꾸면 토큰 소비 파일 191개가 코드 수정 없이 자동 반영**된다.

### 진짜 작업은 색이 아니라 이 셋이다

| 실제 작업                                                                                                | 규모                           | 위험                                                                                                                                                                         |
| -------------------------------------------------------------------------------------------------------- | ------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **토큰 이름 13개 리네임** (`--card-raised`→`--card-2`, `--border`→`--line`, `--text-primary`→`--ink` 등) | 191파일 영향                   | 🔴 **`chart-tokens.ts:65-66` 이 조용히 깨진다.** `read("--text-muted")` / `read("--border")` 가 fallback 으로 떨어져 **런타임 에러 없이 색만 틀린다.** 가장 탐지 어려운 회귀 |
| **반경 14종 → 1종(`var(--r)` 12px)**                                                                     | 유틸리티 30종                  | 🔴 가장 큰 물리 작업. `rounded`(45곳)·`rounded-full`(75곳)·`rounded-2xl`·`rounded-3xl` 은 `@theme` **밖의 Tailwind 기본값**이라 토큰 단일화가 자동 전파되지 않는다           |
| **1024px 아이콘 레일 신설**                                                                              | 셸 5파일 416줄 + `ui-store.ts` | 🔴 현재 계층 자체가 없다. `sidebarOpen` 이 뷰포트를 안 보고 Zustand 수동 토글이며, 그 토글을 부르는 데스크톱 UI 도 없다(`dashboard-header.tsx:27-29` 에 dead prop 으로 명시) |

**따라서 결정 2번은 "색 교체"가 아니라 "이름 정합 + 반경 통합"으로 다시 읽어야 한다.** 계획 수립 시 이 프레임으로 시작하라.

### 좋은 소식 하나

**Playwright 시각 스냅샷 baseline 이 0개다.** `toHaveScreenshot` / `toMatchSnapshot` / `*-snapshots/` 전부 0건. 리디자인으로 깨질 시각 회귀가 물리적으로 존재하지 않는다. E2E 셀렉터도 **98/121(81%)** 이 `getByTestId`+`getByRole` 이고 CSS 클래스 셀렉터는 0건이라 마크업 재작성에 강하다.

---

## 3. 지금까지 된 것 (복제하지 말고 읽어라)

`docs/prototypes/shotgun-2026-07/` 에 전부 있다.

| 파일                                 | 내용                                                                                                                       |
| ------------------------------------ | -------------------------------------------------------------------------------------------------------------------------- |
| `screen-01` ~ `screen-17`            | **확정 프로토타입 17벌.** 이식의 시각적 정본                                                                               |
| `variant-c.html`                     | 디자인 언어 정본 (백테스트 리포트)                                                                                         |
| `_kit.html`                          | 공용 셸 (토큰 + 컴포넌트 + 반응형 CSS + 사이드바/상단바 마크업)                                                            |
| **`_KIT.md`** (600행)                | 하드제약 15 · 컴포넌트 인벤토리 · **§4 워크스페이스 캐논**(4.1 원장 지정 SSOT ~ 4.9 인쇄 금지 목록 ~ 4.10 용어·접근성)     |
| **`terminology-ssot.md`** (1128행)   | enum 한국어 대조표 · 칩 톤 규약 · CSS primitive 규약 · **TypeScript 모듈 전문**(그대로 복사 가능) · 적용 지점 · 미해결 8건 |
| `cross-audit-notes.md`               | 4렌즈 교차 감사 원본 노트                                                                                                  |
| `checklist.md` · `context-notes.md`  | 진행 이력과 결정 근거                                                                                                      |
| `preflight.py` · `runtime-check.mjs` | 프로토타입 검사기 2종 (**HTML 전용. React 에는 그대로 못 쓴다** — §7 참조)                                                 |
| `serve.py` · `viewer.html`           | 뷰어. `python3 serve.py` → http://localhost:4173/viewer.html                                                               |

프로토타입 최종 상태 = **preflight FAIL 0 / runtime 17-17 PASS / 노출 마크업 위반 0**.

---

## 4. 실측된 이식 베이스라인

### 4.1 P1 4화면 코드량 = 4,771줄

| 라우트                   | 줄 수     | error.tsx | loading.tsx | 비고                                                                     |
| ------------------------ | --------- | --------- | ----------- | ------------------------------------------------------------------------ |
| `/trading`               | **2,494** | ❌        | ✅          | 절반 이상. `features/trading` + `features/live-sessions` 2도메인에 걸침  |
| `/dashboard`             | 409       | ❌        | ✅          | 실질 전량이 `dashboard-cockpit.tsx` **383줄 단일 파일**                  |
| `/backtests`             | 528       | ❌        | ✅          | 서버 prefetch + HydrationBoundary 패턴 확립됨                            |
| `/backtests/[id]/trades` | 1,340     | ❌        | ❌          | `trade-detail-table.tsx` 484줄. 인라인 Suspense fallback 이 텍스트 한 줄 |

`page.tsx` 4개가 전부 얇다(6~51줄). **리디자인은 `_components` 만 손대면 된다.**

**★ error.tsx 가 10개 라우트에서 부재**하고 P1 4개가 전부 포함된다. `.ai/stacks/nextjs/frontend.md` §6 이 의무로 규정하는데 미준수다.

### 4.2 셸

- 5파일 **416줄**, 이미 잘 분리돼 있어 리라이트 비용이 낮다.
- **활성 nav 6개가 프로토타입 6개와 정확히 일치.** 항목 재정의 불필요.
- 초과분 2개(`/templates`, `/exchanges`)는 둘 다 `disabled: true`. **거취 결정 필요** (프로토타입에 없음).
- `md`(768) / `lg`(1024) 브레이크포인트 픽셀값이 이미 프로토타입과 일치. 햄버거 768px 도 일치.
- **nav-count 배지는 데이터 소스 자체가 없다.** 신설하려면 훅/API 부터 만들어야 한다.

### 4.3 하드코딩 hex = 실질 8건(5파일)

`brand-palette.ts` 38건은 의도된 SSOT 라 정당하다. 진짜 문제는 8건이고, **그중 4건이 P1 `/trading` 안에 있다.**

- `trading-dash-hero.tsx:100-102` — `#EFF6FF` `#D1FAE5` `#FEE2E2` (Tailwind 팔레트 잔재, **다크에서 대비 붕괴**)
- `kill-switch-banner.tsx:46` — `#FEE2E2` (동일)
- `layout.tsx:23,24` · auth 2곳 — theme-color / Clerk appearance 주입용

`components/monaco/pine-language.ts` hex 11건은 `brand-palette.ts` 를 **import 하지 않고 자체 보유**해 desync 위험이 있다.

### 4.4 차트

- **색상이 `lib/chart-tokens.ts` 단일 게이트웨이로 완전 중앙화.** 16개 차트 파일 중 색 하드코딩 0건.
- 라이브러리 2종 병용 — `lightweight-charts` 11파일 + `recharts` 5파일. 스타일 API 가 달라 **두 경로를 각각 검증**해야 한다.
- `chart-tokens.ts:3-4` 에 제약이 명시돼 있다. lightweight-charts 는 `var()` 문자열을 못 읽어서 `getComputedStyle` 로 해석한 hex 를 넘겨야 한다.

### 4.5 테스트 / CI

- 단위 **155개**(41 ts + 114 tsx), E2E **11 spec / 42 케이스**.
- CI 프론트 게이트 5단계 — 훅 규칙 grep / `pnpm lint` / `tsc --noEmit` / `pnpm test` / `pnpm build`.
- **PR CI 는 E2E 3케이스(`smoke.spec.ts`)만 돌린다.** authed 39케이스는 게이트 밖이라 셸·네비 회귀를 CI 가 못 잡는다. 로컬 `pnpm e2e:authed` 수동 실행 필요.
- **프론트엔드 커버리지 임계 없음** — 리디자인 중 테스트를 지워도 CI 가 침묵한다.
- 카피 결합 지점. `getByText` 18곳, `landing-features.test.tsx:26` 이 `"벡터화 백테스트"` 를 assert 중.

### 4.6 Track A 슬롭 9종 현재 상태

| #   | 항목                         | 상태             | 위치                                                                                                                                        |
| --- | ---------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | "100개 이상의 글로벌 거래소" | 🔴               | `landing-faq.tsx:11`                                                                                                                        |
| 2   | step-4 `isError` 분기 부재   | 🔴               | `step-4-result.tsx:67`. 실패 시 "첫 백테스트 완주!" 헤드라인과 함께 지표 3개가 전부 `—`                                                     |
| 3   | "≤ 50회 평가" vs 실제 100    | 🔴               | `optimizer-page-view.tsx:89-90`. 스키마는 `.max(100)` 이고 주석에 `// BL-237: 50→100`                                                       |
| 4   | 노출 카피 em-dash            | 🟡               | 산문 연결자 **~91건**. **placeholder `"—"` 94건은 정당하니 일괄 치환 금지**                                                                 |
| 5   | "벡터화" 표기                | 🔴               | 4곳 + 테스트 2곳이 문자열 assert                                                                                                            |
| 6   | 가짜 라이브 상태             | 🔴               | `error.tsx:105-118`(500 화면에서 "정상 운영 중" + `animate-ping`) · `maintenance/page.tsx:29-37`(ETA·진행률 전부 하드코딩). **404 는 깨끗** |
| 7   | 가짜 소셜프루프              | 🟢 **이미 해결** | BL-270/271. `landing-stats-strip.tsx:4-9` 의 정직 표기가 나머지 카피 수정의 **참조 모델**                                                   |
| 8   | genetic Best 버그            | 🔴               | `optimizer-run-list.tsx:89-98`. `kind === "genetic"` 분기 누락 → Best 열이 항상 `—`. **한 줄로 고쳐진다**                                   |
| 9   | OrdersPanel 반쪽 한글화      | 🔴               | `orders-panel.tsx:97-106`. 9개 중 영문 7 / 약어 1 / 한글 1                                                                                  |

---

## 5. 이식 착수 우선순위 (실측 기반 제안)

난이도 순. 계획 수립 시 출발점으로 쓰되 그대로 따르지는 마라.

| 순위 | 작업                                                                           | 난이도                         |
| ---- | ------------------------------------------------------------------------------ | ------------------------------ |
| 1    | `--text-muted` `#7a828c`→`#8b939c` (`globals.css:360` + `brand-palette.ts:44`) | 🟢 2줄                         |
| 2    | genetic Best 버그 (`optimizer-run-list.tsx:94`)                                | 🟢 1줄                         |
| 3    | OrdersPanel 헤더 한글화 (`orders-panel.tsx:97-106`)                            | 🟢 7줄                         |
| 4    | 하드코딩 hex 8건 토큰화                                                        | 🟢                             |
| 5    | 슬롭 카피 ①③⑤⑥                                                                 | 🟡 테스트 동반                 |
| 6    | step-4 `isError` 분기                                                          | 🟡                             |
| 7    | error.tsx 10개 신설                                                            | 🟡                             |
| 8    | **토큰 리네임 13건**                                                           | 🔴 `chart-tokens.ts` 동반 필수 |
| 9    | **반경 14종→1종**                                                              | 🔴                             |
| 10   | **1024px 아이콘 레일**                                                         | 🔴                             |

---

## 6. 하드 제약 (프로토타입에서 확립. 이식에도 그대로 적용)

`_KIT.md` §1 이 정본이다. 요약하면

1. 노출 카피 em-dash / en-dash **0건**. HTML 엔티티(`&mdash;` `&ndash;`)까지 검사. **단 값이 오직 `—` 하나인 셀은 "데이터 없음" 관례로 허용**
2. 대비 최소 `#8b939c`(5.83:1). `#565d66`(2.92:1) 텍스트 금지
3. `outline: none` 금지. 쓰면 `:focus-visible` 이 특이도·소스순서 양쪽에서 이기는지 확인. 컨테이너는 `:focus-within`
4. `display:none` 으로 라벨이 사라지는 구간엔 `aria-label` 필수
5. **숫자는 검산이 맞아야 한다.** 공식을 인쇄했으면 그대로 나와야 한다
6. 차트 축은 선형이고 픽셀이 표기값과 일치
7. 엔진은 `pine_v2 · 바 단위 이벤트 루프`. **"벡터화" 금지**(ADR-011)
8. 사용자는 실제 `woosung`. 가공 인물·가짜 알림 배지·가짜 라이브 점 금지
9. 상태 4종을 실제로 그린다 — 스켈레톤 / 에러(`role="alert"` + 실제 엔드포인트 + HTTP 코드 + 재시도) / 빈 상태 / 무데이터 셀
10. `prefers-reduced-motion` 에서 모션 무력화. 주 데이터를 1초 넘게 가리는 애니메이션 금지
11. 반경은 `var(--r)` 하나
12. 무한 반복 애니메이션은 스켈레톤 shimmer 하나뿐. **펄스·블링크 금지**
13. 375px 에서 페이지 가로 스크롤 0

### 하지 말 것 (사용자가 명시적으로 기각)

- **A안(터미널 밀도)의 밀도 장치 이식 금지** — 24셀 격자 · 파라미터 테이프 · 크로스헤어 · 키보드 칩. 병합안을 실제로 만들었고 기각당했다. **C 의 여백은 의도다**
- **미터 바와 번호 아이브로를 임의로 제거하지 마라.** 평가자 4명이 제거를 권고했지만 사용자가 "C 그대로"를 선택했다

---

## 7. ★검사 장치 — React 용은 새로 만들어야 한다

`preflight.py` 와 `runtime-check.mjs` 는 **단일 HTML 파일 전용**이다. React 에 그대로 못 쓴다. 다만 **검사 항목과 임계값은 그대로 이식할 가치가 있다.**

이번 세션에서 이 두 도구가 잡아낸 것들이 근거다.

- 대비를 **배경 합성 후 실제 렌더값**으로 계산 (단순 hex 비교로는 못 잡음)
- 포커스 링을 **실제 Tab 30회 이동**으로 검증
- 차트 y축 **라벨↔픽셀 공선성**(1.6px 이내)
- 공용 CSS **바이트 무결성** (드리프트 원천 차단)

React 이식 시 대응 방안 후보 — 계획 단계에서 정하라.

- 기존 Playwright E2E 에 대비/포커스 검사를 붙인다 (시각 스냅샷 baseline 이 0개라 자유도가 높다)
- `getComputedStyle` 기반 런타임 검사를 E2E 픽스처로
- 반경/색 토큰 이탈은 **ESLint 규칙 또는 단위 테스트**로 (이미 `no-internal-ids.test.ts` 가 카피 가드 선례)

---

## 8. 함정 (전부 이번 세션에서 실제로 데인 것)

1. **검사기를 만들면 known-good 산출물에 먼저 돌려라.** 이번에 검사기 자체 버그 4개를 그렇게 잡았다. 안 했으면 빌더 17명의 시간을 태웠다.
2. **grep 만으로 판정하지 마라.** 이번 세션에서 **3번** 오탐했고 전부 **주석 안의 교정 기록**이었다. 주석을 공백 치환한 뒤 노출 마크업만 검사해야 한다.
3. **에이전트 자기보고를 믿지 마라.** "반경 단일"이라며 8종, "순백 0건"이라며 주석에 존재한 사례가 있었다. 판정은 명령 출력에 걸어라.
4. **워크플로 에이전트가 failed 로 떠도 파일은 이미 있을 수 있다.** 이번에 한도 초과로 죽은 2개 에이전트의 산출물이 **완성 상태로 생존**해 재작성을 피했다. 재실행 전 산출 디렉터리부터 확인하라.
5. **파일 단위 검증 통과 ≠ 전체 정합.** 17벌이 개별 통과한 뒤 교차 감사에서 49건(BLOCKER 3)이 나왔다. **정직성 규율은 파일 경계에서 샌다** — 화면 A 가 "이 값은 지어내지 않는다"고 적고 화면 B 가 그 값을 인쇄한 사례가 3건이었다. React 에서는 **컴포넌트 경계**에서 같은 일이 일어난다.
6. **검증 환경이 실사용보다 깨끗하면 stale 을 놓친다.** 뷰어 검증이 매번 새 브라우저 컨텍스트를 띄워 통과했는데 사용자 브라우저에서는 캐시로 구버전이었다.

---

## 9. 미해결 (계획 단계에서 정해야 함)

프로토타입만으로는 확정할 수 없어 남긴 것들. `_KIT.md` §6 과 `terminology-ssot.md` §6 에도 등재돼 있다.

1. **평가 상한 숫자** — 백엔드 상수와 프로토타입은 100, 프론트 셀렉트는 "≤ 50회", `genetic.py` docstring 도 50. `max_evaluations` 는 프론트 스키마에 **상한 자체가 없다**(`.positive()` 만)
2. **전략 수명주기 enum** — `draft`/`validated`/`deployed` 가 프로토타입에만 있고 `strategy/schemas.ts` 에 0건. 서버 필드 신설 대 프로토타입에서 제거
3. **`strategy.backtest_count` 정의** — 완료 기준인지 전체 실행 기준인지 화면만으로 확정 불가
4. **`.chip.warn` vs `.chip.failed` 개명 범위** — 사용 빈도는 failed 가 4:1 우세
5. **disabled nav 2개**(`/templates`, `/exchanges`) 거취 — 프로토타입에 없다
6. **nav-count 배지** — 넣을지, 넣으면 데이터 소스를 어떻게 만들지

---

## 10. 레포 상태 주의

**프로토타입 전체가 아직 untracked 이고 현재 브랜치가 `main` 이다.**

```
?? docs/prototypes/shotgun-2026-07/
?? skills-lock.json
```

프로젝트 규칙상 **main 직접 커밋·푸시는 영구 차단**이다. 머지 전략은 `stage/<theme>` 경유 + **사용자가 stage→main 을 수동 처리**하는 것이 이 프로젝트의 기본값이다.

### ★이식 브랜치는 반드시 `stage/` 접두사를 써라

`.github/workflows/ci.yml` 의 트리거가 이렇다.

```yaml
on:
  pull_request:
    branches: [main, "stage/**"]
```

**base 가 `main` 이거나 `stage/**`인 PR 에서만 CI 가 발화한다.** 그 밖의 브랜치로 PR 을 올리면 lint·tsc·test·build 게이트가 **통째로 침묵한다.**
앱 코드를 바꾸는 이식 작업에서 이건 위험하다. 이식 브랜치는`stage/c-language-port` 처럼 잡거나 base 를 main 으로 두어라.

(프로토타입 커밋은 `docs/` + `skills-lock.json` 뿐이라 path 필터 `frontend/**`·`backend/**` 에도 안 걸려 CI 가 필요 없었다. 이식은 다르다.)

---

## 11. 다음 세션 첫 스텝

1. `CONTEXT.md` + `.claude/CLAUDE.md` + `docs/TODO.md` + `docs/REFACTORING-BACKLOG.md` 4종 읽기 (프로젝트 규칙)
2. 이 문서 + `_KIT.md` §4 캐논 + `terminology-ssot.md` 읽기
3. **프로토타입 커밋용 브랜치 생성** (§10)
4. **이식 계획 수립** — §2 의 뒤집힌 전제를 반영해서. "색 교체"가 아니라 "이름 정합 + 반경 통합 + 아이콘 레일 신설"이 실제 작업이다
5. 계획을 `checklist.md` + `context-notes.md` 에 기록한 뒤 사용자 확인
