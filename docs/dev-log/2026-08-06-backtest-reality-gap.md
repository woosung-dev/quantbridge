# 2026-08-06 — backtest-reality-gap (btgap)

> **회차 목적:** 소크(BL-003)가 데모 거래소에 쌓은 원장과 같은 구간의 백테스트 시뮬을
> **처음으로 대조**해, 「백테스트가 현실을 얼마나 예측하나」에 숫자를 붙인다.
> deliverable 은 코드가 아니라 숫자다. 격차는 BL-526 의 4단계
> (엔진 기대 gross → 체결 격차 → 비용 → 거래소 확정 net)로 분해한다.

## ★사전등록 — 측정 **전에** 커밋 (이 커밋의 git 시각이 증거다)

**창(window):** 대조 구간 = 2026-08-05T00:00Z ~ 캡처 시각(마지막 종료 봉).
대상 = 전략 `07a22564`(PbR Pivot Reversal, BTC/USDT, 1m, leverage=2)의 소크 세션 4개
(`a16aa640` 8.6h · `5a7147ba` 0.75h · `73eda5ef` 12.3h · `c160a1a9` 진행중).

| #   | 관측량                                               | 예측                                                     | 판별력 확보 조건       |
| --- | ---------------------------------------------------- | -------------------------------------------------------- | ---------------------- |
| ①   | 스팟↔perp 봉-매칭 진입쌍의 `spot_entry − perp_entry` | **중앙값 > 0** (스팟이 위 — 매수 스톱 조기 트리거)       | 봉-매칭 진입쌍 ≥ 10    |
| ②   | 그 차의 크기 (중앙값 절대가)                         | **[25, 42] USDT 자릿수** (0.04~0.066%, BL-535 실측 재현) | 봉-매칭 진입쌍 ≥ 10    |
| ③   | S2 총격차 중 비용 설명 비율                          | **≥ 40%**                                                | decomposable 매칭 ≥ 30 |
| ④   | 음성 대조 — 측정이 바꾸지 말아야 할 것 (아래 3종)    | **전건 불변**                                            | 실행 전/후 실측 병기   |

- ③ 정의: 분자 = \|Σ(actual_net − actual_gross)\| · 분모 = \|Σ(expected_gross − actual_net)\|,
  둘 다 **decomposable 매칭 부분집합만**(워터폴이 닫히는 관측만 — `outcome_parity.py` 규약).
- ④ 3종: (a) `ts.ohlcv` 스팟 행 불변 — `BTC/USDT` 1h **9,337** · 1m **0** (사전 실측 2026-08-06 09:5xZ)
  (b) 동일 perp CSV 2회 실행의 trades digest sha256 **동일**(엔진 결정론)
  (c) 소크 세션 `c160a1a9` **활성 유지**(측정이 창을 끊지 않는다).
  (a)(c) 위반 = 측정 절차 결함으로 **수치 전체 폐기**. (b) 위반 = 엔진 비결정론(별도 BL).
- ★**①이 0(무차)으로 나오면 BL-535 의 「명시적 트레이드오프」 판정이 뒤집힌다** —
  「무해」였다는 뜻이고, 그 자체가 판별력 있는 결과다. 실패로 적지 않는다.
- ②는 1h 봉 기준 실측(25~42)의 재현을 1m 에서 묻는다 — 자릿수가 다르면 그 값 자체가 산출물.

## S0 — preflight 실측 (2026-08-06 10:01Z)

- `soak-gate.sh` = **UNKNOWN 진행중**(exit 2) · 누적 **21.65h/168h** · 최장 연속 **12.28h/24h** ·
  실격 0 · C4/C5 전건 ✓. 현재 창 `c160a1a9` 연속 **8.91h**(T0 `01:06:46Z`).
- 고정 커밋 `3a90f80c` = origin/main 조상 YES · 활성 세션 1. 워커 PID 생성 `06:44:26Z`
  (디스크 사고 재시작 후) — pin 시각보다 뒤라 순서 정합.
- **★착수 전제 2건이 반증됐다 (플랜 단계 실측):**
  1. 「백테스트는 스팟 봉」은 **낡았다** — `providers/timescale.py:59` 가 canonical→perp 변환
     (PR #503, BL-535 🟡부분 Resolved). 남은 것은 **스팟/perp 결과 차 대조 미실시**뿐이다.
     ⇒ S1 을 「적재」가 아니라 「결과 차 정량화로 BL-535 종결」로 재정의.
  2. **라이브는 `ts.ohlcv` 를 읽지 않는다** — `tasks/live_signal.py:2882-2887` CCXT REST 직접.
     ⇒ perp 적재는 소크에 영향 0.
- 추가 실측: `ts.ohlcv` 1m **0행** · leverage=2 + 활성 세션 ⇒ API 백테스트 422
  `mirror_not_allowed`(`backtest/exceptions.py:202`) ⇒ **엔진 직접 호출이 유일한 재실행 경로** ·
  08-05 이후 체결 = `cond:` 72 + `condmkt:` 11 + 수동 flatten 3 · `live_signal_events` **0건**
  (이벤트 경로 미발화 — 기존 outcome-parity API 는 이 기간 expected 축이 빈다) ·
  `exchange_exits` 164행 중 matched **82**(엔진 무경유 오라클).

## 사전등록 판정 (측정 후 기입)

| #   | 예측                     | 실측                                                                                   | 판정       |
| --- | ------------------------ | -------------------------------------------------------------------------------------- | ---------- |
| ①   | `spot − perp` 중앙값 > 0 | 봉-매칭 144쌍 **전건 양수**, 중앙값 **+29.95 USDT**                                    | **적중**   |
| ②   | [25, 42] USDT 자릿수     | 29.95 (min 17.10 · max 47.40), 쌍 144 ≥ 10                                             | **적중**   |
| ③   | 비용 설명 비율 ≥ 40%     | 판정 축(strict) decomposable **0** — 판별력 조건(≥30) 미충족                           | **미판정** |
| ④   | 음성 대조 3종 전건 불변  | (a) 스팟 행 불변(1h 9,337 · 1m 0) ✓ (b) perp 2회 digest 동일 ✓ (c) 소크 창 연속 유지 ✓ | **적중**   |

- ①②의 짝짓기 커버리지: 144쌍 = spot 진입 193 의 **74.6%** · perp 210 의 **68.6%**. 무짝
  잔여의 net 은 spot −249.70 / perp −327.19 로 **양쪽 모두 크게 음수** — 차(差) 지표를
  한쪽으로 미는 뚜렷한 선택 편향은 관측되지 않았다(단 짝짓기 규칙의 방향·봉절삭 항은
  이 표본에서 판별력 0 — eval2 실측 — 이라 규칙 자체는 시험되지 않았다).
- ★③을 「적중」으로 적지 않은 이유 둘. ⑴ 판정 축이 비었다 — strict 매칭이 **구조적으로
  공집합**(아래 회고 §매칭)이라 loose 37쌍의 134.1%(행별-abs 82.4%)는 감도 참고값이다.
  loose 로의 사후 승격은 관대해지는 방향이라 래칫 위반(codex F2-01/03). ⑵ eval2 가 이
  지표의 판별력 자체를 반증했다 — 실측 Σcost 고정 시 FAIL 영역이 Σdiv > +253 / < −590
  뿐(라이브 총손익의 9배)이라 **거의 항진명제**고, 상한이 없어 134%도 「PASS」로 읽힌다.
  사전등록 지표의 판별력을 등록 전에 검증하지 않은 것이 이 회차의 교훈이다.

## 회고

### 한 줄 — 격차의 병목은 체결가도 비용도 아니라 **진입 집합**이다

같은 전략·같은 창(2026-08-05T00:00Z~08-06T11:00Z)에서 백테스트는 **210회** 진입하고
라이브는 **84회** 진입했으며, ±3봉으로 느슨하게 맞춰도 **34쌍(라이브의 40%)** 만 겹친다.
매칭된 쌍의 가격 잔차는 중앙 **0.014%** 로 작다 — 겹치기만 하면 거의 같은 값에 체결된다.
갈리는 것은 「언제 진입하는가」다. 유력 동인(미확정, 후속 BL): 라이브 롤링 300봉 워밍업의
pivot 상태 vs 백테스트 전구간 상태 · 재무장 트리거 드리프트(봉당 62~271 USDT) ·
세션 공백. 비용 축은 원장에서 닫혔다 — 라이브 실효 비용 = **taker 0.055%/leg 단일 성분**
(84 event 중 77 이 소수 8자리 일치, 비-taker 잔차 0.03%), 왕복 **0.11%**. 백테스트 가정
(fees 0.1% + slippage 0.05%/leg = 왕복 0.30%)은 그 **2.7배**다. 그리고 이 창의 라이브
손익 Σgross **+28.27** vs Σ비용 **−172.83** — 수수료가 전략 손익의 **6.1배**다.

### 측정이 스스로 교정된 궤적 (검증 4층이 각자 다른 결함을 잡았다)

- **codex Phase 1** (정적): 사전등록 ③의 부호 상쇄·④의 비판별성·매칭 과잉의 fail-open
  방향 — 측정 **전에** 그리디 키를 강화시켰다(P1 4건 채택 → gen r1).
- **eval2 r0** (실행형): `exchange_exits` 덤프가 **정확히 2배 중복**(ours/unknown 분류 쌍,
  86 event = 172행 — 순진한 Σ 는 손익 2배) · 비용 축 taker 단일 성분 확정 · flatten 3건 중
  2건은 orders 밖 · ③ 항진성.
- **codex Phase 2**: strict 공집합 시 「판정 불능」 표기 의무 · FIFO 귀속의 오귀속 경로 ·
  s1diff 무보고 탈락 — 전부 r2 로 반영.
- **eval2 r1** (실행형): ★**귀속 off-by-one 실증** — 시간순 FIFO 가 직결 링크와 59/86 에서
  다른 주문을 골랐고(수동 flatten 이 커서를 선점), loose 37쌍 중 28쌍이 남의 청산 손익을
  쓰고 있었다. 반사실 재조립에서 「가격 격차 +19.36」이 **−0.36 으로 부호 반전** —
  ⇒ 그 수치는 귀속 인공물이었다. ★그리고 **「버킷 합이 dedup Σ 로 닫힌다」는 두 귀속
  모두에서 성립**했다 — 합계 닫힘은 귀속의 옳음을 증명하지 않는다.
- **gen 의 판별자**: 링크가 왕복의 어느 끝인지(여는 주문 vs 닫는 주문)는 합계로 못 가른다
  (telescoping). 행별 `entry_price_agreement` 를 도구에 심어 실덤프 1회로 닫았다 —
  **exact_matches 0/81** = 링크는 **닫는 쪽**(반전 병합 주문이 앞 포지션을 닫는다) ⇒
  opener 귀속은 링크의 직전 진입(r3 수리).

### 라이브 원장에서 처음 확정된 값들 (엔진 무경유)

- 라이브 왕복 실효 비용률 **0.1101%** — BL-526 화면(전략 누적 31세션 축)과 원장 재계산이
  일치. 백테스트 기본 가정 0.30% 는 **2.7배 과대**.
- 수수료 이탈 5행은 펀딩 가설이 **2×2 표로 반증**(필요조건도 충분조건도 아님, 합
  +0.055 USDT = 총비용의 0.03%) — 원인 미상으로 남기고 등재만.
- 반전 전략의 원장 구조: 반전 주문 1건이 앞 포지션 청산 + 새 포지션 진입(수량 2배,
  81/84) — `closed-pnl` 의 `orderId`(= `order_link_id`)는 **닫은 주문**을 가리킨다.

### S0 반증·정정 기록

- 착수 전제 2건 반증(§S0) 외에, 회차 중 **내 산출 수치 2건도 정정**됐다 —
  「`realized_pnl_synced_at` 1건뿐」은 창 스코프 값이고 레포 전체는 68건(qa D4),
  「condmkt 11」은 12(eval2). 「exits 168행」은 **84(→86) event 의 2배 중복**이었다.
- qa (BL-526 표면): 활성·종료 세션 모두 도달, NaN/콘솔 에러 0. 사전 예측 「이 기간
  세션 축 matched≈0」 적중(이벤트 경로 미발화가 원인 — 데이터의 사실). 발견 3건은
  BL 등재(경고 스코프 맹목 · Decimal 오버플로 · e2e 부재).

### S2 — 4단 분해 확정치 (r3 opener 귀속 · 행별 검증 80/81 `agrees`)

**대조 창** = 2026-08-05T00:00Z ~ 08-06T11:00Z(채점) · 원장 dedup **86 event** ·
라이브 진입 84(cond 72 + condmkt 12) · 백테스트 진입 210. 소크 세션 4개
(`a16aa640`/`5a7147ba`/`73eda5ef`/`c160a1a9`), 노출 합 ≈ 30.6h.

**event 파티션 (합 = dedup Σ −149.84743341, `closes: true` — CONTROL 독립 재계산 비트 일치):**

| 버킷            | 건수 | net (USDT) |
| --------------- | ---- | ---------- |
| matched (loose) | 34   | −50.45     |
| live_only       | 46   | −92.81     |
| ambiguous       | 1    | −1.76      |
| inferred (FIFO) | 3    | +3.04      |
| ledger_only     | 2    | −7.86      |

**4단 워터폴 (matched 34 — ★판정값 아님, strict 축 공집합이라 감도 분석):**
엔진 기대 gross **−8.84** → ② 체결 격차(가격) **+28.35** → ③ 비용 **−69.95** →
④ 거래소 확정 net **−50.45**. 존재 격차는 별도: backtest_only 173 의 기대 gross **+16.52** ·
live_only 46 의 확정 net **−94.57**. 매칭쌍 진입가 잔차 중앙 **0.014%**(0.1% 초과 0건).

**읽는 법.** 매칭이 라이브 진입의 **40%**(34/84) 뿐이므로 워터폴은 「겹친 부분」의
이야기다. 겹친 부분에서는 라이브가 백테스트 기대보다 **+28.35 잘 벌었고**(가격 축),
비용 **−69.95** 가 그걸 뒤집어 net −50.45 가 됐다. 겹치지 않은 라이브 46건이 **−92.81** 로
전체 손실의 62%를 냈다 — **백테스트가 아예 예측하지 못한 진입들이 손실의 주인**이다.

**귀속의 근거 사슬**(이 수치를 믿는 이유): `order_link_id` 는 닫는 주문(반전 병합) —
행별 판별자 `entry_price_agreement` 가 opener=직전 진입 규칙에서 **exact 80/81 ·
median 0**(r2 의 링크=opener 규칙은 같은 판별자에서 0/81). 합계 닫힘은 두 귀속 모두에서
성립했으므로 증거로 쓰지 않았다.
