# 2026-08-06 — backtest-reality-gap (btgap)

> **회차 목적:** 소크(BL-003)가 데모 거래소에 쌓은 원장과 같은 구간의 백테스트 시뮬을
> **처음으로 대조**해, 「백테스트가 현실을 얼마나 예측하나」에 숫자를 붙인다.
> deliverable 은 코드가 아니라 숫자다. 격차는 BL-526 의 4단계
> (엔진 기대 gross → 체결 격차 → 비용 → 거래소 확정 net)로 분해한다.

## ★사전등록 — 측정 **전에** 커밋 (이 커밋의 git 시각이 증거다)

**창(window):** 대조 구간 = 2026-08-05T00:00Z ~ 캡처 시각(마지막 종료 봉).
대상 = 전략 `07a22564`(PbR Pivot Reversal, BTC/USDT, 1m, leverage=2)의 소크 세션 4개
(`a16aa640` 8.6h · `5a7147ba` 0.75h · `73eda5ef` 12.3h · `c160a1a9` 진행중).

| #   | 관측량                                               | 예측                                                     | 판별력 확보 조건       |
| --- | ---------------------------------------------------- | -------------------------------------------------------- | ---------------------- |
| ①   | 스팟↔perp 봉-매칭 진입쌍의 `spot_entry − perp_entry` | **중앙값 > 0** (스팟이 위 — 매수 스톱 조기 트리거)       | 봉-매칭 진입쌍 ≥ 10    |
| ②   | 그 차의 크기 (중앙값 절대가)                         | **[25, 42] USDT 자릿수** (0.04~0.066%, BL-535 실측 재현) | 봉-매칭 진입쌍 ≥ 10    |
| ③   | S2 총격차 중 비용 설명 비율                          | **≥ 40%**                                                | decomposable 매칭 ≥ 30 |
| ④   | 음성 대조 — 측정이 바꾸지 말아야 할 것 (아래 3종)    | **전건 불변**                                            | 실행 전/후 실측 병기   |

- ③ 정의: 분자 = \|Σ(actual_net − actual_gross)\| · 분모 = \|Σ(expected_gross − actual_net)\|,
  둘 다 **decomposable 매칭 부분집합만**(워터폴이 닫히는 관측만 — `outcome_parity.py` 규약).
- ④ 3종: (a) `ts.ohlcv` 스팟 행 불변 — `BTC/USDT` 1h **9,337** · 1m **0** (사전 실측 2026-08-06 09:5xZ)
  (b) 동일 perp CSV 2회 실행의 trades digest sha256 **동일**(엔진 결정론)
  (c) 소크 세션 `c160a1a9` **활성 유지**(측정이 창을 끊지 않는다).
  (a)(c) 위반 = 측정 절차 결함으로 **수치 전체 폐기**. (b) 위반 = 엔진 비결정론(별도 BL).
- ★**①이 0(무차)으로 나오면 BL-535 의 「명시적 트레이드오프」 판정이 뒤집힌다** —
  「무해」였다는 뜻이고, 그 자체가 판별력 있는 결과다. 실패로 적지 않는다.
- ②는 1h 봉 기준 실측(25~42)의 재현을 1m 에서 묻는다 — 자릿수가 다르면 그 값 자체가 산출물.

## S0 — preflight 실측 (2026-08-06 10:01Z)

- `soak-gate.sh` = **UNKNOWN 진행중**(exit 2) · 누적 **21.65h/168h** · 최장 연속 **12.28h/24h** ·
  실격 0 · C4/C5 전건 ✓. 현재 창 `c160a1a9` 연속 **8.91h**(T0 `01:06:46Z`).
- 고정 커밋 `3a90f80c` = origin/main 조상 YES · 활성 세션 1. 워커 PID 생성 `06:44:26Z`
  (디스크 사고 재시작 후) — pin 시각보다 뒤라 순서 정합.
- **★착수 전제 2건이 반증됐다 (플랜 단계 실측):**
  1. 「백테스트는 스팟 봉」은 **낡았다** — `providers/timescale.py:59` 가 canonical→perp 변환
     (PR #503, BL-535 🟡부분 Resolved). 남은 것은 **스팟/perp 결과 차 대조 미실시**뿐이다.
     ⇒ S1 을 「적재」가 아니라 「결과 차 정량화로 BL-535 종결」로 재정의.
  2. **라이브는 `ts.ohlcv` 를 읽지 않는다** — `tasks/live_signal.py:2882-2887` CCXT REST 직접.
     ⇒ perp 적재는 소크에 영향 0.
- 추가 실측: `ts.ohlcv` 1m **0행** · leverage=2 + 활성 세션 ⇒ API 백테스트 422
  `mirror_not_allowed`(`backtest/exceptions.py:202`) ⇒ **엔진 직접 호출이 유일한 재실행 경로** ·
  08-05 이후 체결 = `cond:` 72 + `condmkt:` 11 + 수동 flatten 3 · `live_signal_events` **0건**
  (이벤트 경로 미발화 — 기존 outcome-parity API 는 이 기간 expected 축이 빈다) ·
  `exchange_exits` 164행 중 matched **82**(엔진 무경유 오라클).

## 회고

(회차 종료 시 기입)
