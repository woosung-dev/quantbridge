# metric-guard-parity — 계측이 성공한 발주를 실패로 기록하고, 주문을 하나 더 냈다 (2026-08-02)

> **스코프.** [BL-579] 머니-패스 계측 가드 **18곳**(10 + G6 적발 8) + census 회귀 래칫 + [BL-576] 유도 발화 soak + `/metrics` 측정.
> **배치.** codex 생성자(4세션) + CONTROL 평가자. 사용자 확정.
> **결과.** 결함을 **재현으로 확정**(red 6건) 후 수리(green) · census **159 → 141** ·
> [BL-576] soak **부분(Partial)** · `/metrics` 는 측정만.
> **마이그레이션 0 · 새 엔드포인트 0 · FE diff 0.**

---

## 한 줄

**「성공한 발주가 실패로 기록될 수 있다」를 재현하러 들어와서, 그것이 동시에 주문을 하나 더 낸다는
것을 발견했다 — 그리고 그 자리는 백로그가 가리킨 두 파일 어디에도 없었다.**

---

## 0. 결함은 이제 논증이 아니라 재현이다

착수 시점 `qb_metrics_mutation_failed_total` = **0**. [BL-579] 의 Trigger 는 **발화한 적이 없다.**
그래서 이 회차는 「관측된 사고」가 아니라 **구조적 논증** 위에서 시작했고, 그 사실을 그대로 적었다.

수리 **전에** 고장 주입 테스트 6건을 red 로 만들어 커밋했다(`f9faeb34` · `e8a98954`). 실측:

```
# trailing
result = {'failed': 'trailing_unprotected', 'order_id': 'f70cac05-...'}
ERROR src.tasks.trading:trading.py:1405 trailing_unprotected_giveup
  → set_trading_stop 은 **성공**했는데 「포지션 무방비」로 기록되고 critical alert 가 나갔다

# 조건부 진입
assert harness.order_service.execute.await_count == 1
AssertionError: assert 2 == 1
  → ★계측 mmap 실패가 거래소 주문을 **하나 더** 만들었다
```

★**고장 모델이 정직해야 했다** — multiprocess 에서 새 라벨 조합은 `.labels()` 시점에 mmap 을
늘린다. 그래서 주입은 `.labels()` 가 `OSError` 를 던지게 하는 것이고, 이건 **실제 실패 경계**다.
★**테스트 본문은 `record_metric_safely` 를 한 번도 언급하지 않는다** — 언급하면 순환 오라클이 된다.
단언은 전부 **비즈니스 귀결**(거래소 호출 횟수 · 거짓 알림 · 후처리 누락)이다.

---

## 1. ★★★백로그의 파일 목록이 나를 잘못된 곳으로 이끌었다

[BL-579] 는 수리 지점을 **`tasks/trading.py` · `services/order_service.py`** 라고 적었고,
내 1차 조사가 **그 두 파일만** 깊게 팠다. 실측 결과:

| 백로그                       | 실측                                                              |
| ---------------------------- | ----------------------------------------------------------------- |
| P1 이 두 파일에 있다         | **최강 P1 은 둘 다에 없다** — `live_signal.py:1689/:1690`         |
| `order_service.py` 를 고쳐라 | **P1 0곳.** 10건 전부 발주 **전** 검증 거절 후 즉시 `raise`       |
| 가드 밖 **127곳**            | 규칙 R1 로 **159곳**. ★단 백로그에 **세는 규칙이 없어** 재현 불가 |

⇒ **핸드오프의 파일 목록은 조사 범위가 아니라 조사 대상이다.**
⇒ 그래서 이 회차의 첫 산출물은 수치가 아니라 **규칙을 인쇄하는 커밋된 census 테스트**다.

---

## 2. ★★레포가 이미 돈 쓴 방어를, 한 줄 위가 무효화하고 있었다

두 자리에서 같은 형태를 발견했다.

**(a) `state_handler.py`** — `:157` `record_partial_fill` 은 `record_metric_safely` 로 감쌌고
**전용 회귀 테스트**(`test_partial_fill_metric_failure_does_not_stop_fill_postprocessing`,
2026-08-01 codex 적대 리뷰 MAJOR 후속)까지 붙어 있다. 그런데 **23줄 위 `:135` 의 raw
`qb_active_orders.dec()`** 가 던지면 그 가드도, 그 테스트의 대상도, 후처리 3종도 **전부 도달 불가**다.

**(b) `live_signal.py:902`** — 바로 아래에 25줄짜리 주석이 이 실패 모드를 **문자 그대로** 적어놨다:
「여기서 예외가 올라가면 호출자의 전역 catch 가 그 tick 을 통째로 끝낸다」 ·
「**그 주문의 트레일링은 영구 유실**이다([BL-567] 로 등재)」. 그래서 세 훅을 per-hook `try/except` 로
격리했다. **그 격리 블록 한 줄 위가 가드 밖이었다.**

★**[BL-579] 가 말한 「관용구가 전파되지 않았다」의 실제 모습이 이것이다.**

---

## 3. 수리 — 18곳, 새 추상 0

**Tier 1**(접수·enqueue 성공 직후 · 던지면 성공이 실패로 기록) — `live_signal.py:1689/:1690/:1762` ·
`trading.py:1295` → `_count_safely`.
**Tier 2**(내구 쓰기와 후처리 훅 사이 `qb_active_orders.dec()`) — `state_handler.py:135` ·
`trading.py:496/:839` · `reconciliation.py:130` · `janitor.py:156` · `live_signal.py:902`
→ **기존** `record_metric_safely(qb_active_orders.dec)` (선례 `order_service.py:432`).

★**`_gauge_safely` / `_observe_safely` 를 만들지 않았다** — 본문 0줄 래퍼(shallow module)다.
★`_count_safely`/`_touch_safely` 를 `live_signal.py` → `common/metrics_multiproc.py` 로 **이동**(복제·rename 없음).
순환 import 없음을 독립 확인했다 — `metrics_multiproc` 는 `src.*` 모듈 수준 import 가 **0**이고
실패 counter 는 `except` 안 **지연 import** 다.

**감싸지 않은 것과 그 이유** — `order_service.py` 10곳(blast radius 0) ·
`trading.py` closed_pnl 7곳(`backfill_exchange_realized_pnl` 이 `realized_pnl_synced_at IS NULL` 을
갖고 있어 재시도가 `already_synced` 로 **수렴**한다. DB 는 이미 정확하고 귀결은 거짓 알림 1건) ·
나머지 ~139곳. ⇒ [BL-580] 로 등재하고 **승격 규칙을 사전등록**했다:
`qb_metrics_mutation_failed_total` 의 **창 차분이 0 을 벗어나는 순간** 즉시 승격(절대값 아님).

---

## 4. ★★★zone 을 추론하려다 세 번 다른 답을 얻었다 — 그리고 포기했다

「머니-패스 zone」을 AST 로 추론하려 했다. 정의를 조금 바꿀 때마다:

| 시도 | 정의                                  | 선택                                                                                                                       |
| ---- | ------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| 1    | 앵커(`commit`) 이후 성공 경로 꼬리    | **20곳** (거절·취소 경로 과선택)                                                                                           |
| 2    | 「첫 훅보다 lexically 앞」            | **6곳** ← ★**거짓.** 프로토타입에 박아둔 **임의의 40줄 창**이 필터 노릇을 하고 있었고 나는 그걸 잊었다. 창을 빼면 **13곳** |
| 3    | `commit`×`hook` 최근접 공통 블록 사이 | **14곳**                                                                                                                   |

★★★**내가 넣은 상수가 결론을 만들고 있었다.** 「정확히 Tier2 만 선택된다」는 문장은 그 창의 산물이었다.

⇒ **추론을 폐기했다.** 래칫은 2층이다 — **census 천장**(`(파일, metric)` 45키 · 159→141, 정확 일치)이
**신규 유입을 차단**하고, **손으로 동결한 보호 목록**이 **무엇을 왜 지키는지**를 남긴다.

★**보호 목록도 한 번 틀렸다** — 처음엔 「이 함수 안에 그 metric 의 가드 밖 0」으로 썼다가 red.
`_reconcile_conditional_entries` 안의 `qb_live_conditional_guard_total` 은 **9곳**이고 이번에 감싼 것은
발주 직후 1곳뿐이다. `(파일, 함수, metric)` 삼중항은 **과선택한다** — zone 추론과 같은 병을 다른
경로로 다시 밟았다. 지금은 테스트 docstring 이 **집행하는 것과 하지 않는 것**을 정확히 적는다.

★**「머니-패스는 구문에서 추론할 수 없다」가 이 회차의 산출물 중 하나다.**

---

## 5. [BL-576] 유도 발화 soak — 판정 **부분(Partial)**

창 `01:17:11Z ~ 01:20:47Z`. 착수 `FLAT=YES`(raw HMAC) · 워커 sentinel 4/4.
**T0 에 13 series 전건 존재** ⇒ 전 reading `CounterBasis.delta` (S4 ✓ · S5 ✓).

**Phase 2 — 거래소에서 조건부 주문 하나만 직접 취소(원장 불변), 나머지 하나는 내부 대조군.**

| 축                        | 값                   |
| ------------------------- | -------------------- |
| 취소 → 발화               | **10초**             |
| 로그 tally : counter 차분 | **1 : 1** ✅         |
| ★대조군(취소 안 한 주문)  | **0 발화** ✅ 판별력 |

★★**계획에 없던 소득** — 이 발화는 `_count_safely` 를 거친다. 그 헬퍼를 이번 회차가 **다른 모듈로
옮겼다.** 프로덕션에서 counter 가 정확히 1 오른 것은 **이동이 런타임 동작을 보존했다는 실주행 증거**다.

**Phase 3/4 는 하지 않았다(판단).** `hedge_mode` 는 리컨사일러가 즉시 재등재하는 주문을 걷어내고
**두 exchange_account 가 공유하는** 계정의 position mode 를 바꿔야 해서, 계정에 잔여를 남길 위험이
+1 series 의 가치를 넘었다. `breach_exceeds_cap` 은 확률적이라 사전등록이 미발화를 실패로 세지 않는다.

**사전등록 사다리** — ①계측 실격 ✗ → ②표본 없음 ✗ → ③`S1 그리고 S2` ✗ → ④`S1 또는 S2` **✅ 부분**.
⇒ 도달 가능 6종 중 프로덕션 확인 **2 → 3**(목표 4에 **1 부족**).
★**목표 미달을 「통과」로 쓰지 않는다.** 사다리가 이 조합을 덮었다 — 규율 2b 가 실제로 작동했다.

---

## 6. `/metrics` — 측정만, 수리 없음 (사용자 확정)

| 축           | 값                                  |
| ------------ | ----------------------------------- |
| 파일 수      | 9825 → **10277** (회차 중에도 증가) |
| 용량         | **635MB** (여유 125G)               |
| distinct PID | **1968**                            |
| 최초 파일    | 2026-07-28                          |

증가 드라이버는 **PID churn**(dev `--reload`·watchfiles)이지 컨테이너 churn 이 아니다.
**counter 파일 삭제는 금지** — `entry_completeness.py:64-78` 이 재기동 생존을 전제로 창 차분을 잰다.
⇒ [BL-581] 로 등재.

★**착수 때 내가 「로컬 API 는 단일 프로세스 모드」라고 적었는데 거짓이었다** — `Makefile:163-166` 이
`PROMETHEUS_MULTIPROC_DIR` 와 `QB_METRICS_ROLE=api` 를 직접 주입한다. `.metrics` 의 `api-*` 1489 파일이
그 증거였는데 나는 `.env.local` 만 보고 **Makefile 을 안 봤다**. codex G1 이 잡았다.

---

## 7. ★★게이트가 두 번 「동결 불가」를 냈다 — MAJOR 8건 전건 인정

**구현 전에** codex G1 을 2회 돌렸다. 1차 MAJOR 5건, 그 5건을 고치자 2차가 **새 MAJOR 3건**을 냈다.
전건 코드 재현 판정했고 **액면 수용 0**.

가장 값진 것 셋:

1. **내 고장 주입 테스트가 실 DB 로 갈 뻔했다** — `_async_place_trailing_stop` 은 엔진과 provider 를
   **내부에서** 만든다. 루프만 바꾸는 내 계약으로는 fake provider 가 안 쓰이고, codex 샌드박스는
   5433 에 못 붙으니 **조용히 실패**했을 것이다.
2. **`_reconcile_orders` 라는 함수는 없다**(`Reconciler.run`). 내 zone 표가 없는 함수를 가리켰다.
3. **Slice 1 과 Slice 3 이 동시에 성립 불가** — 천장을 159 로 고정하면서 수리로 site 를 없애면 모순.
   ⇒ 천장을 2단으로 갈랐다(Slice1 = HEAD 159 green / Slice3 커밋에서 149 갱신).

★**전부 코드를 쓰기 전에 나왔다.** 코드 뒤였으면 전부 재작업이다.

---

## 8. 표적 변이 — CONTROL 직접 집행

`git checkout` 금지 · 문자열 치환 + **sha256 복원 대조**(전건 일치).

| 변이                                                    | 결과                                                               |
| ------------------------------------------------------- | ------------------------------------------------------------------ |
| **M1** `trading.py:1295` 를 raw 로 되돌린다             | ✅ **5개 검사가 동시에 red** (천장 · 보호목록 · 공허성 · 행동 2건) |
| **M5** 아무 파일에 가드 밖 `.inc()` 1줄 추가            | ✅ 천장 red (신규 유입 차단)                                       |
| **M6** census 에서 「이름으로 넘긴 중첩 def」 처리 제거 | ✅ 천장 red (`trading.py` 38→39)                                   |
| **N1** 주석만 교체                                      | ✅ green 유지 (과민 아님)                                          |
| **N2**(=M5 겸함) 비-머니-패스 파일에 추가               | ✅ 천장은 red · 보호목록은 green — **두 단언이 다른 것을 잰다**    |
| **N3** 거래소 쓰기 **전** counter 주입                  | ✅ 동작 불변                                                       |
| **N4** 진짜 `ProviderError` + `failed` 라벨만 폭발      | ✅ retry 유지 — **진짜 실패까지 삼키지 않았다**                    |

★**M6 은 내가 실제로 밟은 오류를 변이로 고정한 것**이다(1차 census 가 160 을 냈다).

---

## 8b. ★★★G6 적대 리뷰 — MAJOR 3건, **둘이 내 것**이었다

**전건 코드 재현 판정. 액면 수용 0.**

| 등급                  | 지적                                                                                                                                                                   | 판정 · 처분                                                                                                                                                                |
| --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **MAJOR (Spec)**      | **같은 결함이 내가 이미 고친 파일 안에 남았다** — raw `qb_active_orders.dec()` 등                                                                                      | ✅ **재현. 게다가 codex 가 말한 것보다 나쁘다** — 4곳은 `commit()` **앞**이고 except 가 `rollback()` 한다 ⇒ 계측 예외가 **terminal DB 전이를 되돌린다**. **8곳 추가 수리** |
| **MAJOR (Standards)** | **CONTROL 보호 테스트가 우회 가능** — `metric in ast.unparse()` 문자열 포함이라 같은 함수의 **다른** mutation 을 감싸면 통과. `_PROTECTED_SITES = ()` 도 **공허 통과** | ✅ **재현. 내 코드다.** AST 구조 매칭으로 교체 + 목록 비었으면 실패하는 assert. **변이 M10 으로 red 확인**(전엔 green)                                                     |
| **MAJOR (Spec)**      | 「독립 CONTROL 오라클」 주장이 구현으로 뒷받침되지 않는다                                                                                                              | ✅ 인정 — 위 수정으로 해소                                                                                                                                                 |
| MINOR                 | census 가 **eager `.labels()`** 를 guarded 로 오분류 (`record_metric_safely(qb_x.labels(a="b").inc)` 는 `.labels()` 가 **먼저** 실행돼 예외가 탈출)                    | ✅ 인정 — 현 트리에 그 형태 **0곳**. **한계로 기록**(§11)                                                                                                                  |
| MINOR                 | 문서 미갱신 (Atomic Update)                                                                                                                                            | ✅ 이 커밋에서 해소                                                                                                                                                        |
| LOW                   | docstring 이 **없는 테스트 이름**을 근거로 든다                                                                                                                        | ✅ 수정                                                                                                                                                                    |

★★★**커밋 메시지의 포괄 주장이 거짓이었다** — `4eba9118` 에 「성공한 거래소 쓰기가 더는 오기록되지
않는다」고 썼는데 **성립하지 않는다.** 좁은 주장(「정확히 10곳을 감쌌다」)만 참이었다.
codex 가 그 구분을 정확히 짚었고, 후속 커밋에서 **주장을 축소**했다.
**「고쳤다」와 「그 종류를 다 고쳤다」는 다른 문장이다.**

★**내 하드닝 테스트가 즉시 값을 했다** — 보호 목록에 `qb_live_conditional_cancelled_total` 을
적어놓고 **실제로는 안 감쌌던 것**을 그 자리에서 잡았다.

---

## 9. ★★내 계측기가 이 회차에 **다섯 번** 틀렸다 (전부 남이 잡았다)

| #   | 무엇                                                | 누가                     |
| --- | --------------------------------------------------- | ------------------------ |
| 1   | census 160 — 중첩 `def` 가드를 못 봤다              | 서브에이전트 표와 대조   |
| 2   | zone 1차 정의가 20곳 과선택                         | 내 프로토타입 재측정     |
| 3   | **R4 「로컬 API 단일 프로세스」** — Makefile 미확인 | **codex G1**             |
| 4   | **zone 2차 「6곳」이 임의의 40줄 창의 산물**        | **codex G1 2차**         |
| 5   | 보호 목록 `(파일,함수,metric)` 과선택               | **내가 쓴 테스트가 red** |

★그리고 **여섯 번째는 절차 위반**이다 — 표적 변이를 **전체 pytest 와 동시에** 돌렸다.
테스트 DB 는 `quantbridge_test` 하나이고 세션 픽스처가 `drop_all + create_all` 을 돈다.
**레포가 이미 문서화한 함정**이다. 그 실행 결과를 **폐기하고 단독으로 재실행**했다(3835 passed).

---

## 10. 게이트

| 축                                 | 착수              | 종료                                                     |
| ---------------------------------- | ----------------- | -------------------------------------------------------- |
| BE pytest                          | 3820 / 46 skipped | **3835** / 46 (**+15** = 추가한 테스트 수와 정확히 일치) |
| FE vitest                          | 1242 (205)        | 1242 (변경 없음 — FE diff 0)                             |
| ruff · mypy                        | clean · 214       | clean · **214**                                          |
| 마이그레이션 head                  | `20260801_0001`   | **동일 (+0)**                                            |
| 가드 밖 mutation                   | **159**           | **141** (독립 오라클로 재확인)                           |
| `qb_metrics_mutation_failed_total` | 0                 | **0**                                                    |

---

## 11. 정직하게 남긴 것

- ★**Tier2 6곳 중 행동 테스트는 2곳뿐**(`state_handler:135` · `trading:496`). 나머지 4곳은 테스트
  파일이 **실 DB 의존**이라 **존재 증명(census)만** 있다. 「전건 검증됨」으로 쓰지 않는다.
- ★**soak 목표 미달** — 도달 가능 6종 중 3종만 프로덕션 확인. `hedge_mode` 미유도(판단), `degraded_input`
  은 **제3자 API 남용 없이는 유도 불가**라 아예 제외했다.
- ★**`qb_metrics_mutation_failed_total` 은 여전히 0** — 이 회차는 **재현으로** 결함을 증명했지
  프로덕션 발생을 관측한 것이 아니다.
- ★**census 규칙이 못 잡는 것** — 별칭(`c = qb_x; c.inc()`) · `getattr` 동적 접근 · 모듈 alias.
  테스트가 이 사각을 **명시적으로 단언**한다.
- ★**규칙 (b)(무증분 `.labels()`)는 현 트리에 0 site** — synthetic 픽스처로만 검증된다. 판별력 없음.
- ★**디스크 full 의 mmap write 는 `SIGBUS`** 라 `record_metric_safely` 도 못 잡는다. 가드는 만능이 아니다.
