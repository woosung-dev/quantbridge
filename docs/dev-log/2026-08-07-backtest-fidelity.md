# 2026-08-07 — backtest-fidelity

> 함대 3워커(codex:btgen · claude:bteval · claude:canon) + CONTROL. 브랜치 `stage/backtest-fidelity`.
> 소크 **무중단**(오라클 A1). `backend/src` 는 주석 1블록만 바뀌었다(행위 0줄).

---

## 0. 한 줄

**「백테스트가 현실을 얼마나 예측하나」의 첫 숫자를 만드는 회차였고, 만들어진 것은 숫자 하나가 아니라
「그 숫자를 만드는 기계」와 반증 4건이다.**

---

## 1. 갈래 A-1 — 조건부 진입 체결 귀속 일치율 (첫 측정)

### 1.1 판정식 (착수 전 CONTROL 동결 · §G1.1 규율 7항 대조)

관측량 = 서버 `~/quantbridge/backend/.metrics` 의 `qb_live_conditional_fill_ownership_total{outcome}`.
취득 = CONTROL 이 ssh(`bash -lc`)로 prometheus 원문을 **파일로 동결** → 스크립트가 그 파일 2벌을 읽어 판정.

```
Δ(label) = v_T1(label) − v_T0(label)          ★절대값 금지(mmap 이 워커 재기동을 넘어 산다)
N = Δagree
D = Δagree + Δengine_only_suppressed + Δledger_only_adopted
A = N / D   ·   형A = Δengine_only_suppressed / D   ·   형B = Δledger_only_adopted / D
```

**분모 밖 3종과 그 이유** — `ledger_only_orphan`(pending 이 없어 엔진이 판단할 대상 자체가 없다·무동작) ·
`ledger_fill_out_of_window`(300봉 창 밖이라 엔진의 답이 존재하지 않는다) ·
`ledger_unreadable_fallback`(원장을 못 읽어 엔진이 단독 판정했다 — 대조가 없었다).

**게이트(순차. 위가 이긴다)**

| #   | 조건                                 | verdict       | reason                                                 |
| --- | ------------------------------------ | ------------- | ------------------------------------------------------ |
| 1   | 어떤 라벨이든 `Δ < 0`                | `held`        | `counter_reset_detected`                               |
| 2   | `Δother > 0` 또는 알려진 7종 밖 라벨 | `held`        | `unknown_label_present`                                |
| 3   | `D < 30`                             | `undecidable` | `sample_below_threshold` — rule of three(`3/30 = 10%`) |
| 4   | 그 외                                | `measured`    | `ok`                                                   |

★**합격/불합격 라벨을 붙이지 않는다.** `measured` 는 「값이 나왔다」이지 「통과」가 아니다 —
기준선이 없는 상태에서 문턱을 붙이면 그 문턱이 근거 없이 정본이 된다.

### 1.2 실측

<!-- A1-RESULT-START -->

(측정 중)

<!-- A1-RESULT-END -->

### 1.3 ★해석 규율

이 값은 **조건부 진입 체결 축 하나**다. 낮다는 것은 「백테스트 산식이 틀렸다」가 아니라
**엔진 시뮬과 거래소 매칭엔진이 갈리는 빈도**다 — [ADR-025] 가 원장에 체결 권한을 준 자리를 센다.
백테스트↔현실 3축 중 비용 축은 [BL-603] Resolved(왕복 0.300%→0.138%), 진입 집합 축은 장전봉 정렬
90%(진짜 미예측 2/90)로 수치화됐고 **이 회차가 세 번째 축을 채운다.**

---

## 2. 갈래 A-2 — [BL-621] 부검이 ⑴로 닫혔다

### 2.1 판정 — **두 겹이었다**

| 조합                 | total_return                  | max_drawdown               | win_rate          |
| -------------------- | ----------------------------- | -------------------------- | ----------------- |
| 현행 ATR + 현행 비용 | -0.00075225935038332512252    | -0.0019096436091021742     | 0.2857142857…     |
| 현행 ATR + 구 비용   | -0.000979728462339637565      | -0.0020657351891689804     | 0.2142857142…     |
| 구 ATR + 현행 비용   | -0.00014915585433282336549    | -0.001306871746680595      | 0.2857142857…     |
| **구 ATR + 구 비용** | **-0.0003771138174282226845** | **-0.0014634176774924912** | **0.2142857142…** |

마지막 줄이 `expected.json` 과 **4지표 전건 byte-identical** 이다.
방법 = `stdlib.ta_atr` monkeypatch(체크아웃 없이 그 한 함수만 `cda575f2^` 구현으로) + 구 비용 config.

**원인 ⑴** `cda575f2`(2026-06-30, [BL-378] `ta.atr` rolling SMA → Wilder RMA). 그 커밋은
`stdlib.py`·trust-layer `baseline_metrics.json`·`test_stdlib.py` 를 건드렸고 **이 골든은 안 건드렸다.**
**원인 ⑵** [BL-603](2026-08-07) 비용 기본값 인하. 역시 이 골든을 재생성하지 않았다.

### 2.2 ★왜 아무도 못 봤나 — 판별력 0 인 값 하나만 보고 있었다

`test_golden_backtest.py` 가 assert 하던 것은 `status` 와 `num_trades >= 0` 둘뿐이었고,
`expected.json` 의 `backtest.metrics` 6키·`entries_indices`·`exits_indices` 는 **어느 것도 비교되지
않았다.** 그리고 **`num_trades` 는 위 네 조합 전부 14** 다 — 유일하게 보던 값이 이 축에서 판별력 0 이었다.

값이 마지막으로 갱신된 커밋은 `80a2138e`(2026-06-26)이고, 그 뒤 유일한 수정 `b97ac578`(2026-07-26)은
**`sharpe_ratio` 와 `description` 만** 바꿨다. 손익 3지표는 **2026-06-26 이후 동결**이었다.

### 2.3 수리

- `backend/scripts/regen_golden.py` **신설** — `--confirm` 없으면 exit 1 + 파일 0개 기록 ·
  `--case <id>` · `--check`(의미 비교, 차이 시 exit 1, 파일 미기록). 케이스 발견은
  `test_golden_backtest.py:_discover_cases()` 를 **재사용**한다.
- `expected.json` **재생성** — `BacktestMetrics` 스칼라 전량 + 리스트 3종 `metrics_list_digests`.
- `test_golden_backtest.py` **smoke → 실제 오라클로 승격**.
- [BL-586] — `regen_trust_layer_baseline.py` 의 `metrics_dict`·`_trade_to_dict` 를 **하드코딩 키
  리스트에서 `dataclasses.fields()` 자동 유도로 교체**. 이것이 수리의 핵심이다 — 키를 손으로 적으면
  다음에 `types.py` 에 필드가 늘어도 여기가 안 늘어난다.
  ★실측: `RawTrade` 22 필드는 **리스트/dict 형이 0개**라 digest 설계가 필요 없었다.
  BL 본문이 예상한 「리스트 직렬화 설계 선행」은 metrics 쪽에만 해당했다.

---

## 3. 갈래 B — 캐논↔실화면 첫 대조 + B2 팔레트

### 3.1 ★★★반증 1 — `runtime-check.mjs` 는 기동조차 못 하고 있었다

착수 지시는 이 도구를 「캘리브레이션된 임계값을 물려받는 자산」으로 전제했다. 실제로는 `docs/` 재편
커밋 `fcc36bf7` 이 파일을 두 단계 깊은 곳으로 옮겼는데 playwright import 의 상대 깊이가 안 따라와
**`ERR_MODULE_NOT_FOUND` 로 즉사**하고 있었다.

⇒ **핸드오프 §8.5 의 「다크 17벌 17/17 PASS」는 그 커밋 이후 한 번도 재현된 적이 없는 숫자였다.**
깊이를 고쳐 17/17 을 실제로 재현했고(CONTROL 재측정 exit 0) 그것이 회귀 기준선이다.
뿌리는 경로가 아니라 **소유자 부재**다 — 어느 게이트도 이 도구를 부르지 않는다(신규 **[BL-631]**).

### 3.2 ★★★반증 2 — 라이트 테마가 WCAG AA 미달로 배포돼 있었다

같은 도구·같은 라우트·같은 서버에서 **팔레트만 되돌려** 재측정했다(측정 후 SHA-256 해시 대조로 원복 확인).

| 공개 라우트 라이트 | 구팔레트 AA 하드 실패 | B2    | 구팔레트 canon | B2     |
| ------------------ | --------------------- | ----- | -------------- | ------ |
| `/`                | **40**                | 0     | 98             | 18     |
| `/sign-in`         | **16**                | 2     | 17             | 10     |
| `/pricing`         | **38**                | 0     | 96             | 24     |
| `/waitlist`        | **22**                | 0     | 58             | 16     |
| 합계               | **116**               | **2** | **269**        | **68** |

남은 2건은 Clerk 위젯의 "Development mode" 배지(2.98:1)로 **dev 키에서만 붙는 서드파티 요소**다.
**앱이 만든 하드 실패는 0.** 토큰 단위로도 같다 — `--text-muted` on `--bg-alt` **4.49(AA 미달) → 5.60** ·
`--warning` on `--warning-subtle` **4.30(AA 미달) → 5.66**.

⇒ **B2 는 「탁해지는 대가로 캐논을 산다」가 아니라 AA 미달 116건을 없앤 교체였다.**
인터뷰에서 이 안을 고를 때 우리가 알던 대가(④ 생동감 포기)는 맞지만, **얻는 것은 캐논이 아니라 접근성**
이었다. 이 사실은 **앱을 겨눠보기 전에는 레포 어디에도 없었다** — 프로토타입만 재던 도구가 라이트 앱을
한 번도 안 봤기 때문이다.

### 3.3 대조 표 — 측정 16라우트

인증은 새 우회를 발명하지 않고 기존 e2e 경로를 재사용했다(`playwright.config.ts` setup 프로젝트 →
`@clerk/testing/playwright` → `e2e/.auth/storageState.json`). 동적 라우트 3종은 격리 DB 에서 로그인
사용자 소유 레코드의 실제 id 로 쟀다.

| 집합                  | 라이트 canon | 다크 canon | 하드 실패                        |
| --------------------- | ------------ | ---------- | -------------------------------- |
| 공개 4(프로덕션 빌드) | 68           | 24         | 2 (Clerk dev 배지)               |
| 인증 12(dev 서버)     | 171          | 255        | 3 (`nextjs-portal` dev 오버레이) |
| **전체 16**           | **239**      | **279**    | 앱 생성 **0**                    |

수용 기준 「라이트 canon ≤ 다크 canon」은 **인증 12종 충족 · 전체 16종 충족 · 공개 4종 미충족**이고,
미충족의 **단일 원인**은 마케팅 푸터 법적 고지(`--warning` on `--warning-subtle` = 5.66)다(신규 **[BL-628]**).
canon 은 하드 실패가 아니라 지표이고 그 자리는 AA 를 통과한다 — **기준을 낮추지 않았고 색도 더 손대지 않았다.**

★대조 표는 `docs/reference/design/prototypes/shotgun-2026-07/checklist.md` 에 **3열(측정 축 / 사람
판단 축 / 미측정)로 분리**해 적었다. 한 줄로 합치면 다음 사람이 「검증 완료」로 읽는다.

### 3.4 부수 — 동기화 계약을 집행하는 테스트가 **없었다**

`brand-palette.ts:7-8` 이 "globals.css 의 `:root`/`.dark` 값과 항상 같은 커밋에서 함께 변경" 을
명문화하고 있는데 그것을 집행하는 테스트는 레포에 **없었다.** 인접 둘은 다른 것을 본다 —
`design-canon-tokens.test.ts` 는 `.dark` 만, `chart-tokens-contract.test.ts` 는 변수의 **존재**만.
`design-canon-source.test.ts` 는 `brand-palette.ts` 를 하드코딩 스캔에서 **아예 제외**한다.
신규 `brand-palette-css-sync.test.ts`(5 테스트)를 만들었다.

★**KITPORT 무결성 가드는 이번 변경에 아무 말도 하지 않았다** — 라이트 `:root` 는 센티넬 구역
(983–1885) **밖**이다. 즉 **라이트 `:root` 를 지키는 가드는 이번에 새로 만든 이 테스트가 유일하다.**

---

## 4. 절차에서 나온 것

### 4.1 ★Evaluator 분리가 값을 했다

`claude:bteval` 은 `btgen` 의 코드를 **한 줄도 보지 않고** 계약만으로 시험 17개를 썼다
(`wt/btgen` fetch·체크아웃·열람 0회, 스크립트 2종은 부재만 확인).
판별력은 **스크래치패드에 참조 구현을 만들어** 쟀다 — **변이 24/24 판별 · 음성 대조 2/2 green**.

★그중 결정적인 것 하나: 게이트 **순서**만 뒤집은 변이(`gate_order_swapped`)는 다른 9개 시험을 전부
통과하고 **순서 전용 시험 하나만** 잡았다. 그 시험이 없었으면 순서 위반이 조용히 통과한다.
★`threshold_le`(D=30 이 red)와 `no_threshold`(D=29 가 red)가 **서로 다른 방향**으로 잡힌 것이
경계 2건 원칙이 작동했다는 증거다.

### 4.2 ★통합에서만 나온 실패 1건 — 워커별 green 의 합은 통합 green 이 아니다

`test_baseline_field_coverage.py` 2건이 통합 브랜치에서 `i3_drfx: metrics 가 dict 가 아니다
(got NoneType)` 로 죽었다. 원인 = `i3_drfx` 는 **코퍼스 skip 대상**(Y1 Coverage reject)이라 baseline 에
`metrics: null` 로 들어간다. bteval 의 워크트리 baseline 은 **구 스키마**라 이 조합을 만들 수 없었다.

CONTROL 이 손으로 풀지 않고 **그 워커에 재지시**했다. 재지시에 규율을 하나 얹었다 —
「None 이면 그냥 넘긴다」로 쓰면 구현이 **모든** 코퍼스를 None 으로 만들어도 통과한다(항진명제화).
그래서 제외 근거를 `SKIPPED_CORPUS` **SSOT 로 대조**하고 「metrics 를 실제로 가진 코퍼스가 0이면 실패」
음성 대조를 함께 넣게 했다.

### 4.3 ★codex 워커는 구현을 끝내고도 커밋을 못 한다

`btgen` 은 6파일을 전부 구현·스테이징하고 `blocked` 를 올렸다 — 커밋이 메인 `.git/worktrees/btgen/
index.lock` 을 만들어야 하는데 그건 **워크트리 밖**이라 샌드박스가 `Operation not permitted` 로 막는다.
CONTROL 이 대신 커밋했다. **이 갈래는 앞으로도 재현된다** — codex 워커에게는 「커밋까지」가 아니라
「스테이징까지」가 계약이어야 한다.

### 4.4 ★★BL 번호 예약이 착수 시점에 이미 낡아 있었다

착수 지시가 예약한 `626~633` 은 **로컬 미푸시 브랜치 `stage/soak-unattended-watch` 가 이미 BL-626 을
쓰고 있어** 충돌 상태였다. 전 브랜치(로컬+원격)에서 `BL-6xx` 최대값을 훑어 **627~631 로 재예약**했다.
회차 도중 그 브랜치가 PR #559 로 **머지되어 `origin/main` 이 전진**했고(`0c75aaa3` → `770d0f38`),
통합 지점을 새 main 으로 올린 뒤 squash merge 했다.
★그 사이 워커 브랜치의 `origin/main..HEAD` diff 가 **내가 지시하지 않은 삭제**를 보여줬는데,
그건 워커의 잘못이 아니라 **낡은 base 와의 diff** 였다. 커밋 자체(`git show --stat`)로 확인해야 한다.

### 4.5 ★내 preflight 발화율 추정이 1.8배 낙관이었다

착수 전 절대값/경과로 **25/h** 를 추정하고 「문턱 30 을 1.2h 면 넘긴다」고 적었다. 실제 창에서 측정한
발화율은 **13.9/h**(12건 / 0.865h) 였고 문턱 도달은 **2.2h** 다. 절대값을 경과로 나눈 값은 그 사이의
**혼잡 구간을 포함**하고, 창 내 실측 차분과 같지 않다.

---

## 5. 게이트

| 게이트                                          | 결과                                  |
| ----------------------------------------------- | ------------------------------------- |
| BE `pytest` (전체)                              | **4413 passed / 45 skipped**, exit 0  |
| FE `pnpm test`                                  | **208 files / 1265 tests** 전건 green |
| `runtime-check.mjs` (인자 없이, CONTROL 재측정) | **17/17 PASS**, exit 0                |
| `scripts/bl-audit.sh`                           | 3면 정합 · active 176 / 전체 189      |
| `scripts/docs-audit.sh`                         | clean (링크 · 폐기 경로 · 줄 길이)    |
| `scripts/soak-gate.sh` (서버)                   | <!-- SOAK-RESULT -->                  |

★`docs-audit` 가 내 편집에서 2건을 잡았다 — `[BL-603](2026-08-07)` 이 **마크다운 링크로 파싱**된 것과
BL-586 인덱스 행 1090자(상한 1000). 둘 다 이 회차에 내가 만든 것이다.
★`bl-audit` 는 **3면째(로드맵 체크박스)** 를 잡았다 — 섹션과 인덱스만 고치고 로드맵을 빼먹었다.

---

## 6. 신규 BL

| ID           | 무엇                                                                                                         |
| ------------ | ------------------------------------------------------------------------------------------------------------ |
| **[BL-627]** | `regen_golden.py` 에 출력 경로 리다이렉트가 없어 라운드트립 시험이 실제 `expected.json` 을 덮어썼다 복원한다 |
| **[BL-628]** | 마케팅 푸터 `--warning` on `--warning-subtle` = 5.66 — 공개 라우트 라이트 canon 미충족의 단일 원인           |
| **[BL-629]** | `--chart-axis` 데드 토큰(아무도 안 읽어서 다크값이 구값에 남아 있다)                                         |
| **[BL-630]** | `.pos`/`.neg` **단독**은 여전히 `td` 색에 진다(관례가 지키고 있을 뿐 규칙이 아니다)                          |
| **[BL-631]** | `runtime-check.mjs` 가 어느 게이트에도 안 붙어 있어 죽은 채 방치됐다                                         |

**Resolved** — [BL-621] · [BL-586] · [BL-022].
