# status-triage-mass (2026-08-09)

> `docs/status.md` 에서 강등된 회차 기록이다(2026-08-10 docs-context-diet).
> 강등 이유는 크기가 아니라 **역할**이다 — 끝난 회차의 회고는 진입점이 아니다(ADR-026 §3).
> 원문 위치 = `git show 762e1297:docs/status.md` 의 373-395행.

---

### ★status-triage-mass (2026-08-09) — 내 판정 6건이 반박됐다

| 무엇이 반박됐나                   | 누가      | 어떻게                                                                                                                                                                                      |
| --------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ⓪ 표 G 의 전제                    | 실측      | 「닫혔다고 선언되지 않은 수」가 **거짓** — 116건 중 100건이 진짜 열려 있다(r=5.17%)                                                                                                         |
| **표본 20건 사전등록**            | 실측      | 층화 20건이 **0/20** 인데 전체는 **6/116**. 20건은 5% 를 0% 와 **구분할 검정력이 없다** — 임계(≥40/20~40/<20%)를 20건으로 재는 설계 자체가 결함이었다                                       |
| `target-gone` 판정 (BL-396)       | codex     | 원장 CSV 는 `runup_abs`·`cumulative_pnl`·`fee_paid` 등 5컬럼을 내는데(`utils.ts:100-130`) 서브페이지는 3종뿐(`trade-detail-table.tsx:414-419`) ⇒ still-open                                 |
| `mostly-done` 잔여 서술 (BL-520)  | codex     | 「AGENTS.md 등재뿐」이 **거짓** — `live_signal.py:4180` 의 `sweep_filled.inc()` 가 래퍼 밖이라 metric 실패가 **filled 성공을 `sweep_cancel_failed` 로 뒤집는다**(아래 `dec` 는 감싸져 있다) |
| ★**헤드라인 숫자 자체** (r=6.9%)  | 2축 리뷰  | BL-396 을 정정한 뒤 **합계를 다시 계산하지 않았다**(`100+8+6+3=117≠116`). **판정을 바꾸면 합계를 다시 계산한다** — 이 회차가 가장 자랑한 반증이 자기 헤드라인에 전파되지 않았다             |
| `already-done` (BL-477)           | Spec 축   | [BL-605] 상태줄이 **「기존 574행은 그대로 둔다」**고 명시 — RESOLVED 는 **신규 적재만** 덮는다. [BL-529] 와 같은 현상인데 판정이 갈렸다 ⇒ **부분 해결**                                     |
| 판정문 사실 오류 2건 (BL-507·394) | vercel 축 | `collapseRows` 는 view 안이 아니라 **모듈 레벨**(`:56`)이라 「분리」 처방은 성능을 안 바꾼다 · `hooks.ts:125-158` 은 이미 **first-page-then-`Promise.all`** 이라 「순차 루프」가 거짓       |

★★**음성 대조가 이 회차를 살렸다** — 0/20 은 「다 열려 있다」와 「판정식이 `already-done` 을 못 낸다」를
구분하지 못한다. 종결 **이전** 시점의 섹션 텍스트(git history)를 번호를 가려 태우자 정답이 나왔고
(근거 4건이 코드), 그제서야 0/20 이 실측이 됐다. **판별력을 재기 전의 0 은 0 이 아니다.**
★단 **5건이 전부 양성 라벨이라 민감도만 쟀고 특이도는 안 쟀다** — 「진짜 열린 것을 열렸다고 하는가」는
미측정이다. 종결탐지 기준으로 세면 **4/5**(CAND-2 는 `mostly-done`).

★**zsh 함정 2건** — ⑴ `for x in $VAR` 는 `SH_WORD_SPLIT` 이 꺼져 있어 116단어를 **한 단어**로 돈다
(grep 이 개행 문자열을 다중 패턴으로 읽어 항상 1 → 「표 행 1/116」이 실제 **46/116**이었다).
⑵ **`${PIPESTATUS[0]}` 가 빈 문자열**이다(zsh 는 소문자 `pipestatus`) — 게이트를 파이프에 넣으면
exit code 가 가려진다는 규율이 **이 세션에서 실제로 발화했다**.
