# PREREG-AUDIT — 사전등록 문턱 · CONTROL 계측 감사 (W3, 2026-08-01)

> Evaluator 보고서. 판정식과 숫자만 검사했다. 코드 수정 0 · DB 쓰기 0 (SELECT 전용).
> 대상 HEAD = `f631f1c7`. 앱 DB = `quantbridge-db` / `trading.orders` (총 389행, 2026-07-26 03:00:44Z ~ 07-31 18:39:31Z).

---

> ### ❗ **사후 정정 (2026-08-01, CONTROL) — 이 문서의 층위1 값 `2.63%` 는 틀렸다**
>
> §D-3 의 「07-30~07-31 … has_fill **37** … **2.63%**」는 **`condmkt`(시장가 전환) 3건을 뺀 값**이다.
> 출시된 도구(`entry_completeness_report.py`)는 그 3건을 `has_fill` 에 포함해 **has_fill 40**,
> 즉 **`1/41 = 2.44%`** 를 낸다. 이 문서가 스스로 권고한 「코드의 `confirmed_rejection_rate` 를
> 그대로 채택하라」(§D-1)에 따르면 **2.44% 가 정본**이다.
>
> ★★**중요한 것은 숫자가 아니라 왜 안 걸렸는가다.** CONTROL 과 이 Evaluator 가 **둘 다 같은
> 술어(`trigger_price IS NOT NULL`)로 세어 같은 오답을 냈다.** 6/6 재현은 **독립 재현이 아니라
> 같은 실수의 재현**이었다 — 이 레포가 이미 배운 「나와 codex 가 같은 오답(둘 다 내부 증거만 →
> 독립 표본 아님)」과 같은 형태다. **가른 것은 세 번째 축(출시된 도구)뿐이었다.**
>
> 판정에는 영향 없다 — 2.44% 도 A4 의 10% 를 밑돌고 A3 은 채널 표 기준이라 불변이다.
> 아래 본문은 **이력 보존용으로 그대로 둔다.**

## 0. 한 줄 결론

**B1~B6 여섯 주장은 전부 재현됐다 — 숫자 하나도 안 틀렸다.** 그런데 **CONTROL 이 이번 회차의
진입점으로 지목한 `docs/status.md:24` 의 BL-536 전제가 틀렸고, 그 파일이 스스로를 반박한다**
(`:24` vs `:464`). 그리고 **몫 D 의 층위 1 정의 제안은 이미 출시된 구현과 분자·분모 둘 다 어긋난다.**

---

## 몫 A — 사전등록 문턱 문자 단위 대조

### A-1. 전사 정확성 = **일치**

`docs/status.md:47-53` 을 공백 정규화 후 CONTROL 전사본과 `diff` → **차이 0**.
5행 전부 글자 단위로 같다. **CONTROL 은 이번엔 느슨하게 옮겨 적지 않았다.**

### A-2. A4 는 AND 다 — 이번 회차 산출물에 OR 누출 **없음**. 단 **레포 이력은 OR 다**

이번 회차 산출물(내 보고서 포함)에 OR 누출 없음. 그러나 **과거 기록 4곳 중 3곳이 OR** 다:

| 위치                                           | A4(유지) 표현                                   | 결합    |
| ---------------------------------------------- | ----------------------------------------------- | ------- |
| `status.md:52` (**현행 정본**)                 | 유실률 ≥10% **그리고** 채널 분산 (두 근거 모두) | **AND** |
| `status.md:472` (07-29 판정표)                 | 유실률 ≥10% **이거나** 채널 분산                | OR      |
| `status.md:425` (07-29 개정표)                 | 유실률 ≥10% **이거나** 채널이 분산              | OR      |
| `dev-log/2026-07-30-…:36`                      | 유실률 ≥10% **또는** 채널 분산                  | OR      |
| `dev-log/2026-07-29-…:137` · `backlog.md:4364` | **그리고** — 두 근거 모두                       | AND     |

★**드리프트가 07-29 에 안 걸린 이유** — 그날은 두 근거가 **둘 다 참**이라(`둘 다 충족`)
AND/OR 이 같은 답을 냈다. **판별력이 0인 창에서 검증돼 통과한 것이다.**

★**이번 회차엔 판정이 갈린다.** 아래 §부록 재판정에서 AND 는 「축소」 단일 판정을 주고,
OR 는 **「축소 ✓ 이면서 동시에 유지 ✓」라는 모순**을 만든다. ⇒ **현행 AND 가 옳다. 유지하라.**
다만 위 3곳이 OR 로 남아 있는 한 다음 워커가 그걸 읽고 OR 를 적용할 수 있다 — **정정 대상이다.**

### A-3. A3 분모 = **채널 표의 합**. 내가 정한 게 아니라 **선례가 이미 정했다**

CONTROL 은 "지금 문서에 안 적혀 있다, 네가 정하라"고 했으나 — **적혀 있다. 두 곳에서.**

- `dev-log/2026-07-30-live-entry-completeness.md:35` — **「한 채널이 합의 ≥70%」** · 실측 `C2 9/12 = 75%`
- `status.md:471` — 「한 채널이 **합**의 ≥70%」 · 실측 `최대 C2 12/21 = 57%` (합 21 = C1 2+C2 12+C3 7)

두 번 다 분모가 **채널 표의 합**이고, 시도 수(12)가 아니다. A3 의 괄호 주석
「= 한 곳만 고치면 되는 상태」도 이 해석과만 맞는다 — _유실 총량 중 최대 채널의 몫_.

⇒ **A3 분모 = 채널 표의 합(C2 제외 시 C1+C3+C4+C5). 확정.** 시도 수 분모는 기각.

★**단 A3 은 작은 N 에서 무너진다.** C3=7 을 빼면 07-29 창의 합이 **2**, 현행 코드 창(07-30~31)은
**1** 이다. **단 1건짜리 채널이 자동으로 「100% ≥ 70% → 축소」가 된다.** A3 은 최소 N 하한이
필요하고, 그것을 대야 할 A5 에 **숫자가 없다**(아래).

### A-4. A2 의 「유실률」과 A3/A4 의 「유실률」은 **같은 값이 아니다** — 결함이다

- A3/A4 의 유실률 = 층위 1 시도 확정 거절률 = `2/(10+2)` = 16.7%. **C2 가 분자·분모 어디에도 없다.**
- A2 는 「**C2 제외** 유실률 0」이라 쓴다. C2 가 애초에 안 들어 있으므로 **「C2 제외」가 공허하다** —
  C2 를 빼도 값은 16.7% 그대로다. ⇒ **A2 는 C2 제거로는 절대 발화하지 않는다.**

원인은 §몫 D 의 status.md:24 오독이다. A2 를 쓴 사람은 유실률 분모가 C2 라고 믿었다.

⇒ **A2 를 다시 써야 한다.** 의도가 「C2 를 뺀 채널 표가 비면 소멸」이라면 그렇게 적어라:
**A2′ 소멸 = 층위1 유실률 0 그리고 C2 제외 채널 합 = 0.** (07-29 개정표의 「합 < 1/시간」
둘째 연언지가 현행 A2 에서 **소리 없이 빠진** 것도 같이 복원해야 한다.)

### A-5. (추가 발견) A5 는 **집행 불가능하다**

「표본이 문턱 미달이면」이라 쓰고 **그 문턱 숫자가 어디에도 없다.** A3 이 N=1 에서 100% 를
내는 지금, A5 는 반드시 정량화돼야 한다. 제안: **판정 종결 시도(has_fill+rejected_exchange) < 30
이거나 C2 제외 채널 합 < 5 이면 「판정 불가」.** (07-29 창은 12 · 현행 창은 38 이다.)

---

## 몫 B — CONTROL 숫자 독립 재현

★**받아 적지 않고 전부 직접 쟀다.** 결과: **6/6 재현. 반증 0.**

| #   | CONTROL 주장                                                 | 내 실측                                                                              | 판정 |
| --- | ------------------------------------------------------------ | ------------------------------------------------------------------------------------ | ---- |
| B1  | 진입 281 / 조건부 255 / 창 07-26~07-31                       | `reduce_only=f` = 26+255 = **281**, 조건부 **255**. 일자합 14+34+81+43+59+50=281     | ✅   |
| B2  | C1 일자별 10/20/2/0/1                                        | 07-27 **10** · 07-28 **20** · 07-29 **2** · 07-30 **행 없음(0)** · 07-31 **1**       | ✅   |
| B3  | 진입 부분체결 12건 전부 `trigger_price IS NULL`, 07-26·27 뿐 | 07-26 **9** · 07-27 **3**, **전부 cond=f**. 조건부 진입 부분체결 = **0**             | ✅   |
| B4  | 소크 창 청산측 부분체결 **7**, 진입측 **0**                  | `created_at` 기준 **7/0** · `filled_at` 기준 **7/0**                                 | ✅   |
| B5  | 7건 전부 `filled_quantity=0.029`, 미달 1.9~2.5%              | 전건 `0.02900000`, 미달 **1.886~2.547%**, 전부 `state=filled`·market·`reduce_only=t` | ✅   |
| B6  | 07-30~31 조건부 진입 106 (c68/f37/r1)                        | **cancelled 68 · filled 37 · rejected 1 = 106**                                      | ✅   |

### B4 흔들기 — 지시대로 실행했고, **우연이 아니다**

**(1) 두 시간축 모두 7.** `created_at` 7/0, `filled_at` 7/0. 우연이면 갈렸을 자리에서 안 갈렸다.

**(2) 창 이동 vs 확대 — 결정적.**

| 조작      | 청산/진입 | 조작      | 청산/진입 |
| --------- | --------- | --------- | --------- |
| −1h 이동  | 5/0       | 확대 ±15m | **7**/0   |
| −30m 이동 | 5/0       | 확대 ±30m | **7**/0   |
| −15m 이동 | **7**/0   | 확대 ±1h  | **7**/0   |
| 원본      | **7**/0   | 확대 ±2h  | **7**/0   |
| +15m 이동 | 6/0       | 확대 ±6h  | **7**/0   |
| +30m 이동 | 5/0       |           |           |
| +1h 이동  | 4/0       |           |           |

**이동하면 줄고(가장자리를 잘라서), 확대하면 ±6h 까지 7 로 고정된다.** ⇒ 7건이 전부 창 **안쪽**에
있고 근방에 추가분이 없다. 민감도의 방향이 「우연한 겹침」이 아니라 「경계에서의 절단」이다.
★**진입측은 12개 변형 전부에서 0 이다.** 귀속(=청산측)은 흔들리지 않는다.

**(3) 같은 창에 「7」을 내는 다른 술어 — 있다. 그리고 그게 귀속을 오히려 굳힌다.**
`close_filled` = **7** (청산 `state=filled`). 다른 집합이 아니라 **같은 7행**이다 —
그 창의 **청산 체결 전건이 원장상 「부분체결」로 보인다**. 즉 7 = 청산 체결 수 = 절삭 아티팩트 수.
(청산측 전체 9 = filled 7 + rejected 2, 그 2건이 dev-log 의 `110017` 2건과 일치.)

**(4) 기전이 코드에 이미 적혀 있다.** `backend/src/common/metrics.py:637-650`:

> 청산 수량은 엔진 값을 그대로 싣고 **거래소 눈금으로 절삭되지 않으므로, 전량 체결이어도**
> 거래소 확정 수량이 우리 요청값보다 작아 여기서 부분체결로 잡힌다. 조건부 진입은 반대다 —
> `conditional_entry_planner.plan_reconcile` 이 `qty_step` 으로 미리 정규화해 발주한다.

내 실측이 이 서술과 **정확히** 맞는다: 청산 7건 `quantity=0.0297…` → `filled=0.029`.
그리고 조건부 진입 부분체결은 **전 기간 0건**(B3). 예측과 관측이 양방향으로 일치한다.

**(5) 범위 확장 — 아티팩트는 소크 창만의 일이 아니다.** 전 기간 부분체결 65건 중
**63건이 `filled_quantity=0.029`**(나머지 2건 `0.030`). 진입측 12건도 전부 `0.029`,
미달 0.464~1.818% — **같은 절삭 아티팩트이고 전부 비조건부(시장가) 진입이다.**
⇒ **「C3 부분체결」은 소크 창뿐 아니라 어느 창에서도 진짜 진입 유실을 센 적이 없다.**

**결론 B: CONTROL 의 B4 귀속은 옳다.** 숫자 7 은 우연히 겹친 게 아니라 **청산 체결 7건의 절삭**이다.

---

## 몫 C — 「C3=7」의 계보

### C-1. 출처는 **미상이 아니다** — CONTROL 의 전제를 정정한다

CONTROL 은 「dev-log 가 산출 방법을 안 적었으면 그 자체가 발견」이라 했다. 실제로는:

- `dev-log/2026-07-29-live-orphan-close.md` — C3 언급이 **125행 단 한 곳**, 표의 숫자뿐. **방법 없음.** (CONTROL 관측 맞음)
- 그러나 **`docs/status.md:437` 에 계측기가 명시돼 있다** — leg 1 채널 표의 그 행이 이렇다:

  | 채널        | 계측                        |  차분 | 시간당 | 직전 기준선 |
  | ----------- | --------------------------- | ----: | -----: | ----------- |
  | C3 부분체결 | `qb_partial_fill_total{ws}` | **3** |   ~2.1 | 미측정      |

⇒ **C3 = `qb_partial_fill_total{ws}` counter 차분.** leg1 = 3, 전 창 합산 = 7.
**「출처 미상의 숫자가 P1 판정 근거였다」는 성립하지 않는다** — 다만 **dev-log 만 읽으면
알 수 없고 status.md 를 열어야 한다**(계측기가 정본 표에서 탈락한 채 숫자만 전파됐다).

★그리고 그 counter 가 **정확히 PR #509 가 "쓰지 마라"고 못 박은 counter 다.** 계보가 닫힌다.

### C-2. 순서 — CONTROL 이 제시한 3단계, **맞다**

`git log -L 637,650:backend/src/common/metrics.py` 로 확인:

1. **07-29** — `status.md:437` 이 C3 을 `qb_partial_fill_total{ws}` 차분으로 측정, 합산 **7**. 판정 「유지」.
2. **07-30 PR #509** (`f8706618`, "BL-536 판정 「축소」") — **이 커밋이 metrics.py:637-650 주석을 넣었다.**
   「이 counter 를 진입 부분체결의 측정치로 쓰지 마라 … `{ws}`=19 인데 같은 창 진입 원장은 **0**」 ⇒ **C3=7 반증.**
3. **07-30 PR #511** (`75722cbe`, "BL-560 신설 + **BL-536 판정 철회**") — 「축소」 철회.

⇒ **철회 이후 유효 판정이 07-29 의 「유지」로 되돌아갔고, 그 「유지」의 채널 표에는
#509 가 이미 무효화한 C3=7 이 그대로 실려 있다.** CONTROL 의 서술 그대로다. ✅

---

## 몫 D — 유실률 분자·분모

### D-1. ★★★ **CONTROL 이 틀렸다 — 층위 1 정의가 출시된 구현과 분자·분모 둘 다 어긋난다**

|                                               | 분자                                         | 분모                            |
| --------------------------------------------- | -------------------------------------------- | ------------------------------- |
| **CONTROL 제안**                              | `rejected(origin ∈ {exchange, **unknown**})` | `has_fill + **rejected(전부)**` |
| **출시 코드** `entry_completeness.py:511-520` | `rejected_exchange` (**exchange 만**)        | `has_fill + rejected_exchange`  |

코드 docstring `:514` 이 명시한다:

> ★분자에서 `local`(거래소 미도달)과 **`unknown`(retCode 없음)을 뺀다.** 우리 인프라 장애를
> 거래소 거절로 세면 유실률이 부풀고, **근거 없이 거래소를 탓하게 된다.**

**CONTROL 의 오독 지점.** `RejectionOrigin` docstring `:256-259` 은 unknown 을 유실 쪽에 남긴다고
하지만 **범위가 명시돼 있다 — 「다만 _에피소드 판정에서는_ 유실 쪽에 남긴다」**. 그건 **층위 2** 규칙이다.
CONTROL 은 **층위 2 규칙을 층위 1 에 옮겨 붙였다.** 코드는 두 층위를 의도적으로 다르게 다룬다
(층위1 `confirmed_rejection_rate:514` 는 unknown 제외 / 층위2 `:745` 는 unknown 유지 + `lost_with_unknown_origin` 표면화).

**CONTROL 이 댄 근거도 판별력이 없다.** 「직전 2/12 와 같은 정의라야 대조가 성립한다」 —
그런데 **두 정의 모두 2/12 를 낸다**(그 창의 `rejected_local`=`rejected_unknown`=0). 실측으로 확인:
전 기간 진입 거절 **34건이 전부 exchange(retCode) 출처**, local/unknown **0건**.
⇒ 재현성 논거로는 두 정의를 못 가른다. 가르는 것은 docstring 의 근거뿐이고, 그건 코드 편이다.

⇒ **권고: 코드의 `confirmed_rejection_rate` 를 그대로 사전등록 「유실률」로 채택하라.**
문서에 변형을 새로 쓰면 **같은 이름의 두 번째 정의**가 생긴다 — 이 레포가 반복해서 밟은 함정이다.
(현재 영향 0건이므로 숫자는 안 바뀐다. 바뀌는 건 **다음에 unknown 이 처음 나오는 날**이다.)

### D-2. 층위 2 · 채널 표 분리 — **동의**

`entry_completeness.py:627` 이 이미 강제한다: 「층위 1 과 층위 2 를 묶는 합계 프로퍼티가 **없다**.
분모가 다른 두 수를 더하지 못하게」. 채널 표를 A3/A4 전용으로 두고 층위 1/2 과 합산 금지 — 동의.

### D-3. 실측 — 두 창의 층위 1

| 창                                            | has_fill | rejected_exchange |               층위1 확정 거절률 |
| --------------------------------------------- | -------: | ----------------: | ------------------------------: |
| 07-29 소크 (05:51:05Z~08:48Z)                 |       10 |                 2 | **16.67%** ← 직전 헤드라인 재현 |
| **07-30~07-31 (현행 코드) 조건부 진입 106건** |       37 |                 1 |                       **2.63%** |

★**현행 코드에서는 2.63% 로 A4 의 10% 문턱을 크게 밑돈다.**

---

## CONTROL 이 틀린 곳 (3건)

### ① ★★★ `docs/status.md:24` — 「유실률 2/12 의 분모 12 가 C2 합산이다」는 **거짓**. 같은 파일이 반박한다

- `status.md:24` (이번 회차 진입 근거) — 「유실률 `2/12` 의 분모 12 가 **C2 합산**이고」
- `status.md:464` (같은 파일) — 「**유실률 = 2 / (10 filled + 2 rejected) = 16.7%**」
- `dev-log/2026-07-29-…:133` — 「유실률 = 2 / (10+2) = 16.7%」
- `dev-log/2026-07-30-…` — 「층위1 시도 확정 거절률 **16.67%** ← 직전 헤드라인 16.7% 와 일치」

**분모 12 = filled 10 + rejected 2 다. C2 와 무관하다.** C2 가 마침 **12** 였을 뿐이다.

**오류 발생 기전(추정 근거 있음):** `backlog.md:4362` 가 `2/(10+2)` 를 **`2/12` 로 압축**하면서
라벨을 잃었고, 그 줄 **6행 위**에 `| C2 … | **12** |` 가 있다. 두 개의 12 가 붙어 있었다.
`status.md:24` 는 압축형을 읽고 옆의 C2 와 동일시했다.

**파급 (작지 않다):**

- 「BL-536 의 **두 근거가 모두 C2 에 의존**」 → **거짓.** C2 에 의존하는 건 「축소 ✗」 **하나뿐**이다.
  유실률 16.7% 는 C2 를 빼도 **그대로**다.
- 그래서 A2 「C2 제외 유실률 0」이 **구조적으로 발화 불가**가 됐다(§A-4).
- 이번 회차 첫 step #2 「지금 값 `2/12` 는 분모가 C2 다」(`status.md:30`)도 같은 오류다.
- `roadmap.md:48` 에 **동일 문장이 복제**돼 있다. 3곳 정정 필요: `status.md:24` · `status.md:30` · `roadmap.md:48`.

★ 다행히 **CONTROL 이 내게 준 프롬프트의 몫 D 에서는 이미 올바르게** 적었다
(「2/12 = rejected 2/(filled 10+rejected 2)」). **정본 문서만 안 고쳐진 상태**다 — 그런데
`status.md` 가 「다음 세션의 유일한 진입점」이라 선언된 파일이므로 방치하면 다음 워커가 그대로 밟는다.

### ② ★★ 몫 D 층위 1 정의가 출시 구현과 어긋난다 (§D-1)

분자에 `unknown` 을 넣고 분모에 `rejected` 전부를 넣는 건 **층위 2 규칙의 층위 1 전용**이다.
코드는 정반대를 명시적 근거와 함께 한다. **현재 숫자 영향은 0**(local/unknown 실측 0건)이나 정의는 정정 대상.

### ③ ★ 몫 C 「출처 미상」 전제 — 출처는 있다 (§C-1)

`status.md:437` 이 `qb_partial_fill_total{ws}` 로 명시한다. dev-log 에만 없다.
⇒ 「출처 미상의 숫자가 P1 근거였다」로 쓰면 **과장**이다. 정확한 표현은
**「계측기 이름이 정본 표에서 탈락한 채 숫자만 전파됐다」**.

---

## 내가 확신하지 못한 것 (정직하게)

1. **C3=7 이 문자 그대로 `{ws}` 라벨 counter 차분 7 이었는지** — 원본 Prometheus 스냅샷을 못 봤다.
   근거는 (a) leg1 표가 계측기를 명시 + leg1=3 → 합산 7, (b) 내 원장 실측 청산 체결 7건이
   기전상 정확히 7회 발화시킨다는 일치. **정황이 매우 강하지만 스냅샷 직접 확인은 아니다.**
2. **C4/C5 의 「0」이 진짜 0 인지 미발화인지** — A1(계측기 선행)이 요구하는 판정을 못 했다.
   `sweep_filled_total` · `qb_order_rejected_total` 이 그 창에서 발화 가능했는지 미확인. **A1 은 미완이다.**
3. **07-29 창 원장이 그날 이후 변했다.** dev-log 는 `cancelled 25 · submitted 1`, 지금 DB 는
   `cancelled 26 · submitted 0`. 1행이 사후 전이했다(합 38 불변). 결론엔 영향 없으나
   **「과거 창 재측정 = 그날 값」이 자동으로 참이 아니다.**
4. **재판정 창 선택** — 아래 부록은 07-29 창 기준이다. 현행 코드(07-30~31)와 **코드가 다르다.**
   어느 창으로 BL-536 을 닫을지는 CONTROL 판단이다.
5. **실주행 검증 미실시** — 워크트리에서 구조적 불가(worker 가 메인 `src` bind-mount). 전부 원장 사후분석이다.

---

## 부록 — 확정된 정의를 넣으면 판정이 이렇게 나온다 (참고용, CONTROL 확인 필요)

**07-29 소크 창**, A3 분모 = 채널 합, C3 을 0 으로 정정(§몫 B):

| 채널          | 07-29 원표 |                      정정 후 |
| ------------- | ---------: | ---------------------------: |
| C1 잔여 거절  |          2 |                        **2** |
| ~~C2~~ (제외) |         12 |                            — |
| C3 부분체결   |          7 | **0** ← 청산측 절삭 아티팩트 |
| C4 / C5       |      0 / 0 |           0 / 0 (★A1 미검증) |
| **합**        |         21 |                        **2** |

| ID           | 조건                   | 실측                          | 결과  |
| ------------ | ---------------------- | ----------------------------- | ----- |
| A2 소멸      | C2 제외 유실률 0       | 16.67% ≠ 0 (C2 제거해도 불변) | ✗     |
| A3 축소      | 최대 채널 ≥70%         | C1 **2/2 = 100%**             | **✓** |
| A4 유지      | ≥10% **AND** 채널 분산 | 16.67% ✓ · 분산 ✗(단일 채널)  | ✗     |
| A5 판정 불가 | 표본 문턱 미달         | **문턱 미정의** · 합 N=2      | ⚠     |

⇒ **AND 로는 「축소」 단일 판정. OR 로는 「축소 ✓ + 유지 ✓」 모순.** §A-2 의 근거가 이것이다.

★**그러나 나는 이 「축소」를 채택 권고하지 않는다.** 채널 합 **N=2** 에서 「최대 채널 100%」는
내용이 없다 — **A5 문턱을 먼저 숫자로 박고**(§A-5), C4/C5 계측기 발화 가능성(A1)을 판정한 뒤
재적용해야 한다. 지금 상태의 정직한 답은 **「A5 판정 불가」**에 가깝다.

**현행 코드 창(07-30~31)** 은 층위1 **2.63%** 로 A4 의 10% 를 밑돌고, C1=1 · C3=0 이라
채널 합 **N=1** 이다 — 여기서도 A5 가 먼저다.

---

### 검사에 쓴 쿼리 (전부 SELECT · 세미콜론 1개씩)

```sql
-- B1
SELECT reduce_only, (trigger_price IS NOT NULL) AS conditional, count(*)
FROM trading.orders GROUP BY 1,2 ORDER BY 1,2;
-- B2
SELECT date_trunc('day',created_at)::date, count(*) FROM trading.orders
WHERE reduce_only=false AND (error_message LIKE '%110092%' OR error_message LIKE '%110093%')
GROUP BY 1 ORDER BY 1;
-- B3/B4/B5 (부분체결 술어)
filled_quantity IS NOT NULL AND filled_quantity > 0 AND filled_quantity < quantity
-- D3 층위1
SELECT count(*) FILTER (WHERE filled_quantity IS NOT NULL AND filled_quantity>0) AS has_fill,
       count(*) FILTER (WHERE state='rejected' AND error_message ~ '11[0-9]{4}') AS rej_exchange
FROM trading.orders WHERE <window> AND reduce_only=false;
```
