# 소크 사망·재기동 회차 기록 (2026-08-08)

> `docs/status.md` 에서 강등된 회차 기록이다(2026-08-10 docs-context-diet).
> 강등 이유는 크기가 아니라 **역할**이다 — 끝난 회차의 회고는 진입점이 아니다(ADR-026 §3).
> 원문 위치 = `git show 762e1297:docs/status.md` 의 750-1122행.

---

## 직전 회차 기록 — [BL-003] 시간을 쌓는다 (연속 24h + 누적 168h)

**★zero-touch-bundle 회차가 이 블록에 더한 것 — 소크 무중단(재-pin 0회).**
`backend/src` **0줄** · C1/C2 **11.34h → 13.94h** · 실격 0. [BL-644]·[BL-646]·[BL-649] **Resolved**,
[BL-648] 🟡 **부분**, [BL-598] **①확정**(②미착수), 신규 **[BL-652]**. 전문 =
[`dev-log/2026-08-08-zero-touch-bundle.md`](dev-log/2026-08-08-zero-touch-bundle.md).
★★★**회차 성격이 `backend/src` 0줄이었고, 그것이 후보 순위를 지배했다** — 창 보호가 설계 제약이라
⓪ 표의 A·B(둘 다 「건드림」)를 **제외하고** C·D·E 를 묶었다. 「왜 P1 을 안 했나」로 읽지 마라.
★★★**반증 3건이 최대 산출이다.** ⑴ **[BL-646] 전제가 틀렸다** — 그리드가 받는 폭은 뷰포트가 아니라
`.page` 콘텐츠 박스이고 **뷰포트에 단조가 아니다**(`--sidebar-w` 가 1024 에서 232→64 계단 ⇒ 뷰포트
1023→1025 에서 콘텐츠 911→745, **166px 감소**). 1024 흡수는 「가장 넓을 때 접는」 모순, 768 흡수는
실제 파손(라벨 꺾임) ⇒ **①등재가 답**. ⑵ **[BL-598] 판정이 (a)도 (b)도 아니었다** — import
워밍업(0.26s=0.4%)도 크기 비선형(log-log **0.78** sublinear)도 아닌 **ANTLR 파서 DFA 워밍업**
(캐시만 비우면 3.68→51.37s, **14배**). ★그리고 **codex 가 그 결론의 사거리를 두 번 좁혔다** —
초판 cold 램프의 「평평하다」는 **공유 DFA 가 만든 허상**이었고(격리하니 8.39→50.80s 단조증가),
성분 통제가 매 회차 뒤 되데워 「parser DFA **단독**」은 과대 진술이었다(정정: **「parser DFA 가
워밍된 상태에서는 나머지 둘이 추가 비용을 지지 않는다」**, 독립 통제 재측 13.9/1.0/1.0).
⑶ **라이트 캐논 spec 이 fail-open 이었다** — 「라이트를 감사한다」면서 **없는 경로(404)도 빈 DOM 도
5/5 초록**이었다(`auditUrl` 이 `goto` 반환값을 버렸다). codex 가 잡았고 수리 후 **4 red / 5 red**.
★★**도구·환경이 세 번 속였다** — ⑴ `worktree-bootstrap.sh` 가 **DB 부재로 실패하고도 exit 0**
을 내 `pnpm install`·`uv sync`(`:351-359`)가 통째로 건너뛰어졌는데 `.worktree-slot`·env 는 생성돼
**겉보기 검증을 통과**했다 ⑵ **CSS 변이가 도달하지 않았다**(dev 서버가 같은 이름의 낡은 청크를
계속 줬다 — [BL-650] 재발) ⑶ 이 앱에서 **「없는 경로」는 자동으로 404 가 아니다**(`proxy.ts` 공개
매처 밖이면 Clerk 가 `/sign-in` 으로 리다이렉트해 **엉뚱한 이유로 red**).
★**내 실수** — 머지 충돌 해결본을 **파일 전체로** 만들어 덮어써 워커 하나의 섹션 3개 갱신을 날렸다.
표 행은 ✅ 인데 섹션은 ACTIVE 라 **`bl-audit` 3면 대조가 잡았다**. **충돌은 마커 영역만 손대라.**
★오케스트레이션: generator = Opus 서브에이전트 3(워크트리 슬롯 9/10/11) · evaluator = codex
(워커 안 3 + CONTROL 최종 challenge 1) · CONTROL 은 구현 0. **codex 가 두 층 모두에서 결론을 바꿨다.**
LESSON-090/091.

**★soak-window-and-gate-attribution 회차가 이 블록에 더한 것 — 소크 무중단(재-pin 0회).**
`backend/src` **0줄** · 커밋 5 · C1/C2 **5.84h → 9.00h** · 실격 0. [BL-650] **🟡 부분 해결**.
전문 = [`dev-log/2026-08-08-soak-window-and-gate-attribution.md`](dev-log/2026-08-08-soak-window-and-gate-attribution.md).

★★★**[ADR-024] 가 자기가 기각한 안을 집행하고 있었다.** §대안 (a)「연속 168h」를 **명시적으로
거부**해 놓고(「재시작 한 번에 전부 잃는다 … 사실상 도달 불가」), `soak_gate_predicate.py:502` 의
`window_start = disq[-1].at` 가 바로 그 (a)를 집행한다. §163 은 「**서술**이 틀렸다」로 봉합했고
§대안은 「(a)는 거부」로 남아 있어 **두 절을 아무도 잇지 않았다.** 이제 한 절에 모아 등재했고
**동작은 유지**한다 — 셋 다 더 나쁘다(문턱 인하 = 통과를 판다 · 클립 = 거짓 PASS 복원 ·
사인별 리셋 = fail-open, §대안 **(h)** 로 기각). **바꾼 것은 동작이 아니라 대가를 아는가다.**
★★**실격 귀속 원장 신설** — `docs/reference/operations/soak-disqualifications.jsonl`, 전 이력
11건 등재(운영 **3** · 코드 결함 **1** · 미판정 **7**). 계약 셋: **판정 불참**(C1~C5 비트 동일) ·
**`undecided`=`code_defect`**(엄격 — 지워서 유리해지는 경로 없음) · **자동 유도 금지**(`evidence`
필수). 게이트는 보고 줄 한 줄만 낸다. `mtbf_stratified.py` 가 이 원장으로 층을 **자동 생성**한다.
★**그 층이 곧바로 자기를 겸손하게 만들었다** — 오염 1건을 빼면 MTBF 14.52h → 16.59h 인데
**95% CI 가 전 이력과 겹친다**([8.05, 41.27] ↔ [7.37, 33.63]) ⇒ 「빼면 낫다」고 **말할 수 없다**.
★★**`position_divergence` 5건을 `code_defect` 로 적지 않았다** — [ADR-025] 의 「사망 5/5 를 닫았다」가
**그 5행과 같은 집합인지 대조된 적이 없다**. 그 대조가 ⓪ 표 **B** 다.
★★**[BL-650] 재현 실패가 결과다** — 593MB 캐시에 요청 1건을 먹인 뒤 idle CPU **0.1%** · `/`
**0.61초**(1.99GB 기록은 416.9% / 120초 타임아웃). **증상은 크기 단조가 아니다**(RSS 는 이미
945MB). ⇒ `make fe` 의 1GB 경고는 **정책이 아니라 관측 장치**다. 그리고
**`turbopackMemoryLimit` 은 존재하지 않는다**(Next 가 제거, 대체 없음) — 백로그 처방 ①이 실행 불가였다.
★낡은 빌드 디렉터리는 **4벌이 아니라 5벌 · 8.5GB**(레포 26G → **18G**). `frontend/.gitignore:3` 의
`.next*/` 가 git status 에서 통째로 가려 왔다.
★**내 측정·검증 도구가 세 번 틀렸다** — ⑴ `pgrep -f "next dev"` 가 **부모 래퍼**를 준다(판별자는
`lsof … -sTCP:LISTEN -t`) ⑵ 변이 하네스의 zsh 배열이 **1-based** 라 변이 하나가 안 돌았는데 표는
정상처럼 보였다 ⑶ 테스트 하나가 **이름이 주장하는 것을 안 쟀다**(변이 판별력 0 → 시나리오 교체
후 적발). 최종 **변이 8/8** · sha256 복원 일치. LESSON-086/087.
★**착지 직후 게이트가 나를 두 번 물었다** — `bl-audit` 상태 어휘(「부분」 ≠ 「부분 해결」) ·
`docs-audit` 인덱스 행 1119자. **문장을 비틀지 않고** 어휘와 길이를 고쳤다.

**★fe-canon-and-responsive 회차가 이 블록에 더한 것 — 소크 무중단(재-pin 0회).**
`backend/src` **0줄** · 커밋 10 · C1/C2 **3.99h → 5.84h** · 실격 0.
[BL-556]·[BL-618]·[BL-628]·[BL-629]·[BL-630] **Resolved**, 신규 [BL-644]~[BL-650].
★★★**「검사기가 없어서 결함이 살아 있었다」가 세 번 반복됐다** — 캐논 감사는 `defaultTheme="dark"`
탓에 **라이트를 한 번도 안 재고**([BL-628] 이 그래서 안 물렸다), 토큰 계약 테스트는 「정의된 것을
읽나」를 **안 보고**(다크 `--chart-axis` 가 팔레트 개정 한 바퀴를 썩은 채 통과), e2e 전체에서
`sidebar` grep 이 **0건**이었다(그래서 `DESIGN.md` 가 220/60/1200 을 적고도 조용했다).
세 축 모두 **고치는 것보다 「누가 지킬 것인가」가 본체**였고 오라클 4종을 신설했다.
★★**음성 대조가 낡은 CSS 로 한 번 거짓 통과했다** — 변이 후 1초 만에 e2e 를 돌리면 Turbopack 이
직전 스타일시트를 준다. **도달을 확인하고 재라.**
★★★**회차 중 개발 환경이 두 번 죽었고 범인은 FE 코드가 아니었다** — `.next/dev` Turbopack 영속
캐시 **1.99GB**. 요청 0건·클라이언트 0개에서 `next dev` 가 **416.9% CPU / 1000MB**, 치우면
**0.1%** / 374MB. 렌더 루프 가설은 클라이언트가 0이라 성립 불가였고 랜딩 트리 `useEffect` 도 0건.
★`ps -o pcpu` 는 **수명 평균**이라 판별에 못 쓴다 — `ps -o time` 차분으로 재라(433.6%).
신규 **[BL-650]**(P2).
★**「1200px」 규명 완료** — 4곳이 아니라 **5곳**이고 전부 콘텐츠 그리드 축이라 셸 미개입.
반응형 정본은 셋이 아니라 **넷**이었다(`@theme` 이 `sm:` 640→375 · `xl:` 1280→1200 을 덮는다).

**★soak-exclusivity-and-observability 회차가 이 블록에 더한 것 — 소크 무중단(재-pin 0회).**
`backend/src` **0줄** · 커밋 5 · C1/C2 **3.48h → 3.99h** · 실격 0.
[BL-642]·[BL-643] **Resolved**, [BL-619]·[BL-641] **🟡 부분**.
★★★**MTBF 층화 표는 개선 증거가 아니다** — 95% CI 가 **6쌍 전부 겹치고**, 같은 데이터의 CI 상한을
취하면 P(168h) 가 한 층 0.07% · 다른 층 **38.11%** 다. [ADR-024] 의 결론은 형태를 유지하고 **근거를
셈으로 교체**했다(24h 도달 **0/39** · 최장 19.42h · 요구치는 그 8.65배). 재측정 도구 =
`backend/scripts/mtbf_stratified.py`(self-check 가 앞 38행으로 앞 회차 값을 재현, 2/2).
★★★**게이트 술어는 낱말이 아니라 구문이어야 한다** — [BL-643] 초안의 「다음 행동」 낱말 술어는
규칙을 _설명하는_ 문장을 물었고, `다음 행동 =` 로 좁히자 명세가 지시한 예외 목록이 통째로
불필요해졌다(변이 6/6, 오탐 0). ★그리고 **「블록당 1개」 계약이 실제 사고를 통과시킨다** —
그 2건은 서로 다른 섹션에 하나씩 있었다 ⇒ 게이트는 **파일 전체**로 센다.
★**「설치 완료」 출력은 검증이 아니다** — [BL-619] 로그 장치는 75초 재측정(+10,422B · 커서 전진)으로
확인했다. ★**서버 클론이 `--single-branch`**([BL-623])라 `origin/main` 이 낡아 `stack_pinned` 가
`조상: NO` 를 찍고 있었다 — `git fetch` 한 번으로 해소, **재-pin 은 필요 없었다.**

### ★★★한 줄 — 소크는 **또 죽었다**(`position_divergence`). 창은 0 부터 다시 쌓는다

**현행: 세션 `a4f1cbfb` 활성 · 고정 `3f8af9df` · 소크는 맥이 아니라 오라클 A1(도쿄)에서 돈다.**
★**2026-08-08 재기동 완료** — 계정을 flat 으로 만들고(★같은 Bybit 계정을 가리키는 자격증명이 둘이라
**flatten 은 한 번만** 했다) 새 창을 열었다. 창 시작 `2026-08-08T02:32:38Z` · T0 `02:32:42.863297`.
게이트가 `FAIL 실격` → **`UNKNOWN 진행중`** 으로 바뀌었다(C3 0건 · C4 ✓ · C5 전건 ✓).
~~★★**고정본이 main 조상이 아니다**(PR #561 미머지).~~ ★**2026-08-08 해소** — PR #561 이 머지됐고
`3f8af9df` 는 main 조상이다(서버는 `git fetch` 후 `조상: YES`). **squash 머지하면 고정 sha 가 main
이력에서 사라진다**는 경고는 유효하다 ⇒ 앞으로도 merge commit 으로 머지해라.
★재기동 ⑺ 은 실패했다 — `soak-observe.sh` 가 아직 `:8100` 을 긁는다 — [BL-642]. 재기동 자체는 성공했다.
직전 세션 `39484a2c` 가 **2026-08-07T15:10:49Z** 에 `position_divergence` 로 자동 사망했고
그 실격이 창을 리셋했다 — 게이트 판정 **`FAIL / 실격`**, C1·C2 **둘 다 0.0000h**.
★★★**여기에 숫자를 박지 마라.** 위 값도 스냅샷일 뿐이다. 이 레포는 낡은 스냅샷을 다음 사람이
그대로 인용해 이미 여러 번 덴 적이 있다(어둠 비율이 같은 날 2.9% → 70.6% 로 움직였다 — 둘 다 아래 회차 블록에 출처가 있다).
**지금 값을 아는 유일한 방법은 게이트를 돌리는 것이다 — 첫 명령은 언제나 `scripts/soak-gate.sh` 다.**
★★**서버 게이트는 반드시 `ssh <서버> 'bash -lc "…"'` 로 불러라.** 비로그인 셸은 PATH 에 `uv` 가
없어 phantom 분류기가 실패하고 **그 구간이 커버리지에서 잘려나간다**(2026-08-07 실측 8분 손실).
★**re-pin 의 대가가 C2 뿐인 것은 re-pin 이 *단독* 일 때만이다.** 실격이 끼면 **C1 도 0 이 된다** —
`backend/scripts/soak_gate_predicate.py` 의
`countable = [a for a in attribution if a.start >= window_start]` 가 실격 시점에 **이미 열려 있던**
귀속 구간을 클립하지 않고 **통째로 배제**하기 때문이다(2026-08-07 회차 게이트 출력: C1 5.31h → 0.0000h. ★2026-08-08 이 동결 원장으로 재현한 값은 C1 5.5913h / C2 5.5197h → 0.0 이다 — 게이트의 커버리지 자르기 때문에 두 값이 다르다).
⇒ 「누적은 안전하다」는 **재고정에만** 쓰는 말이다. 사망에는 쓰지 마라.
★**워커 로그 follow 장치는 로컬 맥에만 있다.** `.soak/logs/worker-follow.log` 는 이 맥의 비추적
파일이고, **서버 `~/quantbridge/.soak/logs` 는 존재하지 않는다**(2026-08-08 실측 `ls` 결과
`No such file or directory`). 서버에서 재발하면 이번과 똑같이 컨테이너 재생성이 로그를 먼저
지운다 — 서버에 같은 장치를 걸기 전에는 [BL-619] 의 Trigger 를 충족할 수 없다.
★그 장치를 이번 회차가 만들었다 — `scripts/soak-logs-follow.sh --install`(systemd user unit +
lingering, macOS 는 launchd). ★**아직 서버에 설치하지 않았다** — 설치는 갈래 C 재기동과 함께 한다.

**아래는 그보다 앞선 사망(`c160a1a9`)의 기록이다.**
세션 `c160a1a9` 가 2026-08-06T20:31:48Z 에 `gap_resync_position_mismatch` 로 자동 사망했다.
**19.42시간을 살고**(01:06:46Z → 20:31:48Z, 원장 기준 역대 최장) C2 를 **4.58h 남기고** 놓쳤고,
실격이 창을 리셋해 그 19.42h 는 **한 시간도 계상되지 않았다.** 그 뒤 활성 세션은 **0** 이다.

★★★**[BL-620] 이 진짜 blocker 였다** — 게이트 결함이 아니라 **`:8100` 에 리스너가 없었다**
(`soak-stack.sh up` 은 API 컨테이너를 안 띄운다). **C1/C2 를 다 채워도 PASS 가 안 나는**
상태였다. 취득 경로를 **`backend/.metrics` 직독**으로 바꿔 닫았다 — 워커가 같은 counter 를
거기 쓰므로 API 프로세스가 필요 없다. fail-closed 음성 대조 **3/3**(없는 dir·죽은 포트 →
`측정불가` 유지). `QB_METRICS_URL` 을 명시하면 종전 HTTP 를 쓴다(원격 운영안 보존 — 단 그
갈래는 베어러 토큰과 양립 불가다, **[BL-624]**).
★귀속 불가 시간은 계상되지 않는다(소급 불가, 게이트가 매번 출력한다).

**★그 사망은 부검이 끝났고 수리됐다 — [BL-622] Resolved (판정 = H3 관측 지연).**
거래소는 조건부 매도를 **20:17:19.519** 에 체결했는데(`exchange_exits.exchange_updated_at`)
우리 원장은 **20:31:51.622** 에야 기록했다 — **872초(14분 32초)** 지연이고 판정은 그 **3.5초
전**에 났다. ★같은 세션의 다른 3건은 거래소 시각과 우리 `filled_at` 이 **50밀리초** 차라
**계통 오차가 아니다.** 수리 = `try_claim_bar` **앞**에서 「이 세션의 미확정 조건부 진입이
있으면 판정을 미룬다」. 상한은 **미룬 횟수 3 tick**(≈2분) — 주문 나이가 **아니다**. 변이 4/4 적발.
★PR #556 리뷰가 초판(janitor 문턱 30분)을 **실측으로 반증**했다: 조건부 진입 resting 이 벽시계의
**95.1%** 를 덮어 나이 기준은 사실상 상시 유예가 된다([BL-622] §상한).
★★상류의 **~17분 파이프라인 정지**는 로그 소실로 **판정 불가**다 — 신규 **[BL-619]**, 다음 창에서
로그를 남긴 채 재관측한다. 「이상 없음」으로 접지 않았다.

~~**★★★다음 행동 = 기다린다.**~~ ★★★**2026-08-08 정정 — 「기다린다」는 코드상 성립하지 않는다.**
`soak_gate_predicate.py` 의 `countable` 이 실격 시점에 열려 있던 귀속 구간을 **통째 배제**하므로
실격 뒤에는 기다려도 C1 이 1초도 오르지 않는다. 그리고 지금 활성 세션이 **0** 이라 시계 자체가 안 돈다
(바로 아래 ⑸ 가 스스로 「`up` 은 세션을 만들지 않는다. 세션이 0이면 시계도 0이다」라고 적어 뒀다).
~~**다음 행동 = ⑴ 계정 배타성 확보 ⑵ 배타성 preflight ⑶ 재기동.**~~ ★**셋 다 2026-08-08 에 했다.**
다음에 할 것은 이 파일 맨 위의 §다음 스프린트 블록이다.
아래 절차는 그 ⑶ 의 상세다.
재기동 절차(다음에 또 죽었을 때 그대로 밟는다): ⑴ `live_session_admin.py status` 로
**`FLAT=YES` 확인**(세션 `DELETE` 204 는 아무것도 flat 하지 않는다 — 3회 덴 함정)
⑵ 아니면 같은 CLI `flatten <session_id> --confirm`(서비스 계층 경유 — 원장에 남는다) ⑶ **사용자
승인** ⑷ `soak-stack.sh down` → `pin` → `up`(★돌고 있는 고정본 위에는 pin 이 거부된다)
⑸ `live_session_admin.py start … --confirm` — **`up` 은 세션을 만들지 않는다. 세션이 0이면
시계도 0이다** ⑹ `.soak/session` 은 **맨 uuid 가 아니라 `SESSION_ID=<uuid>`** 형식이다
(`soak-observe.sh:66` 이 그 형식으로 쓰고 `.` 로 소싱한다 — 맨 uuid 를 넣으면 조용히 깨진다)
⑺ `soak-observe.sh --baseline --session <uuid>` (★`--session` 은 **플래그**다. 위치인자로 주면
`unknown arg`) ⑻ `soak-gate.sh` 로 창 확인.
★**이제 이 8단계는 `scripts/soak-restart.sh` 가 집행한다** — 기본이 dry-run(출력만), 집행은
`--confirm`, ⑴ 이 `FLAT=YES` 가 아니면 그 자리에서 멈춘다(자동 flatten 없음). 손으로 밟지 마라:
2026-08-07 09:35 재기동이 ⑺ 을 빠뜨려 `.soak/session` 이 **맨 uuid** 로 쓰였고 `soak-observe.sh`
가 `command not found`(rc=127)로 죽어 **일일 원장 대조가 막혀 있었다** — 위 ⑹ 이 경고한 바로 그 사고다.

**★backtest-reality-gap 회차가 이 블록에 더한 것 — 소크는 건드리지 않았다(창 연속 유지).**
`ts.ohlcv` 에 perp 1m 2,879행이 새로 들어갔다(라이브는 이 테이블을 안 읽는다 — 실측으로
확정, 소크 무영향). 신규 BL 7종(BL-603~BL-609) 중 **BL-604 — 진입 집합 불일치(P1) — 의
분리 실험(300봉 롤링 재생 재실행)은 `backend/src` 무접촉**이라 소크가 도는 동안 안전하다 —
도구는 `backend/scripts/btgap_compare.py` 재사용. BL-605(exchange_exits 2배 중복)의
**소비처 전수 확인**도 읽기 전용이라 안전. 측정 산출물(동결 CSV·match report)은
`.claude/fleet/btgap/artifacts/`(비추적)에 있다.

**★entry-set-divergence 회차가 이 블록에 더한 것 — 소크는 건드리지 않았다(창 연속 유지).**
[BL-604] 동인 분리가 끝났고 **프레임이 반전됐다**: 롤링 300봉 워밍업의 기여 **0건**(B 와
R(fill)의 key multiset 완전 동일) · 세션 공백 **0쌍** · 「예측 못 한 46건」은 존재 격차가
아니라 **키 규약 관측**(B=체결봉·L=장전봉, 체결 지연 57%가 ±3봉 밖). 장전봉 정렬 매칭
**81/90=90%**, 어느 규약으로도 안 붙는 L 은 **2/90**. ⇒ 신호 축은 닫혔다 — 백테스트↔라이브
잔여 축은 **비용([BL-603], 가정 2.7배)** 과 잔차 9건(트리거 드리프트+거래소 게이트 상계,
`docs/dev-log/2026-08-06-entry-set-divergence.md` 판정 표)이다. FE 정직성 3종
(BL-606/607/608)과 BL-609 도 Resolved. 도구: `btgap_compare.py` 에 `replay`(라이브 프로토콜
재생)·`entrysets`(진입 집합 매처) 신설 — 다음 대조는 재실행만 하면 된다.

**★gap-resync-autopsy 회차가 이 블록에 더한 것 — 소크는 죽어 있었고 이 회차가 다시 켰다.**
[BL-622] 수리(위) 외에 **[BL-603] 백테스트 비용 모델도 Resolved** 다. 기본값을 실측으로 좁혔다:
`fees` 0.001→**0.00055**(taker 0.055%/leg) · `slippage` 0.0005→**0.00014**(진입가 잔차 중앙) ·
`maker_fee` **불변**. 왕복 **0.300% → 0.138%**(라이브 실측 fee 왕복 0.1101%).
★★**엔진과 API 스키마에 같은 값의 리터럴이 둘**이라 한쪽만 고치면 **사용자 제출 경로가 안 바뀐다**
— 둘 다 옮겼고 FE 미러 **4곳**(폼·카드·재실행 fallback·안내 문구)과 e2e `house_default` 픽스처도
같이 옮겼다. `_house_default_assumption()` 은 런타임에 읽어 자동으로 따라온다([BL-526] 표면).
★코퍼스 baseline 재생성: **`num_trades` 는 7 코퍼스 전건 불변**(비용은 체결 집합을 안 바꾼다)이고
손익만 움직였다 — `s3_rsid` 는 total_return **−1.083 → +0.184** 로 **부호가 뒤집혔다**(비용 민감
전략의 부당 탈락이 실물로 확인됐다). 신규 **[BL-621]**(골든 `expected.json` metric 이 이번 회차
**이전부터** 낡았는데 아무도 대조하지 않는다). 게이트: BE **4394 passed / 45 skipped** ·
FE **1260 passed** · `bl-audit` 3면 정합 · `docs-audit` clean.
★**[BL-602] 우회** — `frontend/` 안의 json/md/yml 을 **하나도 스테이징하지 않는다**. 건드린 FE
파일은 전부 `.ts`/`.tsx` 다.

**★fe-oracle-deploy 회차가 이 블록에 더한 것 — 소크는 무중단이다(창 연속 유지).**
FE 가 오라클에 떴다: **`qb.woosung.dev`**(Cloudflare Access 이메일 OTP) + **`qb-api.woosung.dev`**
(Clerk JWT). 절차·구조 근거·함정은 [`frontend-deploy.md`](reference/operations/frontend-deploy.md)
가 정본이다. `backend/src` **0줄** · 커밋한 것은 `frontend/next.config.ts`(+8줄)·
`frontend/Dockerfile`·`frontend/.dockerignore`·루트 `docker-compose.frontend.yml`·`.env.example`.
★★★**내 착수 전제가 틀렸고 게이트가 잡았다.** 「[BL-620] 이 이미 서버에 있으니 베어러 토큰을 켜도
게이트는 무관하다」로 시작했는데, **서버 체크아웃이 [BL-620] 이전 커밋**이라 게이트가 실제로는
**HTTP 로 긁고 있었다**. `PROMETHEUS_BEARER_TOKEN` 을 켠 순간 401 → `darkness_computed=✗` →
**C5 위반**. 서버 체크아웃을 올려 직독으로 바꾸자 복구됐다(신규 **[BL-624]**).
★★**「C5 ✓」는 어느 경로로 성공했는지 말해주지 않는다** — 판별자는 API 로그의 `GET /metrics`
유무였다. 수리 전 `401` 1건 / 수리 후 **0건**. 게이트 출력만 읽고 「직독이 돌고 있다」로
읽은 것이 전제 오류의 정확한 지점이다.
★★**비로그인 ssh 셸로 게이트를 돌려 8분치를 잘라먹었다** — PATH 에 `uv` 가 없어 phantom
분류기가 실패했고 그 구간이 커버리지에서 빠졌다(잘린 시간 0.0069h → 0.1405h). 서버 게이트는
**언제나 `bash -lc`** 다. systemd 타이머는 `Environment=PATH=` 가 있어 무사했다.
★**서버 클론이 `--single-branch`** 라 feature 브랜치가 기본 fetch 로 안 온다(신규 **[BL-623]**).
★**빌드는 맥, 서버는 실행만** — 서버에 Node 가 없고 2 OCPU 를 남의 프로젝트와 공유한다.
standalone **50MB** · arm64 이미지 **211MB** · 런타임 실측 FE **64MB/768** · 터널 **17MB/256**.
★**기존 truewords 터널은 못 쓴다** — 브리지 네트워크 안이고 호스트 iptables 가 `INPUT` 에서
22 만 ACCEPT 해 **브리지→호스트 `:8100` 경로가 구조적으로 없다**. 전용 터널을 `network_mode: host`
로 띄워 uvicorn 바인딩·iptables·남의 프로젝트를 **하나도 안 건드렸다**.
★**`APP_ENV` 는 여전히 `development` 다** — Access 가 앞을 막으므로 공개 전환([BL-071])은
발동하지 않았다. 단 `PROMETHEUS_BEARER_TOKEN` 은 **production 없이도 강제**되므로 공개 `/metrics`
는 이미 닫았다(무인증 401 실측).
★★**`/health` 200 은 배포 검증이 아니다** — 서버 `backend/.env.local` 의 `CLERK_SECRET_KEY` 가
**플레이스홀더**(`sk_test_...` 문자 그대로)였는데 API 는 멀쩡히 떴고 `/health` 도 200 이었다.
로그인한 첫 요청이 **전건 401** 로 터지고서야 보였다(신규 **[BL-625]**). 고치는 과정에서
**2차 결함**도 물렸다 — 루트 `.env` 의 인라인 주석(`# [필수 …]`)을 안 떼고 값을 옮겨 한글이
섞였고, 그러자 401 이 아니라 **500**(clerk SDK 헤더 ascii 인코딩) 이 났다. 두 실패의 증상이
달랐던 것이 진단을 도왔다. ⇒ **검증은 로그인 후 데이터 화면까지** 가야 한다(최종 실측 전건 200).
★**보고 전용 관측**: `derive_total{duplicate_open}` 이 올라 이 세션의 [BL-591] 슬라이스1 계측이
흡수 상태에 들어갔다는 뜻이고, **C1~C5 판정과는 무관**하다. ★**어둠 비율은 여기 적지 않는다** —
스냅샷이지 추세가 아니다(2026-08-07 실측: 09:41 **2.9%**(1/35) → 13:11 **70.6%**(173/245),
같은 날 같은 세션). 종전에 박혀 있던 37.9% 는 그래서 낡은 값이었다. **게이트 출력을 봐라.**

**★backtest-fidelity 회차가 이 블록에 더한 것 — ★★★소크가 이 회차 중 죽었다(무중단 아님).**
`backend/src` 는 **주석 1블록**만 바뀌었다(행위 0줄). 판정식·실측·반증 전문 =
[`dev-log/2026-08-07-backtest-fidelity.md`](dev-log/2026-08-07-backtest-fidelity.md).
★**A-1 판정식을 동결하고 도구를 만들었다** — `backend/scripts/fill_ownership_verdict.py` 가 T0/T1
prometheus 원문 2벌을 받아 `agree / (agree + engine_only_suppressed + ledger_only_adopted)` 를
차분으로 낸다. 게이트 순차 4단(음수 차분 → 미지 라벨 → `D<30` → measured). **절대값 금지**이고
합격/불합격 라벨을 안 붙인다(기준선이 없는 상태에서 문턱을 붙이면 그 문턱이 근거 없이 정본이 된다).

★★★**소크 사망 — 세션 `39484a2c` 가 `2026-08-07T15:10:49Z` `position_divergence` 로 자동 사망했다.**
직전 `phantom` **2건**(`15:09:49` · `15:10:49`). 게이트 **FAIL 실격** · C1 **0.0000h** · C2 **0.0000h**
(사망 직전 값 C1 5.31h / C2 5.24h 가 전부 리셋). 분류기 `2026-08-05-recovery-ratchet`, `classifier_ok: true`.
★**이 회차와 무관하다** — 서버 HEAD `0c75aaa3` · 고정 커밋 `0c9ccc68` 불변, 이 회차는 서버에 아무것도
배포하지 않았다.
★★★**2026-08-08 정정 — 위 「무관하다」는 반증됐다.** 근거 셋(HEAD 불변 · 고정 커밋 불변 · 배포 0건)은
전부 참이지만 그것은 **코드 축에 한해서만** 참이다. 실제 결합은 **거래소 계정**이었다 — 서버 세션
`39484a2c` 와 맥 로컬 세션 `fcf1dcbe` 가 같은 `exchange_account_id` · 같은 `BTC/USDT` · 같은 `1m` ·
같은 `strategy_id` 로 **동시에** 돌았다. 원문은 지우지 않는다 — 반증 기록이다(LESSON-081).
★★**바로 아래 「반례 후보」도 함께 정정된다** — 그 창은 오염돼 있었으므로 **[ADR-025] 를 시험한
창이 아니다.** 정확한 문장은 **「반례로 셀 수 없다」**이지 「반증 실패」가 아니다.
★★**[ADR-025] 판정에 대한 반례 후보** — ADR-025 는 노출 12.28h **phantom 0건**으로
Accepted 됐는데 이 세션은 ~5.7h 에 **2건**이고 **둘 다 사망으로 이어졌다**(신규 **[BL-633]**).
~~**다음 행동 = `scripts/soak-restart.sh`**~~ ★**2026-08-08 에 집행됐다**(세션 `a4f1cbfb`). 다음에 할 것은 맨 위 §다음 스프린트 블록이다.
★**A-1 판정 = `undecidable`(`sample_below_threshold`)** — 창 `13:40:18Z`→`15:17:20Z`(1.617h),
`N=Δagree 3` / `D=24`(agree 3 + engine_only_suppressed 18 + ledger_only_adopted 3), 문턱 30 미달.
★★★**문턱이 실제로 발화했다** — 없었다면 `3/24 = 12.5%` 를 「첫 실측치」로 적었을 것이다.
**비율을 인용하지 마라.** ★★**창은 내가 닫은 게 아니라 사망이 닫았다** — 카운터가 21분 멈춘 것을
처음엔 「발화 버스트」로 읽었다. **관측량이 0 이 되면 가장 먼저 「관측 대상이 살아 있나」를 물어라.**

★★★**[BL-621] 이 ⑴로 닫혔다 — 원인이 두 겹이었다.** 구 ATR(`cda575f2` 의 rolling SMA)과 구 비용을
**동시에** 되돌리자 `expected.json` 4지표가 **byte-identical 재현**됐다. 한 축씩만 되돌리면 세 조합이
전부 불일치라 **전건 미확정**이 된다(LESSON-077). ★이 골든이 유일하게 assert 하던 `num_trades` 는
**네 조합 전부 14** — **판별력 0** 이었다. [BL-022] — `regen_golden.py` 신설 — 와 [BL-586] — 키 리스트를
`dataclasses.fields()` **자동 유도**로 교체, 스칼라 46 전량 + 리스트 3종 digest + `RawTrade` 22 전량 — 도
함께 Resolved.
★★★**라이트 테마가 WCAG AA 미달로 배포돼 있었다** — 공개 4라우트 **AA 하드 실패 116건**이 B2 팔레트
적용으로 **2건**(전부 Clerk dev 배지, 앱 생성 **0**)이 됐다. ⇒ B2 는 「탁해지는 대가로 캐논을 산다」가
아니라 **접근성 수리**였다. 캐논↔실화면 첫 대조 표(16라우트)는
`shotgun-2026-07/checklist.md` 에 **3열(측정 축 / 사람 판단 축 / 미측정)로 분리**해 적었다.
★★**`runtime-check.mjs` 는 docs 재편(`fcc36bf7`) 이후 `ERR_MODULE_NOT_FOUND` 로 죽어 있었다** —
핸드오프의 「다크 17/17 PASS」는 **그 이후 한 번도 재현된 적 없는 숫자**였다. 고쳐서 재현했고,
뿌리는 경로가 아니라 **소유자 부재**다(신규 **[BL-631]** — 어느 게이트도 이 도구를 안 부른다).
★신규 **[BL-627]**(`regen_golden.py` 출력 경로 리다이렉트 부재) · **[BL-628]**(마케팅 푸터 5.66,
공개 라우트 canon 미충족의 단일 원인) · **[BL-629]**(`--chart-axis` 데드 토큰) · **[BL-630]**
(`.pos`/`.neg` 단독은 여전히 `td` 색에 진다 — ★핸드오프가 등재한 `td.num` 문제는 **이미 수리돼 있었다**).
★★**BL 번호 예약이 착수 시점에 이미 낡아 있었다** — 로컬 미푸시 브랜치가 BL-626 을 쥐고 있었고,
회차 도중 그것이 PR #559 로 머지돼 `origin/main` 이 `0c75aaa3`→`770d0f38` 로 전진했다.
**워커 브랜치의 `origin/main..HEAD` diff 는 그때 「내가 지시 안 한 삭제」를 보여준다 — 커밋 자체
(`git show --stat`)로 확인해라.**

**★bl003-unblock 회차가 이 블록에 더한 것 — 소크는 꺼진 채로 시작했고 아직 재기동하지 않았다.**
직전 회차가 이미 스택을 내렸으므로 이 회차는 **소크를 끄고 시작했다**. 증거 동결을 위해 로컬 `db`
서비스 **하나만** 잠깐 올렸다 내렸고, 서버는 **무접촉 관측만** 했다. **재기동은 아직 안 했다** —
계정 배타성 확보(청산 + resting 취소)는 **사용자 승인 대기**다(갈래 C).
★★★**위 사망의 근인이 바뀌었다 — [ADR-025] 반례가 아니라 이중 호스트 오염이다.** 같은 Bybit demo
계정에 서버 소크 세션과 맥 로컬 세션이 **동시에** 붙어 있었다. 판정식·분모·문턱·실측 전문은
`.claude/fleet/bl003/artifacts/verdict-corrected.md`(비추적, 원자료·타임라인·MTBF 도 같은 디렉터리)에
있고 여기엔 결론 줄만 둔다 — 소유권(서버 `exchange_exits` 의 고유 `order_link_id` 중 **로컬 원장에만**
있는 것) **7/27** · 귀속 불가 **0**, 정본 항등식 **4/4** 이고 반사실(한쪽 호스트 체결만)이 정의 4가지 어디서도 4/4 를 못 내고 **최대 1/4** 로
떨어진다. 계정 결합은 두 원장의 고유 `exchange_order_id` **27/27** 일치로 독립 확인했다.
★★**CONTROL 자신의 판정식 2개가 적대 검증에 반증됐다** — 하나는 분모가 대상 전량을 골라 **판별력 0**,
다른 하나는 잔차를 식에 그대로 넣은 **항진명제**였다(LESSON-082). 결론은 유지되고 **근거가 교체**됐다.
⇒ 이 회차의 숫자를 인용할 때는 **교체판만** 써라. 낡은 「소유권 6/6 · 산술 닫힘 3/3」은 반증된 값이다.
★**오염은 소크 세션보다 먼저 시작됐다** — 로컬 체결이 세션 생성(09:39:38) 이전에 **5건** 있었다.
⇒ 배타성 검사는 재기동 시점이 아니라 **세션을 만들기 전에** 걸어야 한다(신규 BL-634).
★★**게이트가 「측정 못 했다」를 숨기던 경로를 닫았다 — `32ea2a5d`.** 스택이 없으면 로그 경계 자리에
`Error` 문자열이 들어가고 판정기가 `ValueError` 로 죽는데, 그 `exit 1` 이 **FAIL 과 구분되지 않았다**.
이제 판독 불가 로그는 시간을 credit 받지 못하고 **`exit 2` + `UNKNOWN / 측정불가`** 로 내려간다
(predicate 테스트 37 → **41 passed**). ★이 오염은 과거 흔적이 아니라 **진행 중인 과정**이었다 —
맥 launchd `dev.quantbridge.soak-gate` 가 30분마다 돌며 스택 부재 시 오염본을 하나씩 찍고 있었다.
★**서버 `.soak` 아카이브는 오염 0벌**이다(오염본은 맥에만 있었다) — 이 수리는 복구가 아니라 **예방**이다.
★★★**[BL-003] 의 실질 선행조건은 문턱이 아니라 MTBF 다(신규 BL-641).** 두 벌을 **둘 다** 적는다 —
전 이력 n=38 · 107.12h · 최장 19.42h · 24h 도달 **0건** · 자동사망 8 · MTBF **13.39h** ·
P(24h) 16.66% · P(168h) **3.558e-06**. 2026-08-03 이후 n=14 · 60.91h · 자동사망 7 · MTBF **8.70h** ·
P(24h) **6.34%** · P(168h) **4.115e-09**. [ADR-024] 리셋 표에 의해 실격은 C1 을 0 으로 되돌리므로
「누적 168h」는 사실상 **「168시간 연속 무실격」**이고 현 사망률에서 도달 불가에 가깝다.
**그래도 문턱을 낮추지 않는다 — 병목은 문턱이 아니라 사망률이다.**
★신규 BL 8종 — 634 계정 배타성 가드 부재 P1 · 635 게이트 오염 라이브 기전 P1 **Resolved** ·
636 backlog 인덱스 표 파손 P2 · 637 `bl-audit` 우선순위 미검사 P2 · 638 `docs/archive` 부재 P3 ·
639 미조인 `exchange_exits` 상시 기저율 P2 · 640 `.metrics` 세대 누적 P3 · 641 MTBF 선행조건 P1.
★**이 파일 자신의 두 문장도 이 회차가 고쳤다**(위 배너) — 「re-pin 의 대가는 C1 이 아니라 C2」는
실격이 끼면 거짓이었고, 「`.soak/logs` 가 있다」는 서버에서 거짓이었다.

**★ci-diet 회차가 이 블록에 더한 것 — 소크는 건드리지 않았다.**
`backend/src` 는 주석·독스트링만 바뀌었고(행위 0줄) 창은 안 끊겼다. 다만 **디스크 사고로
워커가 11회 재시작**했는데 게이트가 그걸 못 봤다(위 배너 ⑶ **[확인 필요]**) — 다음 회차가
C4 의 정의를 확인할 값이다.

**★★★커밋 전에 알아야 할 것 — [BL-602].** 지금 `frontend/` 안의 json/md/yml 을 스테이징하면
**pre-commit 이 죽는다**(루트 prettier 가 `frontend/.prettierrc` 의 tailwind 플러그인을 해석
못 함, 실측 재현). `--no-verify` 는 레포 규약상 금지다 — 우회하려면 그 파일을 건드리지 않는
경로를 택하고, 택했다면 **적어라**.

**★저장소가 public 이다**(2026-08-06 전환). Actions 차단은 해소됐고, **branch protection 이
다시 가능해졌다** — `ci-cd.md` §「이 게이트가 덮지 않는 것」의 첫 항목이 재개방됐다.

### ★사전등록 — 전향 예측 (창과 함께)

> ★**이 표는 판정이 끝난 역사 기록이다**([ADR-025] Accepted, 아래 §판정) — **재사용 금지.**
> 다음 창의 사전등록은 재기동 시점에 새로 쓴다.

**새 T0 이후 노출 12h** (벽시계 아님. 세션이 실제로 돈 시간).

| #   | 관측량                                                                                                                 | 예측       | 기저(수리 전) | 12h 기대값     |
| --- | ---------------------------------------------------------------------------------------------------------------------- | ---------- | ------------- | -------------- |
| ①   | 채택 라벨 `phantom`(`classify_direction_divergence.py`)                                                                | **0건**    | 0.319/h       | 3.8건          |
| ②   | `position_divergence` 자동 사망                                                                                        | **0건**    | 0.159/h       | 1.9건          |
| ③   | **양성 대조** — 조건부 진입 발주(원장)                                                                                 | **≥ 40건** | 6.5/h         | 78건           |
| ④   | **양성 대조** — `qb_live_conditional_fill_ownership_total{outcome=engine_only_suppressed\|ledger_only_adopted\|agree}` | **≥ 5건**  | —             | ≈30~60건(추정) |

★★★**④는 반드시 T0 스냅샷과의 차분으로 읽어라.** prometheus multiprocess mmap 이 워커
재기동을 넘어 살아남아 절대값에 **수리 전 누적이 섞여 있다**(이 레포가 「출생일 다른 counter 는
절대값 비교 불가」로 이미 덴 함정). **스냅샷(T0 `12:44:04Z` 직후, `12:49Z` 실측 — 이 값을 빼고 세라):**

```
outcome=agree 34.0 outcome=ledger_only_adopted 2.0 outcome=engine_only_suppressed 4861.0
```

★계상 단위 교체를 프로덕션에서 확인했다 — 교체 후 3 tick 연속 증분 **0**(교체 전 +121/tick).
★그 직후 새 세션에서 `agree` 와 `ledger_only_adopted` 가 **각 +1** 발화했다 — 고친 단위로도
관측량 ④ 가 살아 있다는 뜻이다(위 스냅샷은 그 발화를 **포함**한 값이다).

★★**④의 계상 단위를 프로덕션에서 고쳤다 — 그전 설계는 판별력이 0 이었다.** 처음엔 재생의
**모든 봉**에서 셌는데, 소크에 올려 보니 warmup 300봉을 매 tick 다시 세어 카운터가 사건과
무관하게 **tick 당 정확히 +121** 로 자랐다(45초 간격 실측 121 → 242 → 363). 그 값으로는
「이 tick 에 [BL-595] 순간이 있었나」를 물을 수 없다. ⇒ **이 tick 이 실제로 판정하는 봉
(마지막 봉)에서만** 센다. 한계: 더 앞선 봉에 얹힌 원장 체결은 **적용되지만 계상되지 않는다**
(행위는 그대로).

**★정직하게: 12h 는 단독으로 기각선에 아슬아슬하다.** ①이 0 이면 p≈0.022(기각), ②는 p≈0.15
(기각 못 함). 갚는 방법은 **누적**이다 — 같은 T0 창이 24h 로 자라면 ①의 p≈0.0005 가 되고
게이트 C2(최장 연속 24h)와 창이 합쳐진다.

**③④가 방어선이다.** ③이 무너지면 기계가 멈춘 것이지 고쳐진 게 아니다. ④가 0 이면
**판별력 0** — 「고쳤다」가 아니라 「그 경로를 안 밟았다」이고 그렇게 적어야 한다.
★`replay_lag` 은 **음성 대조로 쓰지 마라** — 이 수리가 그 채널도 줄이므로 「계측이 살아 있다」의
증거가 못 된다.
