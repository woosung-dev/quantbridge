# fe-perf-quartet (2026-08-09)

> `docs/status.md` 에서 강등된 회차 기록이다(2026-08-10 docs-context-diet).
> 강등 이유는 크기가 아니라 **역할**이다 — 끝난 회차의 회고는 진입점이 아니다(ADR-026 §3).
> 원문 위치 = `git show 762e1297:docs/status.md` 의 120-217행.

---

### ★fe-perf-quartet (2026-08-09) — [BL-662]~[BL-665] 전건 종결 · 반증 9건

| 축            | 착수 시                                                  | 종료 시                                                                                  |
| ------------- | -------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| `bl-audit`    | ACTIVE **152** · PARTIAL 20 · RESOLVED 51 · 전체 **223** | ACTIVE **149** · PARTIAL **21** · RESOLVED **55** · 전체 **225**(신규 [BL-666]·[BL-667]) |
| `backend/src` | —                                                        | **0줄** (전 변경이 `frontend/`)                                                          |
| 소크          | C1 **29.34h** · 창 14.02h · 실격 **0** · 세션 `de3db35a` | **무접촉** · 종료 시 C1 **31.17h** · 창 **15.86h** · 실격 0 · C5 전건 ✓ · 창 시작 불변   |
| FE 게이트     | —                                                        | typecheck 0 · lint 0 · vitest **209파일 1307건 전건 통과**                               |

★★★**측정 결과 — `/dashboard` 클라이언트 JS**

|      | 청크 | 바이트                              | 9종 문자열 지문 | 전이 의존                    |
| ---- | ---- | ----------------------------------- | --------------- | ---------------------------- |
| 전   | 17   | 1,140,321 B                         | **8/9**         | `react-hook-form` · `sonner` |
| 후   | 13   | **954,447 B**                       | **0/9**         | `sonner`                     |
| 델타 | −4   | **−185,874 B (−181.5 kB · −16.3%)** |                 | `react-hook-form` 제거       |

양성 대조 `/trading` 은 **8/9 유지 · 1,154,387 → 1,155,162 B**(신규 leaf 파일분 +775 B).

★**소크는 이 회차 내내 무접촉이었고 창이 역대 최장을 갱신했다** — 종료 시 **15.86h** 로 종전 최장
15.30h(세션 `a4f1cbfb`)를 넘었다. C2(24h)까지 **약 8시간** 남았고 39세션 통틀어 도달은 여전히 **0건**
이다. 서버에 한 것은 게이트 읽기 **2회**(착수·종료)뿐이다.

★★★**최대 산출은 반증 9건이다 — 그중 4건은 내가 만든 것이다.**

| 무엇이 반박됐나                          | 누가      | 어떻게                                                                                                                                                                                                                                                                       |
| ---------------------------------------- | --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **[BL-662] 처방의 도구**                 | 실측      | `@next/bundle-analyzer` 는 **이 레포에서 아무것도 못 낸다.** 빌드 로그: `not compatible with Turbopack builds, no report will be generated`. Next 16 은 Turbopack 이 기본 빌더다                                                                                             |
| **측정 기반 자체**                       | 실측      | Next 16 Turbopack 은 `app-build-manifest.json` 을 **안 만들고** 라우트 표에 `Size`/`First Load JS` **컬럼이 없다**. 정본은 `__RSC_MANIFEST`(`page_client-reference-manifest.js`)                                                                                             |
| **「9종 ≈ 3,000줄」**                    | 실측      | 최대 항목 `test-order-dialog`(605줄)는 **양쪽 라우트 청크 어디에도 없다**. 실측 도달은 8종이고 전부 한 청크(84,344 B)에 몰려 있었다                                                                                                                                          |
| ★**내 측정기 1판**                       | 음성 대조 | `clientModules` 를 뒤졌더니 **양성 대조 `/trading` 에서도 0/9**. 9종은 client boundary 진입점이 아니라 `"use client"` **안쪽**이라 거기 안 뜬다. **양성 대조가 없었으면 「효과 0」을 그대로 보고했다**                                                                       |
| **[BL-664] 본문**                        | 코드 대조 | 「이 화면과 **무관한** `useStrategies({limit:100})`」가 거짓 — `trading-cockpit.tsx:76-80` 이 **직접 호출**한다. 무관한 예 셋 중 하나가 자기 쿼리였다                                                                                                                        |
| **[BL-663] 본문의 인과**                 | 코드 대조 | 5초 틱은 **유일한 원천이 아니다.** 같은 컴포넌트가 `useRealtimeStore(useShallow(…))`(`unrealized.ts:122-130`)로 WS ticker 를 구독하고 `applyTicker` 가 매 틱 새 객체를 넣어 shallow 비교가 깨진다 ⇒ 「불리언만 뽑기」는 **활성 세션이 있으면 효과 없고 없으면 볼 것이 없다** |
| ★**내 [BL-664] 1판이 기능을 깼다**       | codex     | 셋만 무효화해 §08 `useAlertRules`(`alert-rules` 루트)가 **빠졌다**. 종전 무필터 호출은 그것도 갱신했으므로 **좁힌 게 아니라 깬 것**이다 ⇒ 무효화 범위를 좁힐 때는 **렌더 트리 전체**의 훅을 세라                                                                             |
| ★**내 [BL-665] 가 CSV 를 어긋나게 했다** | codex     | 배지는 즉시값, 표·CSV 는 200ms 늦은 값 ⇒ 치고 200ms 안에 CSV 를 누르면 「필터 1개」인데 **전량이 나간다**. 즉시성이 필요한 건 **입력창 하나뿐**                                                                                                                              |
| ★**내 [BL-663] 주석이 과장**             | codex     | 「§01~§08 이 재조정되지 않는다」가 거짓 — **§08 은 아니다**(`session-diagnostics.tsx:241-242` 자체 구독 + `realtime-bridge.tsx:62` 가 ticker 마다 `recordEvent`). 지키는 것은 §01~§07 이다                                                                                   |

★**변이 6건으로 회귀 시험을 검사했다**(M1 무필터 복원 · M2 안정성 뒤집기 · M3 디바운스 제거 ·
M4 5초 틱 코크핏 복원 · M5 alert-rules 제거 · M6 배지 즉시값 복원) — **6/6 정확히 빨갛다.**
★**착수 시점에 거래 검색은 단위·e2e 커버가 0건이었다** — 「검색이 여전히 거른다」조차 못 재고 있었다.
★**zsh 함정 2건 재발** — ⑴ Bash 도구 cwd 가 `frontend` 로 남아 `du -sh frontend/.next` 가
`frontend/frontend/.next` 를 재서 **「캐시 없음」이라는 거짓 판정**을 냈다(실제 372MB) ⑵ 파이프 뒤
`$?` 가 `tail` 것이라 변이 시험이 **0건 실행되고도 통과처럼 보였다**. 둘 다 절대경로·파일 리다이렉트로 교정.
★**`rm -rf` 가 권한에서 두 번 거부돼** `.next` 를 스크래치패드로 **mv 격리**했다(되돌릴 수 있고 효과 동일).
★★**커밋이 실제로 막혔고 그것이 기존 결함을 드러냈다 → 신규 [BL-667].** `frontend/.prettierrc` 가
선언한 `prettier-plugin-tailwindcss` 가 **`frontend/node_modules` 에만** 있는데 루트 lint-staged 는
json·md 를 **레포 전역**으로 잡는다 ⇒ `frontend/**` 의 json 을 스테이징하면 pre-commit 이 죽는다.
손대지 않은 파일로 재현된다(`npx prettier --check frontend/tsconfig.json`). ★**증상이 원인을 숨긴다** —
lint-staged 가 eslint 태스크를 `[KILLED]` 로 찍어 **eslint 실패처럼 보인다.** 실제 실패는 prettier 뿐.
★**내 커밋 분할도 한 번 틀렸다** — `trading-cockpit.tsx` 의 [BL-663] 변경이 [BL-664] 커밋에 딸려 들어가
**아직 없는 파일을 import 하는 중간 커밋**이 됐다. 되감아 둘을 한 커밋으로 합쳤다.
**한 파일에 두 BL 이 걸리면 커밋은 나눌 수 없다** — 나누려면 파일을 먼저 나눠야 한다.
★**em-dash 래칫(`design-canon-source.test.ts`)에 두 번 물렸다** — **주석에 쓴 `—` 도 센다**(노출 산문만
세는 줄 알았다). `features/backtest/utils.ts` 허용 2인데 4가 됐다. 한글 문서 습관이 그대로 코드 주석에
들어가면 이 게이트가 문다. **FE 주석에는 `—` 대신 `.` 이나 `·` 를 써라.**

★★★**e2e authed 2건 실패는 내 회귀가 아니다 — 음성 대조로 확정했다.**
`sprint46-tier1-critical.spec.ts:69`(#1 backtest form 422) · `sprint46-tier3-nth.spec.ts:489`(#20
friendly_message) 둘 다 **backtest form 의 POST 가 안 나가는** 같은 뿌리다. `git checkout 85970b83 --
frontend/src` 로 **main 코드에서 같은 두 건을 태우니 똑같이 실패**했다(1 passed / 2 failed 동일).
★내 diff 는 `backtest-form.tsx` 를 안 건드리고, 그 파일이 `utils.ts` 에서 쓰는 것은 **`formatDateTime`
하나**인데 그 함수는 무변경이다.
★★**CI 대조가 결론을 좁혔다 — 코드가 아니라 로컬 환경이다.** PR **#574 에서 `e2e` 가 SUCCESS** 였고
그 뒤 #575·#576·#577 은 **전부 문서 전용**(`e2e` SKIPPED)이라 CI 가 초록으로 본 FE 코드가 지금 main
과 같다. ⇒ 같은 코드가 CI 에선 통과하고 이 맥에선 실패한다. **판별자는 격리 스택의 데이터**이고
(POST 가 아예 안 나가므로 폼 검증 단계에서 막힌다) 이 회차는 거기까지만 좁혔다 → [BL-668].
★**이 대조를 안 했으면 「내가 깼다」거나 「레포가 깨졌다」 중 하나를 잘못 적었을 것이다.**
★**별개 flake 1건도 나왔다** — `trading-ui.spec.ts:108`(kill switch 황색 배너)이 전체 스위트 2회 중
**1회만** 실패했고 **격리 3/3 통과**했다. 이 회차가 실제로 만진 `kill-switch-banner.tsx` 의 시험이라
회귀를 의심해 3회 태웠다. **항상 실패하는 2건과 다른 현상이니 묶지 마라**(같은 [BL-668] 안에 구분해 적었다).

★★**게이트 최종 = `e2e authed` 를 뺀 전건 PASS.** BE pytest · FE build · e2e chromium · e2e
design-canon · CI 재현 3종 · 스킬 4종(vercel·screen·codex·G9) 전부 초록. `e2e authed` 만 위 사유로
빨갛고 **그것을 초록으로 만들지 않았다** — 환경 축이라는 근거를 남기고 [BL-668] 로 넘긴다.

- ★★★**2026-08-09 status-triage-mass 가 ⓪ 표 후보 G 의 전제를 반증했다.** G 는
  「**ACTIVE 는 열려 있는 수가 아니라 닫혔다고 선언되지 않은 수다**」라고 적었는데, 상태줄 없는
  **116건 전량**을 코드 대조하니 **100건(86%)이 진짜 열려 있었다**. 종결 6 · 부분 7 · 사용자대기 3
  ⇒ **r = 5.17%**. 원장은 부풀지 않았다 — 그냥 크다. 상태줄 없는 섹션은 이제 **0**이고,
  다음 세션은 116건을 **다시 재판정하지 않는다**(그것이 이 회차가 판 값이다).
- ★**Phase 2B(XS/S 실작업)는 하지 않았다.** 계획은 `r<20%` 갈래에 「남은 시간을 실작업에」를 걸었고
  조건은 성립했다. 안 한 이유 = 착수 뒤 codex·2축 리뷰가 **판정 3건을 반증**해 그 정정과 재생성에
  예산을 썼다. 실작업은 ⓪ 표 **J** 로 이월한다 — **미이행이지 재량이 아니다.**
- ★**`/vercel-react-best-practices` 교차검증이 미등재 결함 4건을 냈다** → [BL-662]~[BL-665].
  이 회차 diff 에는 React 가 0줄이라 diff 리뷰는 무의미했고, 대신 **FE 근거를 가진 판정 31건**에
  그 기준을 걸었다. 그 과정에서 **내 판정문 2건이 사실 오류**로 잡혔다(아래 표).
- ★★★**bl003-mainnet-runbook 이 P0 의 ⑴산출물 축을 닫았다.** 쓸 수 있는 문장은
  「P0 가 전진했다」가 **아니라** **「게이트가 열릴 때 4~5h 를 더 기다리지 않아도 되게 만들었다」**다.
  [BL-003] 의 완료는 두 축이고 ⑵Trigger(`soak-gate.sh` PASS)는 **그대로 열려 있다**(C1 26.54h/168h = 15.8%).
- **왜 지금 다음 회차가 [BL-003] 이 아닌가** — 남은 것이 ⑴ Trigger(시간) ⑵ runbook §8 의
  **[확인 필요] 5건**(거래소 접촉 = 사용자 승인) ⑶ cutover 코드 2곳(`backend/src`)뿐이다.
  셋 다 **지금 이 세션이 혼자 밀 수 있는 것이 아니다.**
- ★**소크 창 자체가 실험이다** — 실격 0 since `2026-08-07T15:10:49`, C1 **26.54h** 는 [BL-595]/[BL-634]
  수리 이후 **첫 관측**이다. 그 앞은 24h 도달 **0/39** · 최장 19.42h. **사망률이 실제로 내려갔나**가
  이 창의 유일한 미판정이고, `backend/src` 의 사망 축을 건드리면 처치를 바꾸는 것이다.
- **비목표** — ⑴ cutover 코드 변경(runbook §2 — Trigger 전엔 의미 없다) ⑵ 거래소 실호출
  (`--confirm` 은 사용자 승인 뒤 별도 step) ⑶ 신규 BL 사냥(발견하면 등재만).
