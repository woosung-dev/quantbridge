# entry-set-divergence — 사전등록 (측정 전 커밋, LESSON-072 준수)

> 2026-08-06T14:0xZ 등록. btgap 후속 — `BL-604` 진입 집합 격차의 동인 3후보
> (⑴ 롤링 300봉 워밍업 ⑵ 재무장 트리거 드리프트 ⑶ 세션 공백) 기여 분리.
> **이 표는 측정 전에 커밋됐고, 등록 직후 eval(codex)이 기각영역 도달 가능성을 검증한다.**
> 검증에서 항진명제로 판명된 지표는 측정 전에 한해 교체를 허용한다(측정 후 교체 금지).

## 창 (frozen)

- **캡처 창**: `2026-08-04T18:00:00Z ~ 2026-08-06T13:30:00Z` — Bybit v5 공개 kline, perp/spot
  1m 각 **2,610봉**. perp sha256 `6c57bdfe35044d88…`, spot sha256 `60eb1255918c016a…`
  (`.claude/fleet/entryset/artifacts/ohlcv_*`, 비추적).
- **채점 창**: `2026-08-05T00:34:00Z ~ 2026-08-06T13:30:00Z` (첫 세션 생성 ~ 캡처 끝).
- **창 연장 근거(1문장)**: 소크가 무중단으로 계속 돌아 신규 체결 8건이 쌓였고(filled orders
  85→93·exits 172→180), 기존 행 불변을 사전 실측(orders 85/85 필드 동일 · exits 172/172
  동일 · CSV 2,460행 접두 바이트 동일)했으므로 비교 가능성을 잃지 않고 표본만 커진다.
- **원장 덤프**: read-only SQL, `state='filled'` + 창 필터 — orders 93 · exits 180 ·
  sessions 4 · live_signal_states 4. 세션 공백 3구간(실험 B 필터):
  `08-05T09:12:53~11:57:31` · `08-05T12:42:42~12:44:25` · `08-06T01:04:46~01:06:46`.

## 집합 정의

- **B** = 전구간 백테스트 진입 집합 — `btgap_compare.py run` (기존 도구), 채점 창 필터.
- **R** = 라이브 프로토콜 재생 진입 집합 — 신규 `replay`: 매 봉 t 마다 직전 300봉 창으로
  `run_live`(Option B, `backend/src/strategy/pine_v2/event_loop.py:416`)를 재실행해 마지막 봉
  이벤트만 채택. **원장 인자 4종 미주입**(`ledger_seed_legs`·`ledger_conditional_fills`·
  `position_epoch`·`emit_from_bar_time`) — 백테스트 순수성.
- **R′** = R 에서 세션 창(위 4창) 밖 진입을 제거한 집합.
- **L** = 라이브 실측 진입 — orders 덤프의 `idempotency_key` 를 `parse_live_entry_key` 로
  파싱(엔진 무경유 오라클). 예상 |L|≈92(93 − 수동 flatten 1), 정확 수는 도구가 확정.
- **매칭 규약**(직전 회차 확정본 재사용): 키 = `bar_epoch` ±3봉 + 방향 + `trade_id`
  (`ENTRY_BAR_TOLERANCE_BARS=3`), 가격 근접은 술어가 아니라 검증용. `filled_at` 금지.
  X↔L 매칭률 = |매칭쌍| ÷ |L| (분모 L 고정, 행별). B↔R 는 분모 |B| 를 표에 병기.
- ★R 의 「신호봉」과 L 의 `bar_epoch`(장전봉)가 같은 단계인지 측정 전에 코드로 확인하고
  매칭 의미를 보고서에 적는다(직전 실측: 체결은 장전봉 +1~2봉).

## 사전등록 표

| #   | 관측량                                             | 예측                                                                                                         | 판별력(미충족 시 미판정 강등)  | 판정                                    |
| --- | -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | ------------------------------ | --------------------------------------- |
| ①   | R↔L 매칭률 − B↔L 매칭률 (같은 연장 창·같은 분모 L) | **+15%p 이상** (동인 ⑴ 이 주범이라는 가설)                                                                   | \|L\| ≥ 50                     | 측정 후                                 |
| ②   | \|B\| − \|R\| 의 부호·자릿수                       | **양수**(롤링 워밍업이 pivot 상태를 리셋해 진입을 줄인다) · 자릿수 = 수십 건                                 | B·R 모두 산출 성공 + ④(a) 통과 | 측정 후                                 |
| ③′  | 정밀도 변화 M(R′,L)÷\|R′\| − M(R,L)÷\|R\| (실험 B) | **≤ +10%p** (⑶ 세션 공백은 부차 — 공백 구간 R 진입이 R 전체에서 작은 몫)                                     | \|R\|−\|R′\| ≥ 5건             | 측정 후                                 |
| ④   | 음성 대조                                          | (a) 같은 CSV·같은 모드 2회 실행 R digest 동일 (b) 연장 덤프·CSV 에서 기존 행 불변 (c) 소크 창 세션 전후 연속 | 전/후 실측 병기                | (b) **사전 실측 통과** · (a)(c) 측정 후 |

- 비율 지표는 **행별-abs 정의**(Σ후-abs 부호 상쇄 금지 — 직전 실측).
- ★예측이 빗나가도 실패가 아니다 — 「⑴ 이 주범이 아니다」 역시 판별력 있는 결과다.

## 측정 결과 · 판정 (측정 후 기록 — CONTROL 재측정 정본, eval2 독립 재현 바이트 동일)

**4집합**: |B|=216 · |R| = cond 250 + fill 216 (2,216봉 재생, 봉당 3채널) · |R′(cond)|=231
(세션 필터 제거 19, boundary=bar_close) · |L|=90 (eval2 독립 파싱 일치).

| 대조       | strict | loose | 쌍  | amb | 매칭률(분모)                                     |
| ---------- | ------ | ----- | --- | --- | ------------------------------------------------ |
| B↔R(fill)  | 0      | 194   | 194 | 22  | 89.8% (\|B\|=\|R\|=216) · **key multiset EQUAL** |
| B↔L        | 0      | 39    | 39  | 1   | **43.3%** (\|L\|=90)                             |
| R(fill)↔L  | 0      | 39    | 39  | 1   | **43.3%** (\|L\|=90)                             |
| R(cond)↔L  | 49     | 32    | 81  | 4   | **90.0%** (\|L\|=90)                             |
| R′(cond)↔L | 49     | 32    | 81  | 4   | 90.0% (\|L\|=90)                                 |

| #   | 판정                      | 근거                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| --- | ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ①   | **형식 적중 · 가설 기각** | 관측 +46.7%p(90.0−43.3, \|L\|=90≥50). 그러나 정정 2차의 한정 의무(체결봉 병기)에서 **R(fill)↔L − B↔L = +0.0%p**(39쌍 완전 동일) — 상승분 전부가 단계 정렬(키 규약) 성분. eval2 음성 대조: R(cond)를 체결봉 L 과 맞추면 40.0%로 **역전 하락**. ⇒ 「⑴ 이 주범」은 **거짓**                                                                                                                                                                                             |
| ②   | **예측 반증(판별력 有)**  | \|B\|−\|R(fill)\| = **0**, `key_multiset.equal=true`(산출물 직접 증명). 롤링 300봉 워밍업은 이 전략·이 구간에서 진입 집합을 **전혀** 바꾸지 않는다 — pivot 상태가 300봉 창 안에 수렴                                                                                                                                                                                                                                                                                 |
| ③′  | **적중(한정 병기)**       | 81/231 − 81/250 = **+2.67%p** ≤ +10%p (제거 19 ≥ 5 충족). ★eval2 한정: 공백 구간 R 19건 중 매칭에 쓰인 것 **0** — 필터가 지우는 구간엔 L 이 없어 판별 표면 자체가 좁다(0 아닌 값이 가능한 자리 = 세션 경계 ±3봉 L 3건뿐)                                                                                                                                                                                                                                             |
| ④   | **(a)(b)(c) 전건 PASS**   | (a) replay 2회 digest 동일 + **entries 재계산 대조**(eval2 가 「저장값 비교는 entries 변조에 무방비」 절차 결함을 고장 주입으로 발견 — 재계산 방식으로 강화) + eval2 독립 실행 바이트 동일. (b) 공통 행 필드 불변(orders 85/85 · exits 172/172 · CSV 접두 동일) — 행 집합은 비진입 2건 소급 등장(정정 2차 기록, 진입 축 무영향). (c) 소크 창 연속 — 세션 시작 시 연속 12.36h → 마감 시 **14.26h**(같은 창 `c160a1a9`, 실격 0, C4/C5 전건 ✓, final-gates 이후 재판정) |

**동인 분해(건수)**: ⑴ 롤링 워밍업 = **0건** · ⑶ 세션 공백 = **0쌍**(19건 제거에도 쌍 불변)
· ⑵+거래소 게이트 상계 = **미매칭 잔차 9/90** — right_only 5(그중 `condmkt` 시장가 전환
2 = gen 이 상계로 선언한 거래소 게이트 갈래 · `cond` 3 = 트리거 드리프트/상태 후보) +
ambiguous 4(±3봉 다중 후보).

★**재프레임(이 회차의 핵심 결과)**: BL-604 의 「백테스트가 예측 못 한 라이브 진입 46건」은
존재 격차가 아니라 **키 규약이 만든 관측**이었다 — B 는 체결봉, L 은 장전봉에 키가 찍히고
체결 지연(장전→체결)의 57%가 ±3봉 허용창 밖이다(eval2: 매칭 여부가 지연 ≤3봉과 오즈비
166 으로 붙는다). 장전봉끼리 맞추면 90/90 중 **81 매칭**, 어느 규약으로도 붙지 않는 L 은
**2/90**. 백테스트↔라이브 신호 축은 갈라져 있지 않다 — 격차의 실체는 매칭 규약(해소) +
잔차 9건(⑵·게이트 상계) + 비용 축(BL-603, 별건)이다.

### 정정 이력 (측정 전, 2026-08-06T14:5xZ)

- **③ → ③′ 교체**: 원안 「R′↔L − R↔L(분모 L 고정) ≤ +10%p」는 R′⊂R + 분모 고정이라
  변화가 구조적으로 ≤ 0 — **이상화에서 항진명제**(CONTROL 자체 발견 + eval(codex) 기각영역
  검증 교차 일치. eval 은 `match_entries()` 의 ambiguous 재배분 경로로 완전 항진은 아니되
  「단순 subset 산술로 판정 금지」를 판정). LESSON-072 의 절차대로 **측정 전에** 분모를
  각 집합 크기로 바꾼 정밀도 기준으로 교체했다. 측정 시 ambiguous 수와 제거 전후 pair
  증감을 병기한다(eval 권고).
- eval 판정 요약: ① 기각가능(R key = `last_bar_time` 정렬 조건 — gen 이 코드로 확인) ·
  ② 기각가능(부호 비고정 — `PersistentStore` fresh 리셋이 진입을 늘릴 수도) ·
  ④(a) 조건부(유효 계약 내 불일치 = 측정 무효 신호) · ④(b)(c) 기각가능.

### 정정 이력 2차 (측정 전, 2026-08-06T15:1xZ — eval2 독립 실측 반영)

- **|L| = 90 으로 정정**(원문 「예상 ≈92 = 93 − 수동 flatten 1」은 오류): 수동 flatten 류
  비진입(NULL key·reduce_only) 행이 **3건**이고, 그중 2건은 직전 회차 덤프에 없다가 이번에
  **소급 등장**했다(둘 다 직전 캡처보다 먼저 체결된 행 — 세션 비활성화 직후 2.0s/5.3s 의
  정리 주문). 「신규 체결 8건」도 「신규 진입 6건 + 비진입 소급 2건」으로 정정.
  ⇒ **④(b) 는 「공통 행 필드 불변(84/84)」은 참이나 「행 집합 불변」은 거짓** — 원인은
  [확인 필요](직전 덤프 SQL 미보존, `DISTINCT ON(idempotency_key)` 류가 NULL 3행을 1행으로
  접었다는 가설이 관측과 정합). 이번 회차 덤프(`state='filled'` 순수 필터)를 정본으로 한다.
- **① 해석 한정(관측량·문턱 불변)**: B 는 **체결봉** 키(`BacktestTrade.entry_time`), L·R 은
  **장전봉** 키 — B↔L 만 단계가 한 칸 어긋난 채 ±3봉으로 재므로, ① 의 차이에는 동인 ⑴ 외에
  **단계 불일치 성분**이 섞인다(직전 실측: B↔L 매칭 여부가 「라이브 체결 지연 ≤3봉」으로
  설명 — 오즈비 166.2, Fisher p=1.4e-16; 같은 집합에서 키 규약만 바꾸면 44.4%↔100%).
  ⇒ 판정 시 **B↔L(체결봉 키잉) 재매칭을 반드시 병기**하고, ⑴ 의 기여는
  「R↔L − B↔L(체결봉)」 축으로 읽는다. R↔L(장전) vs R↔L(체결) 일치 여부로 R 의 단계
  정합도 함께 검증(갈리면 R 정의 위반). 매칭 지연 감도표(±3 대신 관측 p90=11봉) 병기.
- **R′ 창 규약 고정**: 세션 창 포함 판정은 **봉 종료 시각(`bar_epoch`+60s)** 기준. 봉 시작
  기준이면 세션 첫 진입 2건(L 실측 — 봉 시작이 세션 생성보다 22.7s/25.2s 이름)이 인공적으로
  제외된다. 이 2건은 어느 규약이든 각주로 남긴다.
- 세션 공백 정정: 3구간(7.596%) + **머리 22.676s**(채점 창 시작~첫 세션 생성). 공백에 완전
  포함되는 봉 166개 — ③′ 판별력 조건 충족 전망(기대 7~16건).
- ⑵(트리거 드리프트)는 직접 실험하지 않는다 — R↔L 잔차로 상계만 적는다(A/B 설계상
  잔차 = ⑵ + 원장 체결 권한(ADR-025) + 미모형 요소).
