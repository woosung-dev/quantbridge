# review-and-merge (2026-08-10)

> `docs/status.md` 에서 강등된 회차 기록이다(2026-08-10 docs-context-diet).
> 강등 이유는 크기가 아니라 **역할**이다 — 끝난 회차의 회고는 진입점이 아니다(ADR-026 §3).
> 원문 위치 = `git show 762e1297:docs/status.md` 의 68-119행.

---

### ★review-and-merge (2026-08-10) — PR #579·#580 머지 · 신규 [BL-683]~[BL-686]

| 축            | 착수 시                                           | 종료 시                                                                                                                                  |
| ------------- | ------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| main          | `d277a54a`                                        | `98131541`(#579) → `70fd96fb`(#580) — **둘 다 merge commit**                                                                             |
| `bl-audit`    | active **154** / 전체 233                         | active **158** / 전체 **237** · RC=0 · UNKNOWN 0                                                                                         |
| `backend/src` | —                                                 | **이 회차 0줄** (머지된 #579 가 `close_service.py` 를 바꿨다)                                                                            |
| 소크          | C1 **34.17h** · 창 **18.86h** · 실격 0 · C5 6/6 ✓ | **무접촉**(게이트 읽기 2회) · 종료 시 C1 **40.11h** · 창 **24.80h** ⇒ ★★★**[ADR-024] C2 첫 충족** · 실격 **0** · C5 6/6 ✓ · 창 시작 불변 |
| CI            | #579 backend(a) 진행중                            | #579 전건 pass · #580 **rebase 후 재실행도** e2e 4m57s pass · frontend 3m25s pass                                                        |

★**rebase 충돌은 0 이었다.** `git merge-tree` 로 미리 예측(Auto-merging `docs/backlog.md`)하고
실제 rebase 도 RC=0. 양쪽 BL 섹션 10건이 각 1건씩 살아남았다(중복 0). ⇒ **겹치는 파일이
하나뿐이면 레인을 나눈 값이 여기서 회수된다.**

★★★**2026-08-10T00:04Z — [ADR-024] C2 가 처음으로 충족됐다. `24.8004h / 24h ✓` · 실격 0.**
40세션 통틀어 **첫 도달**이다. 종전 최장은 19.42h(2026-08-08 이전) → 이 회차 직전 15.86h →
**24.80h**. C1 은 **40.11h / 168h (23.9%)** 이고 C5 는 6/6 ✓, 창 시작은
`2026-08-07T15:10:49Z` 그대로다. **[BL-595]/[BL-634] 수리 이후 사망률이 실제로 내려갔다는
첫 양성 신호**다 — 단 C1 이 168h 를 채우기 전에는 아직 판정이 아니다.

~~★★★**소크 C2 는 24h 를 못 넘겼다** — 15:08Z 15.86h → 18:08Z **18.86h**, 24h 까지 5.14h
남았고 도달은 여전히 0건이다.~~ → ★**이 문장은 같은 세션 안에서 낡았다.** 착수 실측(18:08Z)
시점에는 참이었고 **5.93시간 뒤 종료 실측에서 거짓이 됐다.** ⇒ **소크 수치는 문서에 박는
순간부터 우측 절단된다**(LESSON-084 재발). 인용하지 말고 **게이트를 돌려라.**

★★★**최대 산출 — 어젯밤 레인이 남긴 반증 하나가 절반만 참이었다.**

| 무엇이 반박됐나                                      | 누가                | 어떻게                                                                                                                                                                                                                                                                                                                                                                                               |
| ---------------------------------------------------- | ------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ★★★**[BL-545] 「순포지션이 leg 수를 안 들고 있다」** | Spec 축 → 코드      | **판정부에서 거짓.** `_carried_position_size`(`tasks/live_signal.py:712-746`)는 `open_trades` 리스트를 **leg 단위로 순회**하며 leg 마다 `qty` 를 읽어 net 을 누적한다 ⇒ leg 수도 leg 별 수량도 **이미 그 자리에 있다.** 「자료가 없다」가 아니라 **「있는 자료를 버리고 있다」**이고 비용이 다르다. ★단 **tick 관측부**(`:925-938` `position_size` 스칼라)에는 진짜로 없다 — **두 자리를 섞지 마라** |
| **되돌림 커밋의 인용 2건**                           | 코드 대조           | `strategy_state.py:861` 은 [BL-104] pyramiding cap 주석이고 `providers.py:403` 은 `load_markets()` 다(절삭은 **`:404`**). 엔진 무절삭의 근거는 특정 줄이 아니라 **파일 1,321행에 절삭 호출 0건**이라는 사실이다 ⇒ **근거를 줄 번호로 적을 땐 그 줄을 열어 봐라**                                                                                                                                     |
| ★**[BL-661] 「부분」의 경계**                        | Spec 축 → 코드      | 조건부 조회가 `close_service.py:103` 의 `if not positions:` **안에만** 있다. 포지션이 있으면 조회를 **한 번도 안 하고** CLI 는 `✓ 청산 주문 접수` + **exit 0** 이다 ⇒ 같은 거짓 성공이 **더 흔한 경로에** 남았다 → [BL-684]. 백로그 본문이 요구한 것은 「포지션과 조건부를 **함께** 보고」였다                                                                                                       |
| ★★**#580 이 `/trading` 을 prerender 밖으로 밀었다**  | Standards 축 → 실측 | `trading.html` **65,097 → 41,439 B(−23,658)** · `aria-label` 1건→**0건**. **음성 대조 = 16개 라우트 중 15개가 바이트 동일(+0)** ⇒ 빌드 조건 차이가 아니다. 클라이언트 JS 는 **+0.48%뿐** — **HTML 축과 JS 축을 섞지 마라** → [BL-683]                                                                                                                                                                |
| **BL-397 검증 숫자 2건**                             | Spec 축 → 실측      | e2e `report-section-anchors.spec.ts` 「2건」이 실제 **3건** · `trading-cockpit.test.tsx` 「신규 6」이 실제 **7**(11→18). ★내가 처음 센 「오류 3건」도 틀렸다 — `live-session-deeplink` 는 3건이 맞는데 `test(` 문자열 오탐으로 4로 셌다. **세는 도구도 대조가 필요하다**                                                                                                                             |

★★★**변이 4/4 red 를 통과한 구현이 codex P1 2건을 품고 있었다** ([BL-545] 되돌림).
`tol = max(qty_step, larger*0.001)` 은 표적 변이 **4/4 를 빨갛게** 만들었는데, 그 뒤 적대 리뷰가
낸 P1 2건이 **둘 다 숫자로 재현**됐다 — pyramiding 10 leg 에서 **정상 세션을 죽이고**(tol 0.005999
→ `size`), 최소 수량 1 lot 차이를 **삼킨다**(tol 0.001 → `None`). ⇒ **변이는 「내가 상상한 실패」만
잰다.** 변이 전건 red 는 「시험이 이 변경을 지킨다」이지 **「이 변경이 옳다」가 아니다.**

★★★**배선 변이가 GREEN 으로 탈출했다** (`backend/AGENTS.md` §10 item 2).
단위 시험이 순수 함수를 **직접** 부르면 호출부를 바꾸는 변이는 아무것도 red 로 못 만든다.
같은 계열이 이 회차 리뷰에서도 잡혔다 — CLI(`live_session_admin.py:395-397`)가 5개 키를
**하드 서브스크립트**하는데 생산자 시험은 3개만 보므로 `order_link_id`→`link_id` 변이는
**양쪽 다 red 0** 이고 프로덕션만 `KeyError` 다(레인이 [BL-672] 로 이미 등재). ⇒ **「그 함수」가
아니라 「그것을 쓰는 경로」를 재는 케이스가 최소 1개 있어야 한다.**

★**기등재는 재등재하지 않았다** — 되돌림 커밋이 이미 [BL-669]~[BL-672] 를 등재했고, 그중
[BL-671] — 409 dict 가 웹 UI 에 안 닿는다 — 과 [BL-672] 는 이 회차 2축이 **독립적으로 재검출**했다.
**독립 검출은 신뢰도를 올리지 등재를 늘리지 않는다.**
