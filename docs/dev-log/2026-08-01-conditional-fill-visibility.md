# conditional-fill-visibility — 그 코드가 실행되는 조건을 만들어 냈다 (2026-08-01)

**스코프.** BL-566 을 「고치기」가 아니라 **「갈래 분화」**로 재정의 + **X1 유도 시험**.
herdr 함대 3벌(`divsplit` · `ledgerhygiene` · `qa`) + CONTROL 실주행.
**마이그레이션 0 · 새 엔드포인트 0 · FE diff 0.**

---

## ★★★ 헤드라인 — 두 회차 동안 못 얻은 증거를 유도로 얻었다

직전 회차는 `_write_back_confirmed_terminal` 를 착지시키고도 **그 코드가 실주행에서 한 번도
실행되지 않은 채** 끝났다. 이번엔 기다리지 않고 **조건을 만들었다.**

```
17:40:34  793e79db  조건부 sell 등재 (state=submitted, 거래소에 살아 있음)
~17:41:20 CONTROL 이 거래소에서만 취소 (provider.cancel_order 1회 · 원장 미변경)
17:42:23  live_conditional_reconcile_divergence  order_id=793e79db   ← 리컨사일러가 최초 관측
17:42:23  원장 state=cancelled  (filled_at = updated_at, 같은 초)
          qb_live_conditional_reconcile_errors_total{stage=terminal_write_back_cancelled} = 1.0
```

- **독립 계측기 2개가 같은 초에 일치** — Prometheus counter ↔ `trading.orders` 원장 전이.
- 로그 이름이 `..._reconcile_divergence` 라 **스윕이 아니라 리컨사일러**가 최초 관측자였음이 확정된다
  (스윕이면 `live_conditional_entry_sweep_found_filled` 가 찍힌다).
- **거래소 terminal → 원장 기록 = 약 60초(리컨사일러 1틱).** BL-560 사고 때 같은 축의 값은 **818초**였다.

★**왜 기다리면 안 됐나 — 기저율을 먼저 쟀다.** `terminal_write_back_*` series 는 부재였고,
역사상 유일한 「스윕이 체결을 뒤늦게 발견」 사건은 `2026-07-31 07:44:13` **사고 그 자체**(pre-fix)였다.
즉 자연 발생 기저율 ≈ **4일에 1회**. 창을 늘리는 방법으로는 영원히 못 잰다.
경쟁자는 `trading.fetch_order_status`(15s/30s 백오프)와 `trading.scan_stuck_orders`(beat **300초**)이고,
**`scan_stuck_orders` 직후에 유도를 걸어** 리컨사일러(≈60초)에게 2~3틱의 런웨이를 줬다.

---

## ★★ G0 — 문서의 뿌리 서술이 반박됐고, 내 재해석도 절반만 맞았다

BL-566 은 「청산이 성공했는데도 엔진이 유령 포지션을 유지한다 · **41.6건/h · 평가의 69%**」였다.

**① 숫자는 재현되지만 창 하나에만 있다.** `engine_only` 29건 = 세션 `7fb8e2ed`(0.72h) **전량**.
이후 3창(0.36 + 2.56 + 0.88 = **3.8h**)에서 **0건**. 누적으로는 **314 / 평가 927 = 33.9%**.

**② 「청산 성공 후 유령」이라는 기전은 원장과 안 맞는다.**
02:30:20 의 reduce-only `0.02957327` 은 **직전 엔진 포지션과 정확히 일치**한다 = 청산은 반영됐다.
이어지는 `engine_position=-0.029573816` 은 **새 진입**이고, 다음 청산 주문 `495ac7d1` 의 수량
`0.02957382` 와 **8자리까지 같다**. 그 9분 동안 거래소엔 미트리거 sell 조건부(`ea211b72`)가 걸려 있었고
**체결되는 순간(02:40:03) divergence 가 멈춘다.**

**③ 코드가 이미 그 counter 를 무효 선언하고 있었다.**
`metrics.py:706-712` — "유실이 없어도 진입 1건당 1씩 늘 수 있다. **이 값을 진입 유실의 측정치로 쓰지 마라**",
`:524` — "`engine_only` 이 **무효가 된**". 회귀 테스트까지 있었다
(`test_live_entry_loss_instrumentation.py:302`).

**④ ★그런데 내 재해석도 절반만 맞았다.** 술어를 **과거 29건에 미리 적용**했더니(세션 스코프 포함 ·
팬아웃 0 · JOIN 없이 센 29 와 일치):

| 갈래                                                |            n | 뜻          |
| --------------------------------------------------- | -----------: | ----------- |
| `awaiting_trigger` (같은 방향 대기 조건부 **있음**) | **20** (69%) | 설계된 지연 |
| `unexplained` (같은 방향 **없음**)                  |  **9** (31%) | 결함 후보   |

⇒ **BL-566 은 실재하되 보고된 크기의 1/3.2** — 41.6/h → **12.9/h**, 평가의 69% → **21%**.
그리고 9건은 **전부 2~4봉 안에 같은 방향 조건부가 등재되며 스스로 해소**됐다
(`ea211b72` 02:33:22 · `c4ee1477` 02:45:22 · `37030ddd` 02:58:21, 연속 런 3/4/2 · **최대 4**).
즉 관측된 `unexplained` 는 「유령」이 아니라 **계획기 무장 지연**이다.

★**이 분해는 구현 착지 전에 나왔고, 그대로 Y1/Y2 의 사전 예측이 됐다** — 실주행에서
awaiting:unexplained 가 2:1 을 크게 벗어나면 **구현을 의심한다**는 외부 오라클.

---

## 한 일

- **Track A** `engine_only` → `engine_only_awaiting_trigger` / `engine_only_unexplained` / 잔여 `engine_only`.
  술어 = 세션 스코프(`parse_conditional_entry_key`) + 방향 일치. **수량은 술어에서 뺐다** —
  조건부 진입은 병합 주문이라 반전 시 `2×` 가 되어 구조적으로 안 맞는다. 조회 실패는 잔여 버킷(fail-open).
  DB 읽기 1회 · 거래소 REST 추가 0. 순수 분류기 `_classify_position_divergence` 는 **건드리지 않았다**
  (gap-resync 정합 판정이 같은 함수를 재사용한다).
- **Track C** 중복 `### BL-566` 재번호(→ **BL-567**) · `bl-audit.sh` **중복 섹션 헤더 게이트** 신설 +
  회귀 하네스(`scripts/bl-audit-test.sh`, 게이트 체인 편입) · 인덱스 표 누락 10행 복구 ·
  **BL-561 Resolved 승격**(실주행 로그 렌더 확인) · 신규 BL-568/569/570/571/572 + BL-542 흡수.
- **Track D(QA)** 코크핏 + 리포트. 발견 4건(P2 2 · P3 2 · **P1 0**) + PASS 1건.

---

## 표적 변이 — CONTROL 직접 집행 (워커 자기보고 미수용)

**Track A** — baseline **100 passed**(3 파일) 기준:

| 변이                             |  실패 | 죽은 테스트                           |
| -------------------------------- | ----: | ------------------------------------- |
| M1 술어 `True` 고정              | **4** | unexplained 3종 + 조회실패 로그 분리  |
| M2 술어 `False` 고정             | **1** | awaiting_trigger                      |
| M3 side 대조 제거                | **1** | opposite_side                         |
| M4 세션 스코프 제거              | **1** | other_session                         |
| M5 잔여 버킷 제거                | **2** | 조회실패 잔여 유지 + 로그 분리        |
| M7 `_count_safely` 제거(G6 수정) | **1** | 라벨 할당 실패가 평가를 멈추지 않는다 |

음성 대조(`direction` 2연속 차단 · `exchange_only`/`size` 분류 · gap-resync 정합 · 진입유실 가드)는
**전 변이에서 생존**. 복원 후 dirty 0.

**Track C** — `bl-audit`:

- **M6** 합성 중복 `### BL-999` 두 벌 → **exit 1**(「▶ 중복 섹션 헤더」 발화) · 제거 → **exit 0**
- ★★**역사적 불량 입력 시험** — 같은 입력(main 의 실제 중복 BL-566)에 두 탐지기를 걸었다:
  **신규 → exit 1** (`BL-566 첫:5568 중복:5610`) · **구 → exit 0, 언급 0건**.
  합성 케이스가 아니라 **초록을 받고 지나간 진짜 사고**가 이제 빨간불이 된다.
- **M8** `secdup` 기록 제거 → 회귀 하네스가 **exit 1 · 5건 중 2건 실패**.
  codex 가 지적한 「되돌려도 게이트가 못 잡는다」가 해소됐다.

---

## ★codex 적대 리뷰(G6) — MAJOR 4 · MINOR 4, **전건 코드 대조 재현 판정**(액면 수용 0)

| #   | 지적                                                                              | 판정                                                        | 처분                                                                          |
| --- | --------------------------------------------------------------------------------- | ----------------------------------------------------------- | ----------------------------------------------------------------------------- |
| 1   | `engine_only` tick 당 `list_resting_conditional_entries` **2회**(`:633` + `:940`) | ✅ 재현                                                     | **BL** — 발산 감지가 reconcile 보다 **앞서** 돌아 결과 공유가 구조적으로 불가 |
| 2   | counter `.labels()` 예외가 평가를 죽인다                                          | ✅ 재현 — `_count_safely`(`:732`)가 있는데 이 자리만 미사용 | **수정** + 회귀 테스트                                                        |
| 3   | `LIMIT 100` 이 세션 필터보다 앞                                                   | ✅ 재현                                                     | **BL** — 공유 repo 메서드라 범위 밖                                           |
| 4   | 「engine_only 외 DB read 금지」 테스트에 정상 일치(None) 케이스 없음              | ✅ 재현                                                     | **수정**                                                                      |
| 5   | SELECT 실패 시 rollback 없이 같은 세션 사용 → fail-open 계약 파손                 | ✅ — **CONTROL 이 독립적으로 먼저 세운 우려와 수렴**        | **BL** + 과장된 주석 문구 완화                                                |
| 6   | 중복헤더 게이트에 회귀 테스트 없음(되돌려도 초록)                                 | ✅ 재현                                                     | **수정** — 하네스 신설                                                        |
| 7   | `dup[]`/`sec_line[]` 여전히 id 단일 키                                            | ✅ 재현                                                     | 수정                                                                          |
| 8   | 문서의 재현 명령이 재현되지 않는다(`HEAD` 에 이미 중복 없음)                      | ✅ 재현                                                     | **수정**                                                                      |

★#5 는 **선재 패턴**이다 — 같은 파일 `:2155` 의 `list_fills_since` 도 동일하게 rollback 없이 삼킨다.
회귀가 아니므로 범위를 넓히지 않고 BL 로만 남겼다. **다만 주석이 실제보다 강한 보장을 주장하던 것은 고쳤다.**

---

## QA 발견 — 저장 버튼이 조용히 죽는다

**F2(→ BL-570) · P2.** 무편집 상태에서 `설정 저장` 버튼이 **활성인데** 누르면 PUT 0 · 토스트 0 · 에러 0.
기전 확정 방법이 좋다 — RHF 의 `shouldFocusError` 를 역이용해 클릭 후 `document.activeElement` 를 읽어
**탈락 필드를 `max_trigger_breach_pct` 로 특정**했다(2/2 재현). `disabled` 8/8 샘플 전부 false 로 (a) 갈래를 배제.

★**출처 사슬 — 세 회차 동안 안 보였다.** 그 필드는 **PR #493**(2026-07-28, 커밋 `274dc645`)에서 들어온
nullable BE 설정 필드다. 원장에 `"max_trigger_breach_pct": null` 로 저장되고 → 폼 초기 DOM 값이 `""` →
`setValueAs` 는 change 에서만 적용 → zod 가 `""` 를 거부 → `handleSubmit` 차단 → **errors 미렌더**.
**GET 도 멀쩡하고 「편집 후 저장」도 멀쩡하다. 무편집 저장을 눌러본 적이 없었을 뿐이다.**
직전 회차가 FE `.strict()` 파손을 잡았을 때도 이 경로는 지나가지 않았다.
⇒ **nullable BE 설정 필드를 추가하면 FE 폼의 초기값 정규화도 같은 PR 에서 해야 한다.**

**F3(→ BL-571) · P3 — 내 이전 회차들이 만든 오염이다.** `soak_closed_by_operator` ·
`interim_window_stop` · `prefix_w1_window_done` 은 `SessionDeactivationReason` enum 에 없는 값인데
**운영자가 원장에 직접 기입**했다. 화면에 원문이 노출되고 40초에 콘솔 경고 67건이 난다.
AST 가드(`test_deactivation_reason_wiring.py`)는 `tasks/live_signal.py` **호출부만** 훑으므로 원장 직접 기입을 못 막는다.
**이번 창은 enum 값 `user_stopped` 로 닫았다.**

**PASS 1건** — PR #511 계약 성립: 주문 원장 50행 실측에서 거절 = amber warn, 체결 = green, 취소 = 회색.
**거절이 초록으로 보이지 않는다.**

---

## 사전등록 판정

**창 = 2026-07-31 17:40:00 → 18:46 UTC (66분) · 세션 `dc1e08f1` · 평가 tick 66.**

| ID     | 결과                                                                                                                                                       |
| ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **X1** | ✅ **PASS (유도)** — `terminal_write_back_cancelled=1.0` + 원장 전이, **같은 초**. 소요 **60초**(사고 때 818초). 리컨사일러 경로 확정                      |
| **X2** | ✅ **PASS** — write-back 갈래에서 같은 `order_id` 반복 **0회**(before: 같은 주문 **7회**)                                                                  |
| **X3** | ❌ **미충족** — 조건부 진입 체결 **3 < 5**. ⇒ **체결 기반 비율은 인용하지 않는다.** (Y1 분모를 평가 tick 으로 잡아 둔 사전등록이 이 때문에 옳았다)         |
| **X4** | ✅ **유효** — 세션 **66분 생존**, `gap_resync_position_mismatch` **0**. 창 폐기 없음                                                                       |
| **Y1** | ⚠️ **부분** — `engine_only_unexplained` **프로덕션 발화 ✅**(리로드 센티넬 통과). `engine_only_awaiting_trigger` **0건 → 문턱(≥10) 미달**. 사유는 아래 §Y1 |
| **Y2** | ✅ **판정** — 관측된 `unexplained` 연속 런: 과거 자연 9건 = **3 / 4 / 2**(최대 4), 이번 유도 1건 = **1**. **전부 ≤5** → BL-566 재정의 후 종결              |
| **Y3** | ✅ **PASS** — `direction_transient` **14 → 15**(+1), 승격 0, 오탐 사망 0. 17:47:19 1회가 다음 봉에 스스로 풀렸다                                           |

### ★Y1 이 절반만 난 이유 — 두 서브라벨은 계획기의 다른 국면에서 나온다

두 번째 유도(엔진이 숏인 상태에서 **거래소만** reduce-only 로 비웠다, 18:16:32)가 만든 것:

```
18:17:19  category=engine_only_unexplained  engine_position=-0.03015634  exchange_position=0
          resting_lookup=ok  resting_sides=['buy']  resting_qty=['0.06000000']
18:17:40  live_conditional_reconcile_divergence  reason=market_converted  breach_pct=0.0116
18:17:41  aab5a04e  sell 0.03  filled       ← 등재 1초 만에 체결
18:18:19  발산 없음 (정렬 복구)
```

⇒ 엔진이 이미 포지션을 든 채 거래소가 flat 이면 **트리거가 이미 돌파돼 있어 계획기가 시장가로 전환**한다
(`market_converted` **8 → 9**, 로그가 `reason=market_converted breach_pct=0.0116` 으로 명시).
같은 방향 진입이 **대기한 적이 없으므로** `awaiting_trigger` 는 구조적으로 생길 수 없다.
`awaiting_trigger`(과거 창의 20건)는 **트리거 미돌파** 국면에서만 나온다.

★**즉 두 라벨의 비율은 결함이 아니라 「결정 시점에 트리거가 돌파돼 있었는가」의 함수다.**
내 사전 예측 **2:1** 은 틀린 것이 아니라 **이 창에서 분모가 0** 이라 검정 불가다. 그렇게 쓴다.

★**세 줄이 한 줄에서 동시에 증명됐다** — ① 신규 서브라벨이 실주행에서 실행됐다(리로드 센티넬)
② `resting_lookup=ok` = 「확인했더니 없었다」와 「확인 자체를 못 했다」의 구분이 살아 있다
③ 대기 주문이 `buy`(엔진과 반대)라 **방향 술어가 프로덕션에서 실제로 갈랐고**, 그 수량이 `0.06` = **2× 기본**이라
**G1 에서 「수량을 술어에 넣지 마라」고 동결한 판단이 프로덕션에서 확인됐다**(넣었으면 오분류했다).

### 이 창이 새로 기여한 사실

| 지표                           | 이번 창(정렬 시작) |      pre-fix 창 |
| ------------------------------ | -----------------: | --------------: |
| 평가 tick                      |             **66** |              42 |
| `engine_only` **자연 발생**    |              **0** |  **29**(41.6/h) |
| 발산 총계 / 평가               |    **2 / 66 = 3%** | **29/42 = 69%** |
| `reduce_only_same_side` 증분   |              **0** |               — |
| 조건부 진입 체결 / 취소 / 거절 |      **3 / 7 / 0** |               — |

★**이건 깨끗한 before/after 가 아니다** — 이 창은 **내가 정렬시켜 시작했고** 시장 조건도 다르다.
정직한 진술은 이것뿐이다: **「정렬 상태로 시작한 66틱 동안, 실거래가 있었음에도 `engine_only` 자연 발생은 0건」.**
BL-566 의 「청산이 성공한 뒤에도 남고 **다음 진입이 올 때까지 지워지지 않는다**」와는 어긋난다 —
실제로는 계획기가 같은 방향을 무장하거나 시장가로 전환하는 즉시 해소된다(유도한 1건도 **1틱**만에).

---

## 계측기가 또 틀렸다 (이 레포 통산 12·13번째)

- ★**창 경계를 시계로 잡았다** — 감시기가 `created_at > now() - interval '6 hours'` 로 세어
  **직전 소크의 주문 22건을 이번 창으로 집계**했다(첫 이벤트 `0/9/12/1`). 세션 `created_at` 기준으로 바꿨다.
  ⇒ **창은 시계가 아니라 세션으로 잡아라.**
- ★**`PositionSnapshot.quantity` 는 없다** — 필드명은 `size`. 정렬 스크립트가 첫 실행에서 죽었다.
  결국 `_net_position_size`(`live_signal.py:293`)를 그대로 재사용했다 — 그 함수 주석이 이미
  **"발산 감지와 같은 산술을 써야 한다"** 고 못 박고 있었다.
- ★`BybitFuturesProvider.create_order` 는 `leverage`·`margin_mode` **둘 다 필수**다(`providers.py:828`).
  원장 실측(최근 26건 전부 `2`·`isolated`)으로 맞췄다.
- ★★★**기록돼 있던 교훈이 절반만 맞았다 — 변수는 도구가 아니라 캐스트다.**
  직전 회차 기록은 "`psql -At` 는 boolean 을 `false` 로 찍는다" 였는데, 같은 세션 안에서 둘 다 봤다:

  | 쿼리 형태                         | 출력                 |
  | --------------------------------- | -------------------- |
  | `SELECT is_active, ...`(맨 컬럼)  | **`t` / `f`**        |
  | `SELECT is_active::text \|\| ...` | **`true` / `false`** |

  그래서 이번에도 내가 급히 쓴 사망 감지 `case "$A" in f\|*)` 는 **구조적으로 절대 매치되지 않았다**
  (세션이 안 죽어서 드러나지 않았을 뿐이다). ⇒ 교훈은 「표기를 확인하라」가 아니라
  **「boolean 을 판정에 쓰지 마라」**다. `watch_soak.sh` 는 nullable 텍스트 `deactivated_reason` 의
  존재 여부로 판정하게 만들어 뒀고 그쪽은 멀쩡했다.

- ★**정교한 계측기가 단순한 것에 졌다** — `engine_only` 발화를 잡으려고 쓴 `sed` 가 BSD 미호환
  (`t` 레이블)으로 죽는 동안, 감시기의 단순한 `grep -oE 'category=[a-z_]+'` 가 그것을 잡았다.

- ★**`grep "order_id="` 는 `exchange_order_id=` 도 잡는다** — X2 를 처음 쟀을 때 한 줄이 두 건으로
  세어졌다. 줄 수(`grep -c`)를 먼저 세고 나서 필드를 뽑았어야 했다.

- ★**한 이벤트 이름이 구조가 다른 두 사건을 덮고 있다** — `live_conditional_reconcile_divergence` 가
  ① write-back 누락(`order_id` 있음)과 ② 시장가 전환(`trade_id`·`reason=market_converted`, `order_id` **없음**)
  양쪽에 쓰인다. **이 스프린트 계열이 계속 싸워 온 `110017` 라벨 충돌과 같은 형태**다 → BL 후보.

---

## 정직하게 남긴 것

- **BL-565 는 손대지 않았다** — `bracket_attached` series 부재 · `bracket_unavailable` **40**
  = 부착률 **0%**. `strategy.exit` 을 쓰는 전략도 없다. **이 창에서 구조적으로 측정 불가**라
  고쳐도 검증할 수 없다.
- **창을 내가 정렬한 뒤 시작했다** — 직전 소크가 남긴 거래소 숏 `0.03 @ 62658` 을 reduce-only 로 닫았다.
  그대로 뒀으면 엔진이 롱으로 진입하는 순간 `direction` 2연속으로 세션이 죽어 창이 폐기됐을 것이다.
  정렬 이전의 `exchange_only` 4건은 **결함이 아니라 잔여 노출을 계측기가 정확히 잡은 것**이라
  창 계산에서 분리했다.
- **X1 과 `engine_only` 둘 다 유도된 것이다.** 자연 발생을 관측한 것이 아니다.
  유도가 없었다면 이번 창에서도 **둘 다 0회**였다. 유도 = 거래소에만 작용하고 원장은 그대로 두는 조작
  (조건부 취소 1회 · reduce-only 청산 1회)이며, 프로덕션 코드 경로는 손대지 않았다.
- **`engine_only_awaiting_trigger` 는 프로덕션에서 한 번도 실행되지 않았다.** 단위테스트와 과거 원장
  분해(20건)로만 뒷받침된다. `unexplained` 쪽만 실주행 검증됐다.
- **창을 종료한 사유를 `user_stopped`(enum 안)로 썼다** — 이전 회차들이 enum 밖 값을 원장에 직접
  기입해 화면을 오염시킨 것(BL-571)을 반복하지 않기 위해서다. 소크 후 거래소도 flat 으로 정리했다.
- **워커 컨테이너의 `/app/src` 밖은 stale 이다** — 마운트가 `backend/src` 와 `.metrics` 둘뿐이라
  `docker exec … alembic heads` 가 `20260725_0002` 를 보고한다(실제 head 는 `20260730_0001`).
- codex #1/#3/#5 는 **고치지 않고 BL 로 남겼다**(구조적 · 공유 메서드 · 선재 패턴).

---

## 게이트

`scripts/final-gates.sh --run conditional-fill-visibility` — **파이프 없이** 돌렸다.

| 게이트                         | 결과                                  | baseline 대비                                 |
| ------------------------------ | ------------------------------------- | --------------------------------------------- |
| BE ruff                        | ✅ All checks passed                  | —                                             |
| BE mypy                        | ✅ **214 files** clean                | 214 → 214                                     |
| BL 감사                        | ✅ exit 0 · **active 153 / 전체 233** | active 146 → 153 · 전체 224 → 233             |
| **BL 감사 하네스**             | ✅ **5 / 5** (이번 회차 신설)         | 신규                                          |
| BE pytest                      | ✅ **3732 passed / 46 skipped**       | 3721 → **+11**                                |
| e2e design-canon               | ✅ **32 passed**                      | —                                             |
| e2e authed                     | ✅ **66 passed**                      | —                                             |
| CI 커버리지 잡                 | ✅ **93.15%** (문턱 90)               | —                                             |
| CI fresh DB alembic            | ✅ exit 0 (head `20260730_0001`)      | 마이그레이션 **0**                            |
| CI frozen-lockfile             | ✅ exit 0                             | —                                             |
| CI hooks grep                  | ✅ exit 0                             | —                                             |
| FE typecheck/lint/vitest/build | **건너뜀 (frontend diff 0)**          | baseline FE **1232**(205 파일)은 첫 step 실측 |

★**FE 게이트는 「통과」가 아니라 「건너뜀」이다** — 영역 판정이 FE diff 0 을 읽고 건너뛴다.
그렇게 쓴다. 체인은 e2e 전에 **자체 정체성 프로브**를 돌았다(`:3100 <title>QuantBridge</title>`).

★**baseline 정정** — 이 회차 중간에 잰 **3730** 은 G6 수정 커밋(테스트 2건 추가) **이전** 값이었다.
게이트를 다시 돌려서 잡았다. 요약 층(`status.md`)에도 반영했다.
