# close-ownership-axis (2026-08-10)

> `docs/status.md` 에서 강등된 회차 기록이다(2026-08-10 docs-context-diet).
> 강등 이유는 크기가 아니라 **역할**이다 — 끝난 회차의 회고는 진입점이 아니다(ADR-026 §3).
> 원문 위치 = `git show 762e1297:docs/status.md` 의 345-372행.

---

### ★close-ownership-axis (2026-08-10) — 반박 7건 · 그중 4건이 내 것이다

**종결 3 · 부분 2 · 신규 3.** [BL-684] P1 ✅ · [BL-517] P2 ✅ · [BL-671] 🟡 · [BL-672] 🟡 ·
신규 [BL-687]~[BL-689]. 커밋 3 · `frontend/` **0줄** · pytest **4529 passed / 45 skipped** ·
표적 변이 **11/11 red(전부 도달 확인)** · `bl-audit` rc=0(active 158→**157** / 240).
**소크는 게이트 읽기 2회 외에 접촉 0** — `pin` 안 했다.

| 무엇이 반박됐나                      | 누가          | 어떻게                                                                                                                                                                     |
| ------------------------------------ | ------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **변이 6/6 red 를 통과한 구현**      | 나(G3 대조)   | `raise SystemExit(4)` 가 「★원장에 남았다」 검증 안내를 **삼켰다** — 잔량이 남은 회차가 그 안내를 가장 필요로 하는데 **정확히 그때만** 사라진다. 변이 M7 을 신설해 고정    |
| 「`message` 의 유일한 소비자는 FE」  | 나(사전 대조) | CLI(`live_session_admin.py:389`)가 **두 번째 소비자**다. 키만 바꿨으면 CLI 가 조용히 폴백 문구로 떨어졌다                                                                  |
| **내 등재 후보 1건**                 | 나(코드 대조) | 「`position_service` 가 조건부 진입을 못 본다」 → `:210-215` 가 `order.reduce_only` 인 것만 쓴다 ⇒ **의도적**이다. 결함이 아니라 설계 ⇒ **등재 취소**                      |
| **내 docstring 수선이 만든 중복**    | 나            | 새 문단이 기존 문단과 같은 말을 했다. 내가 만든 orphan 이므로 제거                                                                                                         |
| 테스트 페이크의 제약 축              | Standards 축  | 실제 `list_by_exchange_uid` 는 `WHERE exchange_uid == uid` 라 **자기 행을 포함**하는데 페이크가 뺐다 ⇒ `if account.id not in ids` 폴백이 **테스트에서만** 발화(§10 규약 3) |
| `RestingEntryOrder` 주석의 사실 주장 | Standards 축  | 「JSONResponse 가 Decimal 을 직렬화 못 한다」는 **`response_model` 경로엔 거짓**이다(같은 파일 `ExchangePositionSchema` 가 `Decimal` 을 그대로 낸다) ⇒ `Decimal` 로 되돌림 |
| runbook §7 이 **두 경로 모두** 거짓  | Spec 축       | 「`close_service.py:102-104` 는 포지션만 본다」가 [BL-661]+[BL-684] 로 완전히 낡았다. **내 변경이 문서를 더 거짓으로 만들었다** ⇒ 같은 세션에 rc 표까지 갱신               |

★★★**「변이 전건 red」는 「이 변경이 옳다」가 아니다** — 2026-08-10 이 두 번째로 실증했다.
변이는 **내가 상상한 실패**만 잰다. G3 에서 구현을 한 줄씩 읽지 않았으면 rc 4 결함을 놓쳤다.
★★**리뷰 2축이 서로 다른 것을 봤다** — Standards 는 페이크의 제약 축(테스트가 **무엇을 재는지**),
Spec 은 화면 축([BL-688])과 문서 축(runbook). **한 축만 돌렸으면 둘 중 하나를 통째로 놓쳤다.**
★★**기각도 근거를 남겼다** — 「`message` 가 더 정직한 이름」(레포 계약이 `detail` 이고
`describeApiError` 가 **바로 그 소비자**다) · 「`ownership_scope_ids` 에 user 스코프가 없다」
(같은 `exchange_uid` 면 **포지션이 실제로 공유**되므로 user 를 넘어 서는 것이 옳다) ·
「사설 헬퍼 크로스 import」(레포 선례 **5건**).
★**`lint-staged` 가 py 파일 중 첫 하나만 검사한다**([BL-687]) — `bash -c 'cmd ${0#backend/}' f1 f2 f3`
에서 `$0` 는 `f1` 뿐이다. **커밋 도중 관측**했고 `bash -c` 시맨틱으로 재현했다.
