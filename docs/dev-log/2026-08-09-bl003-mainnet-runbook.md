# bl003-mainnet-runbook baseline (2026-08-09)

> `docs/status.md` 에서 강등된 회차 기록이다(2026-08-10 docs-context-diet).
> 강등 이유는 크기가 아니라 **역할**이다 — 끝난 회차의 회고는 진입점이 아니다(ADR-026 §3).
> 원문 위치 = `git show 762e1297:docs/status.md` 의 279-344행.

---

### ⓺ baseline (2026-08-09 bl003-mainnet-runbook) → **집행 완료**

| 축            | 착수 시                                                          | 종료 시                                                                   |
| ------------- | ---------------------------------------------------------------- | ------------------------------------------------------------------------- |
| `bl-audit`    | ACTIVE **161** · PARTIAL **12** · RESOLVED **45** · 전체 **218** | ACTIVE **160** · PARTIAL **13** · RESOLVED **45** · 전체 **218**          |
| P0            | ACTIVE **1** ([BL-003])                                          | **PARTIAL 1** — 산출물 축만 닫혔다. ACTIVE 0 을 「P0 없음」으로 읽지 마라 |
| 소크          | C1 **26.54h** · C2 15.30h · 실격 **0** · 세션 `de3db35a`         | **무접촉** — 서버에 읽기(게이트·DB 조회) 외에 아무것도 하지 않았다        |
| `backend/src` | —                                                                | **0줄**                                                                   |

**산출물 4종** — `bybit-mainnet-runbook.md`(신규) · `scripts/bybit-smoke.sh`(신규) ·
`backend/scripts/bybit_smoke.py`(rename+live) · `.env.prod.example`·`.env.example`(개정).

★★★**이 회차의 최대 산출은 산출물이 아니라 반증 4건이다 — 전부 [BL-003] 본문이 틀렸다.**

| [BL-003] 본문이 말한 것                   | 실측                                                                                                                                                                                                            |
| ----------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 「base URL mainnet 매핑」이 할 일         | **이미 있다** — `_apply_bybit_env`(`providers.py:2202-2210`)가 demo→`enable_demo_trading(True)` · **live→no-op** 로 이미 가른다 ⇒ **cutover 는 2줄**이고 provider 본문은 안 건드린다                            |
| 소액 **$10~50** 시작                      | **그 자본으로는 주문이 0건 나간다.** 서버 실측(자본 190,034 → 주문 0.058 BTC) + 자본 비례 사이징(`strategy_state.py:503-506`) ⇒ `X_min = 190,034 × (0.001/0.058) ≈ **$3,276**`. ★BTC 가격도 pct 도 **소거된다** |
| `scripts/bybit-smoke.sh` **신규**         | 뼈대가 **이미 있었다**(`bybit_demo_smoke.py` 221줄·6단계). 신규는 셸 래퍼뿐                                                                                                                                     |
| `.env.production` **별도 secret manager** | `.env.prod.example` 이 **이미 존재**했고 GCP Secret Manager 를 전제했는데 **실제 배포는 오라클 docker compose** 다 — 신규 작성이 아니라 **현실과 맞추는 개정**이 답이었다                                       |

★★**내 산출물 자신에서 결함 3건을 잡았다** — 셋 다 남이 아니라 내가 만든 것이다:

1. **rollback 명령이 안 먹는 형태였다** — `stop`/`flatten` 은 `session_id` 가 **positional** 이고
   `--confirm` 이 **required** 다(`live_session_admin.py:409-415`). 인용 **17건 전수 대조**로 잡았다.
   ★**실자금 rollback 이 그대로 실패했을 것이다.**
2. **내 셸 검사 하나가 죽은 코드였다** — `case *[![:print:]]*` 는 이 맥의 UTF-8 로케일에서
   한글을 **print 로 보고 통과**시켰다. 실제로 잡은 것은 `LC_ALL=C grep` 쪽(음성 대조 6/6).
3. **내 테스트 도구가 거짓말했다** — zsh `MULTIOS` 가 `2>&1 >/dev/null` 를 **양쪽으로** 보내
   stdout 을 stderr 로 오독했다. stderr 를 파일로 받아 재측정.

★**부수 수리 2건**(rename 김에) — `bybit_demo_smoke.py` 가 `mode` 를 **받고도 안 쓰고**
`enable_demo_trading(True)` 를 하드코딩했다(live 를 여는 순간 「live 인데 demo 로 간다」) ·
credentials 를 **argv 로** 넘겨 `ps` 에 노출됐다(demo 키엔 무해, 실키엔 아니다) → env 우선으로 교체.

★**dry-run 네트워크 호출 0건은 양성/음성 대조로 확정했다** — dry-run 은 `_execute` 에 **도달하지 않고**
(`uv run` 매치 2건은 문자열 할당·출력뿐), `--confirm` 은 도달한다(trace `:81`,`:85`).

★★★**codex 가 내 runbook 의 핵심 주장을 반박했다 — 7건 제기, 7건 전부 코드로 재현.**
지난 2회차 **산출물 0**(파일만 읽고 exit 0)에서 4,518B 로 바뀐 이유는 **계약**이다 —
스펙에 **출력 형식을 강제**하고 「주장 6개를 하나씩 반증해라」로 표적을 좁혔다.

- ★★★**「cutover 는 2곳」이 거짓이었다 — 6곳이다.** 빠진 넷 중 `close_service.py:92` 는
  **live 청산을 `422 live_mode_stub` 으로 거부**한다 ⇒ **나갈 문이 없는 채로 들어갈 뻔했다.**
  **진입 자물쇠(⑵⑶)와 출구 자물쇠(⑷)가 다르다** ⇒ 「⑷ 를 먼저 풀어라」 규약을 넣었다.
- ★★**「registry stub 이 안전장치」도 거짓** — `BybitFuturesProvider()` 직접 생성이 **13곳**이고
  호스트는 클래스가 아니라 `account.mode`(`account_service.py:92`)가 정한다.
- ★셸 결함 3건 실측 확정 — 값 없는 `--mode` **무한 루프**(rc 124) · `REPLACE_ME` **통과**(rc 0) ·
  「외부 호출 0건」이라는 **부정확한 술어**(`stat`·`sed`·`grep` 은 돈다). 셋 다 수리 + 회귀 11/11.
- 신규 **[BL-661]** — `flatten` 이 「이미 flat」으로 **exit 0** 하는데 조건부 주문은 남는다(P1).

★★**후속(같은 날) — `min_qty` 실측이 §8 5건 중 2건을 닫았다.** 공개 `load_markets` · 인증 없음 ·
주문 없음. perp `BTC/USDT:USDT` **min_amount 0.001 BTC**(가정 일치 · min_cost 없음) ·
spot `BTC/USDT` **min_cost $5.0**(min_amount 1e-06 은 무의미).
★★★**가정을 확인하러 간 조회가 확인 밖의 것을 고쳤다** — ⑴ 내가 쓴 「perp 명목 ~$100」이 **$65**
(BTC 를 $100k 로 어림했는데 실측 **$64,957**) ⑵ [BL-003] 본문의 **「1 USDT limit-order」가 불가능**
(spot 하한 $5) ⑶ 셸 spot 기본 수량 0.0001(=$6.5, 하한에 너무 가깝다) → **0.0002**(=$13).
★**$3,276 은 오히려 독립 검증됐다** — 원 식은 가격을 안 쓰는데, 가격을 넣어 명목 비율로 다시
세면 데모 **1.98%** · mainnet **2.0%** 로 일치한다. **맞은 가정을 확인하는 것도 값을 낸다.**

★★★**통합 리뷰가 이 회차 자신의 수리에서 P1 fail-open 을 잡았다** ([BL-656] 섹션 전문).
`docker inspect` 가 「데몬 도달 불가」와 「컨테이너 없음」을 **둘 다 exit 1** 로 내는데
`soak-restart.sh` 가 그것을 「완전 down」으로 읽어 `down` 을 건너뛰고 `pin` 을 부르는 경로가 있었다.
**탐지기와 가드가 같은 docker 를 쓰므로 함께 눈이 먼다** ⇒ 살아 있는 컨테이너의 mount 원본을
덮어쓸 수 있었다. 판별자 = `docker version`(서버 필드라 도달성만 잰다), rc **2** 신설, 변이 3/3.
