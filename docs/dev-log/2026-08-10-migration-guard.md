# 2026-08-10 migration-guard — [BL-451] 종결 · 개발 DB 전소 경로 차단 · 신규 [BL-697]

> ★**강등 출처** — 2026-08-11 ledger-truth 가 `docs/status.md` 의 이 섹션을 [ADR-026] §3 에 따라
> 여기로 내렸다(status.md 700줄 상한). 원문 = `47748e77:docs/status.md` §★migration-guard.
> 본문은 **손대지 않고 그대로** 옮겼다.

| 축            | 착수 시                              | 종료 시                                               |
| ------------- | ------------------------------------ | ----------------------------------------------------- |
| main          | `a5f3f600`                           | `stage/migration-guard`                               |
| `bl-audit`    | ACTIVE **7** / 전체 247 · RC=0       | ACTIVE **6** / DEFERRED 154 / 전체 248 · RC=0         |
| `backend/src` | —                                    | **0줄** · `frontend/` **0줄**                         |
| 소크          | rc=2 UNKNOWN · C1 47.22h · C2 31.91h | **무접촉**(게이트 읽기 1회)                           |
| 배선 테스트   | —                                    | 9건 (서브프로세스 · 붙지 않는 DSN · `--collect-only`) |
| 변이          | —                                    | **5/5 red · 도달 5/5 · diff 5/5**                     |

★★★**최대 산출 — ①의 실체가 원장 기술과 달랐다.** 원장은 잔여 ①을 「같은 폴백 구조가
`tests/conftest.py` 에도 있다 — 파괴성은 낮지만 동일 가드가 필요한지 **검토**」로 적었다.
실측은 정반대였다: 문제는 폴백의 존재가 아니라 **가드가 배선돼 있지 않다**는 것이었고
파괴성은 낮지 않았다. `DATABASE_URL`(개발 DB) 하나만 있는 셸에서

```
pytest tests/real_broker/       → rc=3   가드 발동
pytest tests/trading/           → rc=0   ★1088건 수집 — 그 경로의 세션 픽스처가 drop_all 을 돈다
pytest tests/test_migrations.py → rc=0   수집 통과
```

`tests/real_broker/conftest.py` 의 가드는 **그 디렉터리를 수집할 때만** 로드된다. 즉 실사고
이후 붙인 가드는 사고를 낸 파일 하나와 우연히 같이 열리는 디렉터리만 지키고 있었다.
⇒ 판정을 `tests/_db_guard.py` 로 모으고 **루트** `tests/conftest.py::pytest_configure` 로 올렸다.

| 무엇이 반박됐나                                 | 누가          | 어떻게                                                                                                                                                                            |
| ----------------------------------------------- | ------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ★★★**프롬프트의 「`DATABASE_URL` 만 있는 셸」** | 워크플로 대조 | 그건 사고 상황이 아니라 **CI 의 정상 배치**였다. `ci.yml`·`trust-layer-nightly.yml` 이 `TEST_DATABASE_URL` 없이 돈다 ⇒ 폴백 금지와 CI 수정이 **같은 커밋**이어야 했다             |
| ★★**원장의 ① 기술**                             | 실측 rc 3종   | 위 문단. 「검토」가 아니라 **배선 부재**였고 파괴성도 낮지 않았다                                                                                                                 |
| ★★**내 계획의 「CI 4곳」**                      | 전수 grep     | pytest 호출은 CI 전체에 **3곳**뿐이고 `nightly-real-broker.yml` 은 이미 둘 다 준다 ⇒ 실제 수정은 **2곳**. 나머지 2곳은 `alembic upgrade` 스텝이라 방향 가드와 무관                |
| ★**원장의 ④ 「alembic CLI 무가드」**            | 전수 grep     | 무가드인 것은 `downgrade` **하나**다. entrypoint·`make migrate`·`migrate-isolated`·`final-gates.sh`·`worktree-bootstrap.sh` 는 전부 `upgrade` 라 무해했다 ⇒ 가드를 방향 조건부로  |
| ★**⓪ 표의 「난이도 하 / S(2h)」**               | 실소요        | 배선 테스트 + CI 계약 + 변이 5종 + 백업 왕복 실증까지 **4h 남짓**. 표의 값은 `[가정]` 이 맞았다                                                                                   |
| ★**내 변이 하네스의 diff 축**                   | 자기 대조     | 1차 실행에서 M1·M2 의 `git diff` 가 **비었다** — `_db_guard.py` 가 untracked 라서였다. 도달 마커로는 참이었지만 **diff 축은 무증거**였다 ⇒ `git add -N` 후 재실행해 5/5 로 채웠다 |

★**M4(역방향 변이)가 음성 대조의 의미를 바꿨다.** `test_alembic_upgrade_..._is_not_refused` 는
착수 시점에도 green 이었다 — 하지만 그건 **가드가 없어서**였다. 가드를 방향맹으로 만드는 변이가
그 테스트를 red 로 만들자 비로소 「지금의 green 은 가드가 정확해서」가 증명됐다.
**초록의 이유가 바뀌었는데 초록은 그대로인 구간**이 있고, 변이 없이는 그것을 못 가른다.

★★**`/code-review` 가 내 구현에서 결함 4건을 잡았다 (변이 5/5 를 통과한 뒤에).** 이번에도
「변이 전건 red 는 설명이 옳다는 뜻이 아니다」가 재현됐다 — 변이는 **내가 심은 가드**만 재고,
내가 안 심은 축은 못 잰다.

| 채택 | 무엇                                                                 | 왜 변이가 못 잡았나                                                                                                                                                              |
| ---- | -------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ✅   | **`-x allow_destructive=0` 이 파괴를 허용**했다                      | `bool("0")` 은 참이다. 값 없는 `-x allow_destructive` 는 `""` 라 거꾸로 막혔다 — **두 표기의 결과가 정반대**. 내 M5 는 「탈출구를 무시」만 쟀고 「탈출구가 값을 오독」은 안 쟀다 |
| ✅   | **`TEST_DATABASE_URL` 이 `.env.example` 에 없다** (Golden Rule 위반) | 내가 그 변수를 **선택 → 의무**로 승격시켰다. `.env.example` 을 복사한 신규 체크아웃은 pytest 가 전부 rc=3 으로 죽는다. 코드가 아니라 **계약**이라 어떤 변이도 못 잡는다          |
| ✅   | **rc=3 은 가드 고유 신호가 아니다**                                  | `pytest_configure` 에서 **아무 예외나** 나도 pytest 는 INTERNALERROR 로 rc=3 을 낸다. ★내 파일이 스스로 「rc≠0 은 무증거」라 적어 놓고 **pytest 축만 그 규율에서 빠져 있었다**   |
| ✅   | **`effective_dsn()` 2층 방어에 도달한 테스트가 0**                   | 리뷰어가 폴백을 되살리는 변이를 심었더니 **9건 전부 green**. `refusal_reason()` 이 env 를 먼저 독립적으로 보므로 어떤 배선 테스트도 그 층을 지나지 않는다 — §10-2 그 자체다      |
| ❌   | Middle Man — `_assert_disposable_database` 가 위임 한 줄             | 그 이름은 실사고 회귀 테스트가 부르는 이름이다. 없애면 그 테스트가 **무엇을 지키는지의 연결**이 끊긴다                                                                           |
| ❌   | ② 「주기」 백업 미구현                                               | 사용자가 「덤프+복원, 수동, 자동 실행 없음」을 명시적으로 골랐다. 스펙의 「또는」 분기를 택한 것                                                                                 |
| ❌   | `--sql` 오프라인 downgrade 도 막힌다                                 | 피해가 SQL 출력 한 번을 못 받는 것뿐이고 탈출구가 있다. 조건을 하나 더 쪼개는 값보다 크다 ⇒ **주석으로 인정**하고 남겼다                                                         |

⇒ 넷을 고치고 **회귀 변이 3종을 새로 심었다** — M6(탈출구가 값을 오독) · M7(2층 방어 폴백 부활) ·
M8(가드 마커 제거). 최종 **변이 8/8 red · 도달 8/8 · diff 8/8**, 배선 테스트 9 → **14건**.

★★**`final-gates.sh` 가 rc=1 인데 그 1건은 이 회차 것이 아니다 — [BL-698] 로 갈랐다.**
`e2e authed` 의 백테스트 폼 422 케이스 2건이 FAIL 했다. 이 브랜치는 `frontend/` **0줄**이라
의심스러워 `git checkout main` 후 같은 두 케이스만 `--grep` 으로 돌렸더니 **main 에서도 정확히
그 2건이 red**(1 passed)였다 ⇒ **선재**. 나머지 20 게이트는 PASS/skip 이다.
★그리고 이건 **구조 문제**이기도 하다 — `final-gates.sh:220` 은 `e2e authed` 를 `has_fe` 와
무관하게 항상 돌린다. FE 를 한 줄도 안 건드린 회차가 전부 「PR 을 만들지 마라」를 받는다.
**상시 붉은 게이트는 게이트가 아니다** — 그 상태가 이어지면 진짜 red 가 섞여도 안 보인다.

★**백업은 「타깃이 생겼다」로 끝내지 않았다** — 덤프 2.15MB 를 임시 DB 로 복원해 orders **823** ·
strategies 3 · **암호화 API 키 2/2** 왕복을 실증했다. 실사고에서 **복구 불가**였던 것이 정확히
그 API 키다. 개발 DB 에는 `--clean` 을 돌리지 않았다 — 그 편의가 곧 이 항목이 막으려는 사고다.
