# 2026-08-10 — bl-trigger-triage + precommit-scope (같은 세션)

> [ADR-026] §3 강등. 원문 = `docs/status.md` 의 이 두 블록이고 2026-08-11
> bl-701-c1-window-count 회차가 700줄 상한에 걸려 내렸다. 색인 = `INDEX.md`.

### ★bl-trigger-triage (2026-08-10) — 어휘 `DEFERRED` 신설 · ACTIVE **159 → 7** · [BL-687]·[BL-695] 종결 · 신규 [BL-694]~[BL-696]

| 축            | 착수 시                          | 종료 시                                                       |
| ------------- | -------------------------------- | ------------------------------------------------------------- |
| main          | `7db0d426`                       | `stage/bl-trigger-triage`                                     |
| `bl-audit`    | ACTIVE **159** / 전체 244 · RC=0 | ACTIVE **7** / DEFERRED **153** / 전체 247 · RC=0 · UNKNOWN 0 |
| `backend/src` | —                                | **0줄** · `frontend/` **0줄**                                 |
| 소크          | rc=2 UNKNOWN · C1 46.24h/168h    | **무접촉**(게이트 읽기 1회)                                   |
| 하네스        | `bl-audit-test.sh` 5/5           | **8/8** (⑥ 분리 · ⑦ 음성 대조 · ⑧ 3면)                        |
| 판정 커버리지 | —                                | **159/159** (표본 아님)                                       |

★★★**최대 산출 — 판정기의 초판이 5건을 잘못 「도래」로 올렸다.** `BL의존` 축을 「참조된 BL 이
전부 RESOLVED 면 도래」로 짰는데, **트리거는 대개 절의 접속**이고 BL 참조는 그중 하나일 뿐이다.
[BL-669] 가 표본이다 — `[BL-517] 종결 + 거래소 접촉 검증이 가능한 회차` 에서 앞절은 실제로
닫혔지만 뒷절은 **사용자 승인**이다. 기계가 **반쪽만 읽고 전체를 단정**했다.
같은 뿌리로 [BL-409]·[BL-654]·[BL-681]·[BL-685] 도 올라갔다.
⇒ 남은 절에 외생·동승 어휘가 있으면 **「판단 필요」로 떨어뜨린다**로 고치고, 그 5건 중
[BL-669] 를 `--selftest` 음성 대조로 **박아** 회귀를 막았다.
**전량 스윕이 이걸 잡았을 리 없다** — 159건 안에서 5건은 눈에 안 띈다. 잡은 것은 스윕 **앞**에
세운 판별력 검사다.

| 무엇이 반박됐나                                 | 누가         | 어떻게                                                                                                                                                                                            |
| ----------------------------------------------- | ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ★★★**내 판정기의 `BL의존` 축**                  | `--selftest` | 위 문단. 5건을 근거 없이 도래로 올렸다                                                                                                                                                            |
| ★★**내 판정기의 「선행 섹션 없음 = 미해결」**   | `--selftest` | 섹션 없는 BL 8종(BL-365·376·402·461·486·488·562·071)은 **아카이브된 Resolved 일 수도** 있다. 모르는 것을 미도래로 단정하면 안 된다 ⇒ 「판단 필요」로 내린다                                       |
| ★★**내 하네스 케이스 ⑧ 이 거짓 통과**           | 자기 대조    | `▶ 불일치` 를 마커로 썼는데 그 블록 머리는 **「없음」일 때도 찍힌다**. 이 파일 59-61 이 경고한 그 함정에 새 케이스가 그대로 빠졌다 ⇒ 본문 `섹션은 DEFERRED` 로 교체                               |
| ★★**초안 판정기가 2축뿐이었다**                 | `--selftest` | 양성 대조 2건([BL-451]·[BL-591])이 「판단 필요」로 떨어졌다. `지금` 축(트리거가 **스스로 도래를 선언**)이 빠져 있었다 — 판별력 검사를 스윕 앞에 두지 않았으면 159건 판정 **뒤에** 드러났을 것이다 |
| ★**[BL-499] 「trigger 는 이제 발화 가능하다」** | 원문 대조    | 내 스캔이 이걸 「이미 발화」로 올렸다. 상태줄이 말하는 것은 **관측 가능성 확보**이고 `cancel`/`cancel_raced`/`cancel_stalled` 는 **시리즈조차 없다**. 「발화 가능」 ≠ 「발화」 ⇒ 미도래           |
| ★**[BL-462] 「구 백테스트가 남아 있는 동안」**  | 로컬 DB 실측 | 산문으로는 못 가른다 ⇒ 쟀다. COMPLETED 중 `sharpe_convention` 마커 없는 **구 컨벤션 1건** 잔존 + 신 컨벤션도 `tv_daily_rfr2`(2)/`tv_monthly_rfr2`(1) **혼재** ⇒ 조건 **참 = 도래**                |

★**어휘의 경계는 사용자가 정했다** — 동승 트리거(`그 파일을 다음에 열 때` 류, 실측 **73건**)를
미도래에 **포함**한다. 그것들은 「단독 착수 시 값이 0」이라고 **트리거 자신이 선언한 것**이다.
3면에서 DEFERRED 는 **ACTIVE 와 같은 「미완」 쪽**이라 인덱스 표·roadmap 편집이 **0행**이다.
근거 = [ADR-028](decisions/028-backlog-deferred-verdict.md).

★**변이 4/4 red — 그리고 각각 다른 케이스를 잡았다.** 어휘 삭제 → ⑥⑧ · crosscheck 제외 → **⑧만** ·
카운트표 → ⑥⑦ · **과다 포획(Open 도 삼키게)** → ⑦. 마지막 것이 음성 대조의 값이다.
sha256 복원 대조 완료.

### ★precommit-scope (2026-08-10, 같은 세션 후속) — [BL-687]·[BL-695] 종결 · 신규 [BL-696]

**⓪ 표를 만들자마자 그 표의 A·H 를 닫았다.** 둘 다 XS · `backend/src` 0줄.

- **[BL-687]** — 루트 `package.json` 의 backend 훅 3개가 `${0#backend/}` 만 참조해 **스테이징된 py
  중 첫 하나만** 검사했다. 재현 `1 file reformatted`(b·c 무시) → 수리 `"${0#backend/}"
"${@#backend/}"` → `3 files reformatted` → **음성 대조 1파일**(인용을 빠뜨리면 빈 인자가 들어가
  CWD 전체를 잡는다 — 큰따옴표가 0단어로 확장해 막는다) → 종단 `pnpm exec lint-staged` 2파일 전건.
- **[BL-695]** — 이 회차가 만든 `**트리거 판정:**` 규율에 **소유자가 없었다.** `docs-audit` 에
  `trigger_verdicts` 축 신설 — ACTIVE/DEFERRED 마다 **정확히 1개**(0=규율 누락 · 2+=중복 상태줄과
  같은 사고). 판정은 `bl-audit --list` 를 **되읽는다** — 상태줄 파서를 두 벌로 만들면 갈라지고,
  갈라지는 순간 어느 쪽이 맞는지 아무도 모른다. 양성 2(누락·중복) + 음성 1(정합 원장) 로 판별력 확인.

★★**같은 뿌리인데 실패 모드가 반대인 것을 찾았다** — `frontend/**` eslint 줄은 파일 목록을
**한 번도 참조하지 않아** 매 커밋 **레포 전량을 린트한다**(실측 **14.7s · 203% CPU**).
backend 는 **과소**(첫 하나), frontend 는 **과대**(전량)다. ⇒ [BL-696] 로 분리 등재.
★**결과가 맞아서 아무도 못 알아챘다** — 전량 린트는 스테이징분을 포함하므로 검사 자체는 통과한다.
**「초록이니까 맞다」가 여기서 또 틀렸다** — 맞은 것은 결과고 틀린 것은 비용이다.
