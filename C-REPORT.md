# 레인 γ — DEFERRED 178건 재판정 보고

제안 = [`C-PROPOSAL.md`](C-PROPOSAL.md) · 코드 0줄 · `docs/` 무변경 · 커밋까지, push 없음.

## 한 줄 결과

DEFERRED **178건 전량**을 ①~④ 로 갈랐다 — **① 1 · ② 2 · ③ 170 · ④ 5**.
닫자고 제안한 것은 **[BL-602] 하나**이고, ACTIVE 로 올리자고 제안한 것은 **[BL-718]·[BL-489] 둘**이다.

## 수용 기준별 판정

| AC   | 요구                           | 판정                                         | 근거                                                                                                                                                                                                                  |
| ---- | ------------------------------ | -------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AC-1 | 178건 전건 분류 · 합계 178     | ✅                                           | 판정 줄 178 · 고유 BL 178 · `bl-audit --list DEFERRED` 집합과 **완전 일치**(빠짐 0 / 군더더기 0)                                                                                                                      |
| AC-2 | ①·② 전건에 근거 명령 + 출력    | ✅                                           | BL-602 7건 · BL-718 5건 · BL-489 2건. 근거 없는 ①·② 0건                                                                                                                                                               |
| AC-3 | ① 무작위 5건 음성 대조         | ⚠️ **요구대로 못 했다 — 방향을 바꿔서 했다** | **① 이 1건뿐이라 5건을 뽑을 수 없다.** 없는 표본을 지어내지 않았다. 대신 ⑴ ① 1건에 변이 대조(플러그인 제거 → 원문 오류 문자 그대로 재현 → 복원) ⑵ ③ 무작위 5건(`seed=20260817`)을 반대 방향으로 확인 → **0/5 뒤집힘** |
| AC-4 | 판정별 묶음 · 4열 한 줄 요약   | ✅                                           | 178줄 전건이 `\| BL-NNN \| 판정 \| 근거 \| 제안 상태줄 \|` 4열                                                                                                                                                        |
| AC-5 | `docs/` 무변경                 | ✅                                           | `git status --porcelain -- docs/` = 0줄 · 추적 파일 diff 0. 워킹트리 변경은 `?? C-PROPOSAL.md` `?? C-REPORT.md` 둘뿐                                                                                                  |
| AC-6 | `bl-audit` · `docs-audit` rc=0 | ✅                                           | 둘 다 rc=0. bl-audit = 「5면 정합 · active=8 / deferred=178 / 전체=332」                                                                                                                                              |

## 표적 변이

판정기는 `C-PROPOSAL.md` 를 AC-1/AC-2/AC-4 로 재는 스크립트다(스크래치패드 보관, 커밋 대상 아님).
문자열 치환 쌍으로 심고 되돌린 뒤 **심을 때 쓰지 않은 방법(sha256)** 으로 복원을 확인했다.

| 변이                                                       | 기대                       | 실제                                                                             | 결과   |
| ---------------------------------------------------------- | -------------------------- | -------------------------------------------------------------------------------- | ------ |
| ① 판정(BL-602)에서 근거 명령을 지운다 (`$ ` 프롬프트 제거) | AC-2 가 ④ 로 내리라고 요구 | `AC-2 FAIL: BL-602 은 ①·② 인데 실행 가능한 근거 명령이 0건 — ④ 로 내려라` · rc=1 | ✅ red |
| 분류 합계를 177 로 만든다 (③ 한 줄 삭제)                   | AC-1 red                   | `AC-1 FAIL: 판정 줄 177 != DEFERRED 178` + `빠짐 ['BL-541']` · rc=1              | ✅ red |

복원: 기준선 `ad7d5581…` → 변이 2 되돌린 직후 `4ba5ac9a…` (**행을 다른 자리에 되꽂아 불일치**) →
③ 표를 원본 블록으로 통째 재생성해 `ad7d5581…` **바이트 동일** 복귀. 재검증 rc=0.
★손으로 되꽂은 복원이 실제로 틀렸고, sha256 대조가 그것을 잡았다.

## 무엇이 됐나

**① BL-602 — 닫자.** 본문의 재현 명령이 지금은 rc=0 이다. 처방 ①(루트 devDependencies 에
`prettier-plugin-tailwindcss`)이 `71f7101e`(2026-08-09) — **이 BL 과 무관한 커밋** — 에 얹혀 들어갔다.
prettier 는 ignore 된 파일에도 「All matched files use Prettier code style!」 를 찍으므로 메시지로는
통과와 건너뜀이 구분되지 않는다. 그래서 플러그인을 **실제로 떼어** 판별력을 쟀고, 원문 오류가
문자 그대로 재현됐다(rc=1). ★닫을 때 본문의 **이연 부채 2건**(`apps/web/README.md:39` 의 죽은
`.ai/rules/frontend.md` 참조 · `apps/web/AGENTS.md:271` 의 거짓이 된 [BL-646] 문장)을 별도 항목으로
옮겨야 한다 — 그냥 닫으면 둘이 함께 사라진다.

**② BL-718 — 올리자.** 트리거 「PR-1 머지 후」의 PR-1 = [ADR-029] 표준 레이아웃(`1cd5345a`).
`git merge-base --is-ancestor` 로 `origin/main` 조상 확인, 구획 경로 4종 실재, `.github/CODEOWNERS` 부재.

**② BL-489 — 올리자, 단 사람이 볼 충돌이 있다.** 트리거 「BL-488 해소 후」의 [BL-488] 이 Resolved 이고
그 산출물 `emit_from_bar_time` 이 `tasks/live_signal.py:3457,3462,3577` 에 실재한다.
★**그런데 같은 섹션의 `**트리거 판정:**` 줄은 「미도래 — 동승 조건」이라고 적는다.** 그 문장은 도래
여부가 아니라 **우선순위 주장**이고 DEFERRED 84건에 같은 문구가 일괄로 찍혀 있다. 트리거 문장을
정본으로 본 것이 내 판단이며, **그 한 걸음만은 근거가 아니라 해석이다** — 사람이 뒤집을 수 있다.

**④ 5건.** BL-753(사용자 결정 대기) · BL-023(「Trust Layer v2」가 레포에 정의처가 없다 — grep 2건이
전부 자기 행) · BL-235/236(트리거가 「Sprint 57+」/「Sprint 56+」인데 **스프린트 번호 체계가
없어졌다** — 현행은 날짜명, roadmap 의 마지막 숫자는 57) · BL-616(본문에 「2026-08-07 사용자 판정:
둘 다 하지 않는다」가 이미 적혀 있다).

**③ 170건 중 7건은 판정은 그대로지만 근거가 낡았다** — 제안서에 `★근거 낡음` 으로 표시했다.
BL-519(「Makefile 2곳」→ [ADR-036] 로 `mise.toml` 2곳) · BL-625(예시 `CLERK_SECRET_KEY` 는 [ADR-034]
로 소멸, 기전은 `config.py:404` 에 생존) · BL-658(`optimizer/executors/` 는 없다, 실제는 `engine/`) ·
BL-408(동승 대상 [BL-402] 가 「구조 소멸」로 Resolved — 묶음 절이 죽었다) · BL-491/BL-505([ADR-035]
경로 이동) · BL-581(로컬 축만 측정).

## 무엇이 안 됐나 / 확인하지 못한 것

- **AC-3 을 문자 그대로 이행하지 못했다.** ① 이 1건이라 5건 표본이 불가능하다. 위 표에 ⚠️ 로 남겼다.
- **[BL-581] 의 소크 서버 축.** 트리거 3종을 **로컬에서만** 쟀다(`.metrics` 1,445파일 = 상한 20,000 의
  7.2% · 디스크 여유 31G > 20G). 실제 계측이 도는 곳은 소크 서버이고 계약이 접속을 금지한다.
  **서버에서 재면 ② 로 뒤집힐 수 있다.**
- **DB 를 근거로 하는 트리거 4건 이상**(BL-574 · BL-725 · BL-732 · BL-738 등)은 조건이 DB 행이다.
  계약상 DB 를 읽지 않았다. 이들의 ③ 은 「트리거가 안 왔다」가 아니라 **「내가 볼 수 없었다」**다.
- **soak-gate 축 5건**(BL-015 · 553 · 593 · 745 · 751)은 스윕의 기계 판정 `soak-gate=UNKNOWN` 을
  그대로 받았다. 게이트를 직접 돌리지 않았다.
- **③ 170건의 「대상 생존」을 전건 손으로 보지는 않았다.** 근거는 기계 스크린 4종 + 무작위 5건
  대조까지다. 파일과 심볼은 남아 있는데 그 코드 경로가 더는 돌지 않는 종류의 사망은 안 걸러진다.

## 배치 진행 — 어디까지 했나

컨텍스트가 마르지 않았고 **178건 전량을 끝냈다.** 실제 진행은 BL 번호 구간 배치가 아니라
스크린 축이었다 — 178건을 한 번에 요약 덤프(4회 분할)로 훑고, 기계 스크린 4종으로 후보를
19+14+18+5건 뽑아 그 후보만 본문을 읽었다. `docs/backlog.md` 를 통째로 열지 않았다
(섹션 정규식 분해 + `sed`/`grep` 만 사용).

## 환경 메모

- 워크트리는 **이미 부트스트랩돼 있었다**(슬롯 4 · `.worktree-slot` = `QB_SLOT = 4`).
- **브랜치가 레인 파일과 다르다** — 레인 파일은 `stage/deferred-resweep` 를 적었는데 워크트리에
  붙어 있던 것은 `stage/deferred-sweep` 다. 환경을 바꾸지 않고 그대로 썼다. 게이트 슬러그는
  레인 파일대로 `deferred-resweep` 를 썼다.
- `bl-trigger-sweep.sh --selftest` = **17/17 통과**(전량 스윕의 전제) · `--include-deferred --tsv` rc=0 · 211행.
