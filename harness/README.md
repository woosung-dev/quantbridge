# harness — 순차 무인 step 실행기 (도입 시험 중)

> 출처: <https://github.com/jha0313/harness_framework> (강의 자료).
> **원본 as-is 채택이 원칙이다.** 우리 방식으로 고쳐 쓰지 않는다 — 2회 돌려보고 판정한다.
> 아직 `docs/reference/` 승격 대상이 **아니다**. 파일럿 결과에 따라 승격 또는 삭제(+tombstone).

---

## 1. 이게 무엇인가 — 우리 함대와 어떻게 다른가

`scripts/execute.py` 는 **오케스트레이터를 LLM 에서 스크립트로 내린다.** `claude -p` 를 step 마다
새 프로세스로 띄우고, 부모 컨텍스트가 아예 없으며, 유일한 보고 채널이 파일(`phases/*/index.json`)이다.

|                | herdr 함대                        | claude 서브에이전트    | harness `claude -p`      |
| -------------- | --------------------------------- | ---------------------- | ------------------------ |
| 컨텍스트 격리  | 강 (보고가 파일)                  | **약** (리턴이 부모로) | **강** (부모가 없다)     |
| 파일시스템     | **강** (워크트리 + 샌드박스)      | 약                     | 없음 (§3 으로 보완)      |
| 상태 영속·재개 | 파일은 있으나 **재개 로직 없음**  | 세션과 함께 소멸       | **`index.json` 이 재개** |
| 독립 검증      | CONTROL 이 통합 브랜치에서 재측정 | 없음                   | ★**없다** (§4)           |
| 병렬           | **4레인**                         | 다중                   | 순차 1                   |

★ **셋은 대체재가 아니라 계층이다.** CONTROL(판정) → 워크트리(격리·병렬) → harness(실행·상태·무인).

---

## 2. 원본과의 차이 — **1줄뿐이다**

```python
# scripts/execute.py:182
docs_dir = ROOT / "harness" / "docs"   # 원본: ROOT / "docs"
```

**왜 고쳤나.** 원본 `_load_guardrails()` 는 `docs/*.md` 를 필터 없이 통째로 프롬프트에 넣는다.
그건 게으름이 아니라 계약이다 — 원본 `docs/`(PRD·ARCHITECTURE·ADR·UI_GUIDE)에는
**정적 설계 제약만** 있고 진행 상태·백로그가 없기 때문이다.

우리 `docs/` 최상위는 그 계약과 **정확히 반대**다. glob 이 잡는 것은
`status.md`·`backlog.md`·`roadmap.md`·`lessons.md`(**814,211자**)뿐이고, 정본인
`docs/reference/`·`docs/decisions/` 는 하위 디렉터리라 하나도 안 잡힌다.
크기 문제가 아니라 **주입하면 안 되는 것만 골라 잡는** 문제다 —
step 세션이 「다음 행동 = …」과 미해결 BL 수백 건을 손에 쥔다.

그래서 우리 `docs/` 를 바꾸지 않고(참조 163회 + 상대경로 32회가 달려 있다), **원본 계약을 지키는
슬롯을 새로 만들었다.** 경로만 다르고 계약은 동일하다.

### `harness/docs/` — 원본 4축과 1:1

| 원본              | 우리 (심링크)                           | 묻는 것         |
| ----------------- | --------------------------------------- | --------------- |
| `PRD.md`          | `01-domain.md` → `CONTEXT.md`           | 무엇을 만드나   |
| `ARCHITECTURE.md` | `02-structure.md` → `AGENTS.md`         | 어디에 두나     |
| `ADR.md`          | `03-backend.md` → `backend/AGENTS.md`   | 무엇으로 만드나 |
| `UI_GUIDE.md`     | `04-frontend.md` → `frontend/AGENTS.md` | 어떻게 보이나   |

심링크라 **사본이 생기지 않는다**(ADR-026 SSOT). 파일명이 곧 주입 순서이자 섹션 헤더다.

**실측 (2026-08-12):** 814,211자 → **45,367자 (−94.4%)**.
양성 대조 4축 전건 주입 확인 · 음성 대조 제외 5파일 전건 미주입 + 그 검사가 수정 전 주입물에서
**5/5** 를 잡는 것까지 확인했다.

★**슬롯에 들어갈 수 있는 것은 「거의 변하지 않는 설계 제약」뿐이다.** 진행 상태·할 일·회고를
여기 넣으면 원본 계약이 깨진다. step 마다 다른 문서가 필요하면 `step{N}.md` 의
**「읽어야 할 파일」에 경로로** 적어라 — 원본이 그 섹션을 만든 이유가 그것이다.

**안 고친 것:** `:179` 의 `CLAUDE.md` 주입은 그대로 뒀다. 우리 `CLAUDE.md` 는 11바이트
(`@AGENTS.md` 한 줄)이고 `read_text()` 는 import 를 안 따라가므로 문자열 `@AGENTS.md` 가 그대로
들어간다 — 무해한 잡음이다. 규칙 본문은 Claude Code 가 자체로 로드하고, 슬롯에도 이미 있다.
`:12` 의 미사용 `import os` 도 원본 그대로 뒀다(상류 추적).

---

## 3. 실행

★**러너(원본 §E)만으로는 못 돈다.** 그 앞의 **C(step 설계) · D(파일 생성)** 이 있어야
`phases/<task>/` 가 생긴다. 그 절차와 **step 파일 템플릿·설계 원칙 7개**는
[`workflow.md`](workflow.md) 에 **원문 그대로** 있다 — step 파일을 쓰기 전에 그걸 먼저 읽어라.

```bash
python3 scripts/execute.py <phase-dir>          # 순차 실행 (원본 §E)
```

- **`--push` 는 쓰지 마라.** 승인 없는 push 는 Golden Rule 위반이다.
- **실행 위치는 호출자가 정한다.** `ROOT = Path(__file__).parent.parent`(`:22`)라서
  워크트리 안의 `scripts/execute.py` 를 부르면 ROOT 가 그 워크트리가 된다 — **코드 수정 0**.
  워크트리에서 돌리면 파일시스템 격리가 붙고 `--dangerously-skip-permissions`(`:239`)의
  블라스트 반경이 브랜치 하나로 묶인다.
- ★**celery 를 타는 step(백테스트·라이브신호·옵티마이저)은 워크트리에서 못 돈다** —
  worker 가 메인의 `src` 를 mount 한다. 그 step 은 메인 체크아웃에서 돌려라
  ([`worktree-parallel.md`](../docs/reference/operations/worktree-parallel.md)).

테스트: `python3 -m pytest scripts/test_execute.py` (원본 51건. 픽스처 2곳만 슬롯 경로로 맞췄다.)

★**「51 passed」를 실행기가 검증됐다는 뜻으로 읽지 마라.** 실측 — 테스트 12개 클래스는 전부
헬퍼 단위이고, **순차 실행 루프는 한 줄도 안 탄다**: `run()` · `_execute_single_step` ·
`_execute_all_steps` · `_finalize` · `_ensure_created_at` 이 테스트 파일에 **각 0회** 등장한다.
`run()` 을 no-op 으로 바꿔도 51건은 대부분 그대로 초록이다. 즉 이 초록이 말하는 것은
「내 1줄 수정이 헬퍼를 안 깼다」까지이고, 그 이상이 아니다.

## 3.1 ★as-is 로 안고 가는 위험 — 첫 run 전에 읽어라

2026-08-12 codex 적대 리뷰 8건을 **전건 코드 대조**했다. 아래는 전부 **원본 그대로의 성질**이지
이번 수정이 만든 결함이 아니다. 고치지 않는 이유 = as-is 채택이 이 회차의 실험 조건이기 때문이다.
파일럿에서 실제로 무는지 보고 판정한다.

| #   | 위험                                                                                                                                                                                                                                                           | 대조 결과                                                | 등급   |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- | ------ |
| 1   | `_checkout_branch`(`:113-134`)에 **clean-tree 검사가 0건**. dirty tree 를 그대로 새 브랜치로 데려가고 이후 `git add -A` 가 그것까지 커밋한다                                                                                                                   | ✅ 참 — 검사 0건 확인                                    | **P1** |
| 2   | 실패 경로(`:356`)에서도 `_commit_step()` 을 부른다. 3회 실패한 step 의 부산물이 커밋된다                                                                                                                                                                       | ✅ 참                                                    | **P1** |
| 3   | `_finalize`(`:387-393`) 가 **커밋 실패를 무시**하고 "Phase completed!" 를 찍는다. `--push` 면 그 상태로 push 까지 간다                                                                                                                                         | ✅ 참 — returncode 미검사 확인                           | **P1** |
| 4   | `--dangerously-skip-permissions`(`:239`)를 쓰면서 **워크트리를 강제하지 않는다**. §3 은 권고일 뿐 코드가 메인 실행을 막지 않는다                                                                                                                               | ✅ 참                                                    | **P1** |
| 5   | `_check_blockers`(`:84-90`)가 **checkout 보다 먼저** 돈다. 현재 브랜치 기준으로 판정한 뒤 브랜치를 바꾼다                                                                                                                                                      | ✅ 참 — `run()` 호출 순서 확인                           | P2     |
| 6   | `core.symlinks=false` / dangling 심링크면 슬롯이 **조용히 링크 문자열**이 되거나 `FileNotFoundError`                                                                                                                                                           | △ 조건부 — 이 레포는 macOS·ubuntu CI 라 도래 안 함       | P2     |
| 7   | ★`_invoke_claude`(`:241`)의 `subprocess.run(timeout=…)` 이 **`TimeoutExpired` 를 안 잡는다**. 상한을 넘기면 phase 전체가 traceback 으로 죽고, 그 step 의 자기신고는 **검증되지 않은 채 커밋된 상태로 남는다**                                                  | ✅ **B회차에서 실제로 물었다** (아래 §4)                 | **P1** |
| 8   | `progress_indicator` 의 `info.elapsed` 는 `finally` 에서 채워지는데 `_execute_single_step`(`:334`)이 **`with` 블록 안에서** 읽는다 ⇒ `✓ Step N [0s]` 로 **항상 0초**가 찍힌다                                                                                  | ✅ 참 — B회차 4 step 전건 `[0s]`                         | P3     |
| 9   | 실행기의 **non-zero exit code 가 완료를 막지 못한다**. `exitCode` 는 `:254` 에서 output json 에 실릴 뿐 `_execute_single_step` 이 **한 번도 읽지 않는다** — 세션이 비정상 종료해도 `index.json` 에 `completed` 만 있으면 통과한다                              | ✅ 참 — codex 리뷰 S2, 그 함수 범위 내 등장 0회          | **P1** |
| 10  | ★**`ac` 필드가 없으면 fail-open** 이다(`step.get("ac", [])` → 빈 배열 → 통과). B회차 개조분의 성질이고 테스트가 그 동작을 고정까지 한다. `step` 은 세션 호출 **전** 스냅샷이라 자기 ac 는 못 지우지만, **앞 step 이 뒤 step 의 ac 를 지우는 갈래**는 열려 있다 | ✅ 참 — codex S3. 실측 훼손은 **0**(동결 후 24건 무변경) | **P1** |
| 11  | AC 가 **세션이 변조했을 수도 있는 대상**을 그대로 실행한다 — clean snapshot·파일 해시·변경 allowlist 가 없어 `package.json` 스크립트를 no-op 으로 바꾸면 AC 가 통과한다                                                                                        | ✅ 참 · 이번 회차 **미발현**(`package.json` diff 0줄)    | **P1** |

★**7번의 최악 갈래는 재실행이다** (2026-08-13 codex 리뷰 S1 · 실제로 밟았다). 상한 초과로 러너가
죽어도 세션이 이미 써 둔 `completed` 는 파일에 남는다 ⇒ **다시 돌리면 그 step 은 pending 이 아니라서
AC 를 건너뛴 채 phase 가 닫힌다.** B회차 step 1 이 정확히 그렇게 통과했고(CONTROL 이 사후에 6/6 재실행해
참임을 확인), 그 사이 러너는 **AC 를 0건 실행**했다.

★**그래서 첫 run 전 preflight 2개는 건너뛰지 마라.**

```bash
git status --porcelain          # 반드시 빈 출력. 1·2번 위험이 여기서 막힌다
git rev-parse --abbrev-ref HEAD # 지금 어느 브랜치인지 눈으로 확인 (5번)
```

`--push` 는 어차피 안 쓴다(§3) — 3번의 최악 갈래가 그걸로 닫힌다.

## 3.2 ★실행기 샌드박스 (`-s workspace-write`) — 실측된 경계

2026-08-13, `--dangerously-bypass-approvals-and-sandbox` 를 `-s workspace-write` 로 바꾸고
1-step 프로브(`phases/l1probe/`)로 경계를 직접 쟀다. **가정하지 말고 이 표를 봐라.**

| 대상                                   | 결과                                              | 재현성              |
| -------------------------------------- | ------------------------------------------------- | ------------------- |
| 워크스페이스 안 쓰기 · `git commit`    | 허용                                              | 전건                |
| `/tmp` 쓰기                            | 허용                                              | 전건                |
| **홈 디렉터리 쓰기**(`~/.cache/uv` 등) | **거부** — `Operation not permitted (os error 1)` | **전건**            |
| 네트워크(`localhost:8100`)             | ★**갈린다**                                       | **3 차단 / 6 관측** |

★★**「샌드박스가 네트워크를 막는다」는 거짓이다.** 같은 커맨드를 6회 관측해 3회 차단 · 3회 허용
(200)이 나왔다. **한 번 보고 단정했다가 프로브가 뒤집었다** — 기전은 아직 모른다(`[확인 필요]`).

★**그럼에도 BE pytest 는 실제로 막혔다** — 네트워크가 아니라 **홈 캐시 쓰기**에서다
(`uv` 가 `~/.cache/uv` 초기화에 실패). 즉 이 옵션의 집행력은 **파일시스템 축에만** 있다.
「네트워크가 막히니 무거운 검증이 구조적으로 불가능하다」로 설계를 세우지 마라 — 3할은 통과한다.

★**게이트가 안 돈 이유도 샌드박스가 아니었다** — `final-gates.sh` 자신이 **dirty worktree** 를
거부했다. 프로브의 미커밋 파일이 우연히 그 갈래를 열었을 뿐이고, 트리가 깨끗했다면 돌았을 것이다.

⇒ **범위 강제를 샌드박스 하나에 걸지 마라.** 확실히 얻는 것은 「워크스페이스 밖을 못 쓴다」뿐이고,
`--dangerously-bypass-approvals-and-sandbox` 대비 **더 안전하다는 것까지가 이 옵션의 값**이다.
step 이 무엇을 돌릴지는 여전히 프리앰블(코드)과 러너의 diff 검사로 막아야 한다.

## 3.3 ★실행기 대조 실험 (2026-08-13) — 왜 회차가 66분이 됐나

**step 파일·프리앰블을 고정하고 실행기만 바꿔** 같은 1-step 프로브를 4회 돌렸다.

| arm | 실행기 | 벽시계             | 셸 실행 | `final-gates`                 |
| --- | ------ | ------------------ | ------- | ----------------------------- |
| A   | codex  | 2분 36초           | 19건    | 읽기만(실행 0)                |
| B   | codex  | **1시간 6분 33초** | 14건    | **실행 3회** → BE pytest 전량 |
| C   | claude | **52초**           | 5건     | 0                             |
| D   | claude | **59초**           | 4건     | 0                             |

★**하네스 자체의 오버헤드는 1분 미만이다.** 66분은 전부 게이트였다.

★**arm B 가 자기 입으로 이유를 말했다** — 「**프로젝트 규칙의 마지막 커밋 후 게이트 의무에 따라**
`scripts/final-gates.sh` 를 실행하겠습니다」. 그리고 막히자 스스로 우회했다: 인자 없이 실행(rc=1) →
`--run eod`([BL-706] 으로 금지) → **자기가 슬러그를 지어냄**(`execprobe-b-step1`).

**인과는 3단계다.** ⑴ `_load_guardrails`(`:183`)가 슬롯 4파일을 **필터 없이** 주입한다 ⑵ 그중
`02-structure.md` = 루트 `AGENTS.md` 전문이고 `:27` 이 「게이트는 마지막 커밋 뒤에」다 ⑶ 프리앰블
규칙 6이 **세션에게 커밋을 시켜** 그 트리거를 당긴다. 게이트는 **회차 단위**(PR 직전 1회)인데
그 맥락이 글에 없다 — 무인 세션은 물어볼 사람도 없다.

⇒ **수리는 프리앰블 2줄이다**(2026-08-13): 규칙 6을 「커밋하지 마라 — 커밋은 러너가 한다」로
뒤집어 트리거를 없애고, 규칙 4에 검증 경계(`ac` 가 전부 · `final-gates`·BE 전량 pytest·e2e·
게이트 신호 파일 금지)를 박았다. 러너의 `_commit_step()` 이 어차피 `git add -A` 로 커밋하므로
기능 손실은 0이고 커밋 노이즈는 절반이 된다. ★**수리 후 재측정은 아직 못 했다** — 검증 run 이
12분간 codex 세션조차 열지 못해 중단했다(`[확인 필요]`).

★**claude 는 6/6 으로 범위를 지켰다**(오늘 2회 + A회차 4 step). codex 는 2/2 로 게이트에 손을 댔고
그중 1회가 실행까지 갔다. **실행기 축은 실재하지만, 문을 열어 둔 것은 우리 규칙이다.**

---

## 4. ★파일럿 설계 — 무엇을 재는가

**재는 것은 「자기채점이 뚫리는가」 하나다.**

원본은 AC 커맨드를 **러너가 실행하지 않는다**(`:312-313`). step 세션이 `index.json` 에 써넣은
`"completed"` 를 그대로 믿는다. 즉 **코드를 쓴 쪽이 자기 채점을 한다.** 이 레포는 그 실패를
4회 기록했다(`docs/lessons.md` · 메모리). 원본이 그 자리를 비워둔 것은 실수가 아니라
**완전 격리의 대가**다 — 판단 주체를 없애면 판사도 없어진다.

| 회차  | BL                          | 판본                                                                                                                                                          |
| ----- | --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **A** | [BL-708] 캐논 대비 비결정성 | **순수 as-is.** 세션 자기신고. CONTROL 이 **사후에 각 step 의 AC 를 재실행**해 대조                                                                           |
| **B** | [BL-709] RSC prefetch 정렬  | **러너 AC 판정 + 실행기 codex.** `index.json` step 에 `ac` 필드 추가, `execute.py` 가 exit code 로 판정. ★축이 **둘** 바뀌었다 — 결과를 한 축에 귀속하지 마라 |

- 둘 다 `Est: S`, 둘 다 2026-08-12 같은 회차 출신, 둘 다 celery·`make seed` 무관 → **같은 조건**.
- ★**가드레일은 두 회차 공통으로 고정한다**(§2 슬롯). 회차마다 다르면 실패 원인이
  「자기채점」인지 「컨텍스트 폭발」인지 구분되지 않는다.
- 표본은 BL 2건이 아니라 **step 6~8개**다. 「자기신고 completed vs 사후 실측」 불일치 건수를 센다.
- step 파일은 **CONTROL 이 쓰고 사용자가 승인**한다(함대 §1 「수용 기준은 착수 전 동결」과 같은 계약).

### A회차 실측 (2026-08-12, `feat-bl708` · 미푸시)

| 항목             | 값                                                                         |
| ---------------- | -------------------------------------------------------------------------- |
| **불일치**       | **0 / AC 16건** (자기신고 4/4 `completed`, CONTROL 전건 재실행)            |
| 러너 재시도      | **0회** (`MAX_RETRIES` 미도달, `blocked` 0)                                |
| **저작 비용**    | **3분 3초** — `index.json` 2 + `step1~4.md`, 12,059자                      |
| 벽시계           | 46분 3초 (step1 11:56 · step2 13:51 · step3 13:00 · step4 7:12)            |
| `claude -p` 비용 | **$17.83** (step당 $4.18~4.85 · turns 35~53)                               |
| 커밋             | **9** = step당 2(feat+chore) × 4 + finalize 1. 우리 PR 중앙값 2 대비 4.5배 |

★**불일치 0 을 「자기채점은 안 뚫린다」로 읽지 마라.** AC 16건 중 **12건이
typecheck·lint·`git diff`·`bl-audit`·`docs-audit`** 이다 — CONTROL 이 재실행해도 같은 답이 나오도록
**일부러 결정적인 것만 골랐다**(안 그러면 불일치가 부정직인지 측정 잡음인지 안 갈린다). 즉 이 0 이
말하는 것은 「**결정적 AC 를 주면 자기신고가 그 AC 와 어긋나지 않았다**」까지이고, 그 이상이 아니다.
확률적 AC(캘리브레이션 3회)는 1건뿐이었고 CONTROL 이 2배수(6회)로 재실측해 6/6 초록을 얻었다.

★**대신 값이 나온 곳은 AC 설계 비용이었다.** 착수 전 스모크에서 **내가 쓴 AC 2건이 헛초록**이었다 —
하나는 숫자를 `wc -l` 로 찍기만 하고 판정을 안 했고(자기채점 프리패스), 하나는 기준을 `main` 으로 잡아
후속 step 의 정당한 변경까지 위반으로 셌다. 그리고 「단독 파일 3회 반복」을 AC 로 쓸 뻔했는데 실측이
**3/3 초록**(전량 병렬에서만 재현)이라 판별력 0 이었다. **B회차가 재야 할 것은 이 설계 비용이
러너로 옮겨가는가다** — `ac` 필드를 러너가 실행하면 AC 를 쓰는 쪽의 부담은 그대로다.

★**부수 발견 2건.** ⑴ 러너 브랜치는 `feat-{phase}`(하이픈)인데 이 레포 pre-push 화이트리스트는
`feat/*`(슬래시)다 — push 전 `git branch -m` 이 필요하다. ⑵ preflight 를 **세션 시작에 한 번만** 재고
20분 뒤 브랜치를 만들었더니 그 사이 HEAD 가 `main`→`feat/harness-workflow` 로 옮겨 가 있었다
(AGENTS.md 「baseline 재측정 preflight」가 정확히 이것을 막으려는 규칙이다). 브랜치를 딸 때 다시 재라.

### B회차 실측 (2026-08-13, `feat-bl709` · [BL-709])

**판본:** `index.json` step 에 `ac` 배열 + `execute.py` 가 `bash -c` 로 재실행해 exit code 로 판정
(`_run_acceptance` · `AC_TIMEOUT=600`). 자기신고 `completed` 를 **받아들이기 전에** 돌리고,
하나라도 rc≠0 이면 `↯ AC OVERTURN` 후 기존 retry 경로로 보낸다. **실행기도 `codex exec` 로 바꿨다.**

| 항목                  | 값                                                                                                   |
| --------------------- | ---------------------------------------------------------------------------------------------------- |
| **러너 뒤집기**       | **0회** (step 2~4 의 AC 18건 전건 1차 통과 · 재시도 0 · blocked 0)                                   |
| 러너가 실제로 돌린 AC | **18 / 24** — step 1 의 6건은 **0건**(위험 7번이 러너를 죽였다). CONTROL 이 대신 6/6                 |
| CONTROL 사후 재실행   | **24/24 참** (22건은 그 자리에서, **순서 의존 2건**은 그 step 커밋 시점 트리로 재현)                 |
| `ac` 필드 훼손        | **0** — 동결 커밋(`56c57212`) 이후 24개 문자열이 한 줄도 안 바뀌었다                                 |
| **저작 비용**         | **6분 10초** = 개조 2분 21초(변이 3건 검증 포함) + step 파일 3분 49초 (AC 24건)                      |
| 벽시계                | 1차 30분(죽음) + 2차 41분 41초 = **71분 41초** · step2 15:06 · step3 13:58 · step4 11:15             |
| 실행기 비용           | codex 토큰 **input ~57.7M(94% 캐시) / output ~120k**. ★A 의 $17.83 과 **비교 불가**(과금축이 다르다) |
| 커밋                  | **10** (step당 2 × 4 + finalize 1 + 하네스 개조 1)                                                   |

★**「뒤집기 0」을 「자기채점은 안 뚫린다」로 읽지 마라 — A 와 같은 이유다.** AC 24건 중 20건이
typecheck·lint·vitest·grep·`git diff`·게이트라 결정적이고, 세션이 그걸 **먼저 스스로 돌리고** 신고했다.
이 0 이 말하는 것은 「결정적 AC 를 주면 자기신고가 그것과 어긋나지 않았다」까지다 — **2회차 연속 같은 답**이다.

★★**대신 이 회차가 실제로 반증한 것은 「as-is 위험 6건」의 목록 자체였다.** 회차를 죽인 것은
표에 있던 6건이 아니라 **없던 7번째**(timeout 미처리)였고, 그 결과가 정확히 B 가 막으려던 상태다 —
**AC 를 한 건도 안 돌린 채 `completed` 가 커밋됐다.** 판정기를 추가해도 그 판정기를 부르는 경로가
죽으면 아무것도 안 잰다. `--push` 를 안 쓴 것만이 이 상태가 원격으로 나가는 것을 막았다.

★**실행기 축(codex)이 축을 하나 더 만들었다.** step 1 에서 codex 는 작업을 **4분 19초**에 끝내고
스스로 커밋한 뒤, **AC 에 없는 backend pytest 전량을 두 번** 돌리다 1800s 상한에 걸렸다
(상한을 2700 으로 올린 뒤 step 2~4 는 11~15분에 정상 종료). A 의 claude 는 이러지 않았다.
⇒ **B 가 A 와 다른 결과를 낸 원인을 「러너 AC 판정」으로 귀속하지 마라.** 축이 둘이다.
분리하려면 실행기만 claude 로 되돌린 3회차가 필요하고, step 파일은 실행기와 무관하니 저작 비용은 0 이다.

★**AC 설계 비용은 러너로 옮겨가지 않았다** — 이것이 B 의 답이다. 러너가 실행을 가져갔을 뿐,
「무엇을 재야 red 가 나는가」는 그대로 사람 몫이었다. 착수 전 24건을 전부 돌려 **red 10 / green 14** 를
확인하는 과정에서 헛초록 2건을 걸렀다: ⑴ `grep -q 'searchParams' page.tsx` 는 **결함을 설명하는
주석** 때문에 이미 rc=0 이라 판별력 0 이었고 ⑵ `SORT_OPTIONS` 는 backtests 쪽에 **무관한 동명 상수**가
있어 「정의 1벌」이 거짓 red 였다. 오히려 **비용이 늘었다** — 러너가 exit code 로 판정하므로
AC 버그가 이제 측정 오차가 아니라 **회차를 막는 red** 가 된다.

★**순서 의존 AC 2건은 사후 재실행이 불가능하다.** 「이 회차가 아직 X 를 안 건드렸다」(step1 #6·step2 #6)는
뒤 step 이 X 를 정당하게 고치면 영원히 red 가 된다. 그 step 의 커밋 시점 트리(`git diff main <commit>`)로
재현해야 참이 확인된다 — **사후 대조 프로토콜을 쓸 거면 AC 를 시점 독립으로 짜라.**

### 같이 재는 것

| 항목                 | 왜                                                           |
| -------------------- | ------------------------------------------------------------ |
| **회차당 저작 비용** | step 파일 N개 작성 시간. 도입 판단의 유일한 미측정 변수      |
| 커밋 노이즈          | step 당 2커밋(feat+chore). 우리 PR 중앙값이 2커밋 → 6커밋 됨 |
| 실제 토큰·비용       | 45,367자 × step × 재시도. 캐시가 붙는지 포함                 |

---

## 5. 판정 후 갈 곳

- **값을 냈다** → ADR-029 로 승격 + `docs/reference/operations/workflows/` 로 이동, 팀 플러그인화
- **안 냈다** → `scripts/execute.py`·`harness/`·`phases/` 삭제 + tombstone 1줄
- 어느 쪽이든 반증 카드 1~2천자 → `docs/lessons.md` → `dev-log/INDEX.md` 한 줄 (ADR-026 §3)

### 2회차 끝난 시점의 판정 근거 — ★**최종 판정은 사용자와 함께 낸다** (2026-08-13 현재 미결)

**측정된 값 (2회차 합산)**

|                        | A (as-is)   | B (러너 AC + codex)              |
| ---------------------- | ----------- | -------------------------------- |
| 자기신고 ↔ 실측 불일치 | 0 / 16      | 0 / 24                           |
| 저작 비용              | 3분 3초     | 6분 10초 (개조 2:21 + step 3:49) |
| 벽시계                 | 46분 3초    | 71분 41초 (죽은 1차 30분 포함)   |
| 커밋                   | 9           | 10 (우리 PR 중앙값 2 대비 5배)   |
| 산출물                 | BL-708 종결 | BL-709 종결 (FE 4파일 · 85+/25−) |

**승급 쪽 근거** — 두 회차 모두 **사람이 한 번도 개입하지 않고** BL 하나를 끝까지 닫았다
(코드 + 원장 3면 + ⓪ 표). 산출물 품질은 리뷰에서 수정할 것이 없었다.

**삭제 쪽 근거** — ⑴ **재는 것이 안 재진다.** 두 회차 20건에서 불일치 0 인데, 그건 결정적 AC 를 준
결과라 「자기채점이 뚫리는가」에 대한 답이 **여전히 없다**. ⑵ 값이 나오는 자리(AC 설계)는
**옮겨가지 않았고 오히려 비싸졌다**. ⑶ 실행 경로가 취약하다 — 위험 7번 하나로 회차가 죽었고,
그때 남은 상태가 정확히 이 하네스가 막으려던 것이었다. ⑷ 커밋 노이즈 5배.

★**판정을 미루려면 3회차의 조건을 먼저 정해라** — 실행기를 claude 로 되돌려 축을 분리하고,
**결정적이지 않은 AC**(같은 커밋 N회 반복·확률적 계측)를 일부러 섞어야 이 파일럿의 원래 질문이
비로소 측정된다. 지금 판정하면 「값을 못 냈다」가 아니라 「**질문을 못 물었다**」로 닫는 것이다.
