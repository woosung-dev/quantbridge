docs/archive/

# [BL-595] 사망 재현 픽스처 — `apps/api/scripts/capture_bl595_death_fixtures.py` 가 쓰는 그대로
# 둔다. prettier 가 OHLCV 배열을 다시 접으면 캡처를 재실행할 때마다 diff 가 나서
# "얼렸다" 가 성립하지 않는다(실측: 재실행 1회에 12,000줄 diff).
apps/api/tests/fixtures/bl595/

# [BL-717/ADR-031] 계약 산출물은 export_openapi.py 의 json.dumps 가 포맷을 소유한다.
# prettier 가 짧은 배열을 한 줄로 접어 커밋 시점에 재포맷하면 `--check`(byte 대조) drift
# 게이트가 영구 빨강이 된다 — 1차 커밋에서 실측(797줄 diff). bl595 픽스처와 같은 축.
contracts/openapi/

# [BL-602] 루트 prettier 가 `apps/web/.prettierrc` 의 `prettier-plugin-tailwindcss` 를 해석하지
# 못해 죽는다(루트 node_modules 에 그 플러그인이 없다). [ADR-027] 이 스택 규칙을 그 디렉터리로
# 옮기면서 이 함정의 표면이 넓어졌다 — 두 파일을 ignore 로 **회피**한다(근본 수리 아님).
# ★BL-602 가 닫히면 아래 세 줄을 지워라.
apps/web/AGENTS.md
apps/web/CLAUDE.md
# 2026-08-07 prototype-canon-v2 회차 추가 — 1세대 프로토타입 삭제로 :70 의 `00-landing.html`
# 참조가 dangling 이 되는데, 이 파일을 스테이징하면 같은 트랩에 걸려 커밋이 죽는다.
apps/web/README.md
