docs/archive/

# [BL-595] 사망 재현 픽스처 — `apps/api/scripts/capture_bl595_death_fixtures.py` 가 쓰는 그대로
# 둔다. prettier 가 OHLCV 배열을 다시 접으면 캡처를 재실행할 때마다 diff 가 나서
# "얼렸다" 가 성립하지 않는다(실측: 재실행 1회에 12,000줄 diff).
apps/api/tests/fixtures/bl595/

# [BL-717/ADR-031] 계약 산출물은 export_openapi.py 의 json.dumps 가 포맷을 소유한다.
# prettier 가 짧은 배열을 한 줄로 접어 커밋 시점에 재포맷하면 `--check`(byte 대조) drift
# 게이트가 영구 빨강이 된다 — 1차 커밋에서 실측(797줄 diff). bl595 픽스처와 같은 축.
contracts/openapi/

# [BL-602] 2026-08-15 제거. 이 회피의 전제(「루트 node_modules 에 prettier-plugin-tailwindcss 가
# 없어 루트 prettier 가 죽는다」)가 2026-08-09 `71f7101e` 에서 루트 devDependencies 에 그 플러그인이
# 추가되며 깨졌다. 실측: `prettier --ignore-path /dev/null --check apps/web/{AGENTS,CLAUDE,README}.md`
# 가 **죽지 않고** 포맷 차이만 보고한다(rc=1, 2파일). 파일 스스로 「BL-602 가 닫히면 지워라」고
# 적어 뒀던 세 줄 = `apps/web/AGENTS.md`·`CLAUDE.md`·`README.md`.
