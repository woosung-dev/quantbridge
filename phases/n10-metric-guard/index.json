{
  "project": "QuantBridge",
  "phase": "n10-metric-guard",
  "steps": [
    {
      "step": 0,
      "name": "census-message-and-labels-axis",
      "status": "completed",
      "ac": [
        "cd apps/api && set -a; . ./.env.local; set +a; uv run pytest tests/common/test_metric_guard_census.py -q",
        "cd apps/api && set -a; . ./.env.local; set +a; uv run pytest tests/common -k labels_outside_guard -q",
        "cd apps/api && set -a; . ./.env.local; set +a; test \"$(uv run pytest tests/common -k labels_outside_guard --collect-only -q 2>/dev/null | grep -c '::')\" -ge 2"
      ],
      "started_at": "2026-08-24T23:32:37+0900",
      "summary": "착수 red 동결: labels-outside-guard 14건 (`apps/api/src/tasks/live_signal.py`: 14); lambda 지연평가 음성 대조 4건 (`apps/api/src/common/metrics.py`: 1, `apps/api/src/common/metrics_multiproc.py`: 2, `apps/api/src/tasks/trading.py`: 1).",
      "completed_at": "2026-08-24T23:39:16+0900"
    },
    {
      "step": 1,
      "name": "fix-live-signal-labels-outside-guard",
      "status": "completed",
      "ac": [
        "cd apps/api && set -a; . ./.env.local; set +a; uv run pytest tests/common -k labels_outside_guard -q",
        "cd apps/api && set -a; . ./.env.local; set +a; uv run pytest tests/common/test_metric_guard_census.py -q",
        "cd apps/api && set -a; . ./.env.local; set +a; uv run pytest tests/tasks -q",
        "cd apps/api && uv run ruff check src/tasks/live_signal.py"
      ],
      "started_at": "2026-08-24T23:39:19+0900",
      "summary": "`live_signal.py`의 labels-outside-guard 14건을 13개 `_count_safely`와 duration observe 1개 lambda 지연평가로 치환해 scanner 0건과 Step 1 AC를 통과했다.",
      "completed_at": "2026-08-24T23:43:45+0900"
    },
    {
      "step": 2,
      "name": "wrap-conditional-entry-janitor",
      "status": "pending",
      "ac": [
        "cd apps/api && set -a; . ./.env.local; set +a; uv run pytest tests/common/test_metric_guard_census.py -q",
        "cd apps/api && set -a; . ./.env.local; set +a; uv run pytest tests/common -k labels_outside_guard -q",
        "cd apps/api && set -a; . ./.env.local; set +a; uv run pytest tests/tasks -q",
        "cd apps/api && uv run ruff check src/tasks/conditional_entry_janitor.py"
      ]
    },
    {
      "step": 3,
      "name": "wrap-ws-circuit-breaker",
      "status": "pending",
      "ac": [
        "cd apps/api && set -a; . ./.env.local; set +a; uv run pytest tests/common/test_metric_guard_census.py -q",
        "cd apps/api && set -a; . ./.env.local; set +a; uv run pytest tests/common -k labels_outside_guard -q",
        "cd apps/api && set -a; . ./.env.local; set +a; uv run pytest tests/tasks -q",
        "cd apps/api && uv run ruff check src/tasks/_ws_circuit_breaker.py"
      ]
    },
    {
      "step": 4,
      "name": "wrap-tasks-trading",
      "status": "pending",
      "ac": [
        "cd apps/api && set -a; . ./.env.local; set +a; uv run pytest tests/common/test_metric_guard_census.py -q",
        "cd apps/api && set -a; . ./.env.local; set +a; uv run pytest tests/common -k labels_outside_guard -q",
        "cd apps/api && set -a; . ./.env.local; set +a; uv run pytest tests/tasks -q",
        "cd apps/api && uv run ruff check src/tasks/trading.py"
      ]
    },
    {
      "step": 5,
      "name": "wrap-trading-package",
      "status": "pending",
      "ac": [
        "cd apps/api && set -a; . ./.env.local; set +a; uv run pytest tests/common/test_metric_guard_census.py -q",
        "cd apps/api && set -a; . ./.env.local; set +a; uv run pytest tests/common -k labels_outside_guard -q",
        "cd apps/api && set -a; . ./.env.local; set +a; uv run pytest tests/trading -q",
        "cd apps/api && uv run ruff check src/trading"
      ]
    },
    {
      "step": 6,
      "name": "wrap-common-and-backtest",
      "status": "pending",
      "ac": [
        "cd apps/api && set -a; . ./.env.local; set +a; uv run pytest tests/common/test_metric_guard_census.py -q",
        "cd apps/api && set -a; . ./.env.local; set +a; uv run pytest tests/common -k labels_outside_guard -q",
        "cd apps/api && set -a; . ./.env.local; set +a; uv run pytest tests/common tests/tasks -q",
        "cd apps/api && uv run ruff check src/common src/tasks/backtest.py"
      ]
    },
    {
      "step": 7,
      "name": "allowlist-self-accounting-site",
      "status": "pending",
      "ac": [
        "cd apps/api && set -a; . ./.env.local; set +a; uv run pytest tests/common/test_metric_guard_census.py -q",
        "cd apps/api && set -a; . ./.env.local; set +a; uv run pytest tests/common -k census_allowlist -q",
        "cd apps/api && set -a; . ./.env.local; set +a; test \"$(uv run pytest tests/common -k census_allowlist --collect-only -q 2>/dev/null | grep -c '::')\" -ge 1"
      ]
    },
    {
      "step": 8,
      "name": "fault-injection-selfcheck",
      "status": "pending",
      "ac": [
        "cd apps/api && set -a; . ./.env.local; set +a; uv run pytest tests/tasks tests/trading tests/common -k metric_failure_does_not_escape -q",
        "cd apps/api && set -a; . ./.env.local; set +a; test \"$(uv run pytest tests/tasks tests/trading tests/common -k metric_failure_does_not_escape --collect-only -q 2>/dev/null | grep -c '::')\" -ge 4",
        "cd apps/api && set -a; . ./.env.local; set +a; uv run pytest tests/common/test_metric_guard_census.py tests/common -k 'census or labels_outside_guard' -q",
        "cd apps/api && set -a; . ./.env.local; set +a; uv run pytest tests/tasks tests/trading tests/common -q"
      ]
    }
  ],
  "created_at": "2026-08-24T23:32:37+0900"
}
