{
  "project": "QuantBridge",
  "phase": "runner-ac",
  "steps": [
    {
      "step": 0,
      "name": "ac-verdict-core",
      "status": "pending",
      "ac": [
        "cd apps/api && uv run --env-file .env.local pytest tests/harness/test_execute_ac.py -q",
        "cd apps/api && test \"$(uv run --env-file .env.local pytest tests/harness/test_execute_ac.py --collect-only -q 2>/dev/null | grep -c '::')\" -ge 4",
        "cd apps/api && uv run ruff check tests/harness/"
      ]
    },
    {
      "step": 1,
      "name": "ac-sequence-and-timeout",
      "status": "pending",
      "ac": [
        "cd apps/api && uv run --env-file .env.local pytest tests/harness/test_execute_ac.py -q",
        "cd apps/api && test \"$(uv run --env-file .env.local pytest tests/harness/test_execute_ac.py --collect-only -q 2>/dev/null | grep -c '::')\" -ge 7",
        "cd apps/api && uv run ruff check tests/harness/"
      ]
    }
  ]
}
